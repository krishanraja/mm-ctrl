# G25 private rendered operator composition, R138 strategy

## Outcome

Prove that the approved private operator instrument can render the exact live five-field review projection from the isolated Supabase project inside a disposable authenticated browser session, while production, public routes, customer data, decision authority and durable fixture residue remain zero.

## Why this route

R135 proved hosted bytes can cross the strict TypeScript adapter, but not that the actual React gateway can render those bytes. The smallest compounding move is therefore a private composition harness around the already approved Decision Bench, not another mock and not a public route.

The rejected alternative is wiring the existing application client or opening a persistent route. The existing client targets production, and a public route would widen authority before the private seam is proved.

## Chain

`krish-principles -> mindmake CTRL context -> strategy-brief -> build-apps-with-krish resume -> krish-design locked-system classification -> krish-build -> Supabase isolated runtime -> verification-loop`

## Target and source of truth

- Repository: `krishanraja/mm-ctrl`
- Branch: `codex/g20-context-exchange-proof`
- Baseline: `4842258c1f2943e197c76b58559e5684dbe60940`
- Isolated project: `cgkcplcamsijghalintq`
- Production project: `bkyuxvschuwngtcdhsyg`, read and write closed
- UI baseline: founder-approved R137 Decision Session and existing desktop Decision Bench
- Data boundary: R130 owner binding, R131 strict client parser, R133 gateway and R135 hosted adapter proof

## Governing rule

The private desktop instrument may show one owner-authorised review signal containing only review packet ID, question, headline, consequence and ready time. It may copy the exact question. It may not expose raw packet material, hashes or denial reasons; contact the leader; decide for the owner; mutate the Brain; or infer a signal from an unavailable response.

At 620 pixels and below, the later founder-approved R137 sequential Decision Session remains the only active projection. The hidden desktop gateway must not issue a review read merely because it exists in the DOM.

## Authority

Autonomous scope covers branch code, a disposable local browser server, temporary isolated-project identities and rows, exact cleanup, deterministic checks, a branch commit and documentation. Main, production, public route connection, customer data, durable writes, release, merge and legacy retirement remain closed.

## Pass signals

1. One desktop browser request crosses the exact isolated RPC and strict R131 adapter.
2. The rendered signal matches all five live fields and exposes none of the forbidden packet or authority material.
3. Clipboard success follows a confirmed browser write of the exact question.
4. An unauthorised workspace yields the same absent signal as unavailable state.
5. At 320 by 568, the R137 phone session still fits without horizontal or vertical overflow and issues zero hidden review reads.
6. The owner standard remains unchanged, the operator cannot decide and production writes remain zero.
7. Both a successful browser run and a deterministic failure immediately after hosted row creation end with all eleven disposable categories reading back as zero.
8. The staged browser session moves into memory-only storage and the local-storage auth value is removed before the instrument renders.

## Rollback ready

The known-good code baseline is commit `4842258c1f2943e197c76b58559e5684dbe60940`. Code rollback is an ordinary revert of the bounded R138 commit. Runtime recovery is deterministic deletion of the exact disposable users, operator principal, links, workspaces, grants, roles, receipts and derived owner fixture, followed by a zero-count readback. No production recovery path is required because production is not touched.
