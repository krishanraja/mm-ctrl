# G24 trusted canonical ingress R59

**Status:** fully materialized local architecture repair; independent attack remains blocking

**Authority:** the exact generated R59 JSON contract

## What R59 repairs

R59 closes every declared array-order authority in the frozen R58 contract. It independently inventories all 21 `ordered_by` sites and routes them through six closed handlers: unsigned UTF-8 scalar order, unsigned UTF-8 effect-key order, structured watermark tuple order, the closed reuse/enrich/ask/session route order, the declared outbox failure-code order and companion-aligned evidence order. Unknown ordering authorities fail closed.

The fixture interpreter now executes both named ordered identifier array types and exact-member constraints. Scalars, duplicates, missing members and reversed values are rejected. Question answer effects and honest exits therefore retain their exact membership and deterministic order, while the outbox failure list cannot be resealed in a different order.

Intervention payload bytes must be the exact canonical encoding of the decoded, kind-specific closed inner schema. Pretty printing, key injection, alternate key order and inner-outer substitution fail before the row is accepted. Question and atom fingerprints are recomputed from their declared authorities. Visible consequence and pending proposal text must also be nonempty and exactly trimmed.

The conditional exclusion boundary remains honest and is now independently reconstructed. The checker derives the exact 127 affected identity rows from the frozen identity fixtures and the 78 unproved rule set, then requires exact row content, uniqueness and count. Empty, dropped, duplicated or substituted exclusion rows fail.

The focused checker independently validates the candidate and rejects 278 attacks. It covers 1,228 native persisted identities, 374 selectors over 2,516 source-schema-valid contexts, 57,902 semantic-reference occurrences and 244 manifest rows.

## Honest semantic boundary

R59 does not convert the 78 unresolved cross-artifact or live rules into documentation proof. Twenty-five closed local predicates execute against their canonical fixtures, zero detached contexts count as proof, and the same 127 identities remain excluded from full conditional-semantic completeness.

## Product boundary

R59 is invisible infrastructure for the one canonical Brain and leader-owned authority. It changes no customer-facing language, interaction, approval step or product promise. It opens no adapter, database object, runtime connection, UI, deployment or external action.
