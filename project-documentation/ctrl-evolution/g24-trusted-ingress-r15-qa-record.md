# G24 trusted canonical ingress R15 QA record

**State:** fully materialized local contract candidate; deterministic and mutation checks pending independent attack

## Verification scope

The R15 checker validates exact R14 preservation, public operation parity, a single canonical operation registry, canonical operation-ID identity, exact canonical result bytes and fingerprints, both Release branch extraction maps, complete invalidation-result fingerprinting, exact proof-map joins, lifecycle vocabulary closure across every declared active root, receipt-based combine and collision rules, removal of legacy fingerprint families, and an ordered reaper-owned ambiguity path for expired consumed or maybe-called invocation tips.

Twenty-two mutation probes create a parallel result registry, splice identifier namespaces, alter either Release branch map or fingerprint, erase exact joins, restore every legacy lifecycle form, remove or reassign the reaper ambiguity path, weaken live-worker scope, collide priorities or remove the current-tip compare-and-swap. They test exact meaning rather than field counts.

## Honest limits

No adapter, schema, database procedure, runtime connection, proof resolver, outbox worker or external call implements R15. Passing these checks is not independent review, database proof, production proof, product intelligence, usability or customer value.

**Scope:** local contract only

**External actions:** none authorised or performed
