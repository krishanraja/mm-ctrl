# G24 trusted canonical ingress contract R14

**State:** thirteenth local repair candidate for independent attack; no adapter, database or runtime implementation

**Supersedes:** the rejected R13 candidate at `6452461bd48fd451623b0fde3c9a36d0108ca47e`

## Purpose

R14 closes the remaining identity and precedence gaps found by both R13 attacks. The checked-in JSON is the complete effective contract, generated byte-for-byte from the frozen R13 machine document.

## Complete identity

Provider operation class now uses one field name throughout schemas, identities, unique keys, predicates and fingerprint preimages. Provider-target derivation equates every hashed field to one exact current capability member.

Canonical case authority has one fingerprint over its complete binding. Case identity can therefore reproduce rather than merely claim the source fingerprint.

## Exact consumption

Terminal Release consumption resolves one committed `use_release` success row, result schema, result reference, result fingerprint and exact branch. Pending delivery maps only to its Release-use receipt; invalidation maps only to its invalidation receipt.

Answer and lifecycle consumption joins bind fingerprints as well as references. Joint leader and operator actions are separate pairs and must be distinct; single-human transitions bind the role-appropriate action pair.

## One lifecycle action family

The remaining R8 stable-action projection is replaced by the same nullable predecessor identity used by the authoritative R14 action. `open_preparation` permits null only with proven lifecycle absence; later transitions require the exact current predecessor. Evaluator version is equal to the current registry member and operation result.

## Exact kernel and deterministic delivery

Retirement references use the locked kernel's actual rule: unique exact strings that are nonblank after trimming and equal their trimmed values. They do not inherit stricter identifier grammar.

Final provider rechecks are an ordered first-match table evaluated from one serializable snapshot. Terminal state, superseded tip, lost fence, prior capability consumption, lease expiry, other failed rechecks and the one allowed call each have one precedence. Append actions require a current-tip compare-and-swap against that same snapshot.

## Boundary

R14 remains unimplemented local trust infrastructure. It authorises no UI, database change, customer data, research, model or provider call, deployment, merge or release. Independent technical review remains blocking.
