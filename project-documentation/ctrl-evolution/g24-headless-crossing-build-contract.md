# G24 headless Crossing build contract

**State:** authorised local implementation

**Founder authority:** On 12 September 2026 Krish answered "yes to both": first, lock the exact G24 R1 through R5 architecture chain; second, proceed with the bounded local headless Crossing and adversarial fixtures.

## Frozen source

- Founder architecture lock: `997ab55`
- Architecture source commit: `9fdca0a`
- Immutable retrieval tag: `ctrl-g24-architecture-approved-r5`
- Normative interpretation: R5, then R4, then unaffected R3, R2 and R1 rules

## Target

Build one deterministic, non-networked kernel that proves the difficult backstage mechanics before material interface design:

1. resolve a complete transitive set of controlling references;
2. return exactly one of `reuse`, `enrich`, `ask`, `session` or `abstain_hold`;
3. fail closed on missing, unknown, mismatched, future-dated, expired, invalid or indeterminate controlling state;
4. distinguish invalid control from valid unresolved evidence;
5. bind one versioned human-facing intervention atom to the selector result;
6. preserve honest exits, immutable answer evidence, rebuildable case effects and pending human-owned proposals;
7. preserve the exact six-state, thirteen-transition engagement lifecycle separately from Release;
8. bind selector-influenced pending Release projections to the exact selector result and its complete controlling watermark closure;
9. invalidate before use when any recorded controlling dependency changes, while ignoring unrelated lineage;
10. prove that rebuild alone does not restore Release eligibility.

## Allowed delta

- Pure TypeScript domain code under `src/features/operator-brain/`
- Deterministic synthetic fixtures and Vitest tests under the same feature
- One focused local check command in `package.json`
- This contract, a truthful QA record and state-route updates
- Local commits only

## Preserved invariants

- One canonical Brain; this kernel is not a second memory authority.
- Models may propose but cannot award standing, mutate the Brain, widen audience, deliver, schedule or Release.
- The named human owns purpose, standards, exceptions, final call, final quality and Release.
- Commercial or engagement state never grants permission or Release.
- Quiet and abstention are valid outcomes.
- Policy identifiers, lifecycle machinery and receipts stay backstage.
- No customer-facing efficacy or decision-quality claim is earned by deterministic fixtures.

## Closed in this phase

- Customer UI or navigation
- Customer accounts, personal data or consented capture
- External research, provider calls or model spend
- Supabase branches, migrations, schemas or production writes
- Email, connectors, notifications, scheduling or session capture
- Deployment, merge, release, feature enablement or legacy-backend deletion

## Predeclared verification

- Focused Vitest checks for the new headless kernel and fixtures
- Independent mutation of every controlling dependency in the frozen selector lineage
- Missing, unknown, mismatched, future-dated, expired, invalid and indeterminate attacks
- Quiet, sparse, valid-conflict, duplicate, stale, unrelated-lineage and rebuild-without-authority cases
- All thirteen permitted lifecycle transitions and rejection of implied edges
- Adjacent G21 operator-brain regression tests
- G24 architecture and founder-lock checkers
- Documentation checks, TypeScript check and changed-file lint
- Repeat run to establish deterministic result order and bytes where represented
- Diff, credential-shape and prohibited-character inspection before commit

## Rollback

The implementation is additive and unreferenced by product runtime. Rollback is the exact local implementation commit made after `997ab55`; no database, deployment or external residue is created.
