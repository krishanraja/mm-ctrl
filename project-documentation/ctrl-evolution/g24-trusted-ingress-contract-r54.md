# G24 trusted canonical ingress R54

**Status:** fully materialized local architecture repair; independent attack remains blocking

**Authority:** the exact generated R54 JSON contract

## What R54 repairs

R54 replaces generic identity fixtures with schema-valid linked fixtures evaluated through each persisted shape's own native derivation authority. Of the 1,229 R53 candidates, 1,222 are genuine identities and seven are honestly excluded because no native identity formula exists. The applicable set proves 276 wrapper content-address equalities, five row-or-nonce content addresses, six non-self-referential row versions and 33 companion fingerprints resolved from actual target fixtures.

Row versions now use their exact committed-target authority and exclude the derived version field. Companion fingerprints are recomputed from the selected target's schema-valid bytes and fingerprint rule rather than accepted as duplicated scalar evidence.

R54 also rebuilds selector contexts as the intersection of source-schema constants and the operation, branch, proof-family, evidence and store tables. Three hundred and seventy-four selectors cover 2,516 valid contexts. It removes all 49 invalid R53 contexts, including the 48 verified-session evidence vocabulary mismatches and the proof-nonce family mismatch.

The final semantic closure contains 53,039 reference occurrences sealed across 236 manifest rows. R53 persistence, codec, proof signature, concrete target and restart evidence remains exact.

## Boundary

R54 is invisible infrastructure. It changes no customer-facing language, interaction or product promise. It opens no adapter, database object, runtime connection, UI, deployment or external action.
