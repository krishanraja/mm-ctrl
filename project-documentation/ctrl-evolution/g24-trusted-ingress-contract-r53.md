# G24 trusted canonical ingress R53

**Status:** fully materialized local architecture repair; independent attack remains blocking

**Authority:** the exact generated R53 JSON contract

## What R53 repairs

R53 replaces persistence inferred from candidate-shaped markers with an independently declared registry of 217 durable schema shapes. It includes the 56 previously omitted direct append-only or uniquely keyed rows, the proof-nonce receipt store, all durable container variants, canonical artifact families, opaque inputs and committed authority rows. Every included shape resolves to a closed schema and a named sole writer, while nested row-union aliases are explicitly excluded as duplicate views of the same persisted authority.

Persisted identity is derived from native schema roles rather than field-name suffixes. Its 1,229 executable identities distinguish canonical row bytes, unique key components, row versions, bindings, standings, fingerprints and exact companion equalities. Fingerprint fixtures use the complete selected schema, declared domain, exact preimage order and field types.

R53 also makes internal reference selection source-driven. Three hundred and seventy-four selectors cover 2,513 valid operation, branch, proof-family, evidence, store and schema contexts. Each context resolves to exactly one concrete schema, variant, version and native identity formula. The 782 typed equality rows use the same authority.

The semantic scanner is independently pinned to 936 schema declarations and control fields, including `owner_lineage_version_source`, `selected_result_schema_version`, `canonical_encoding`, `then` and `source`. It closes 43,017 final-state occurrences into a 234-row self-sealed manifest.

## Boundary

R53 is invisible infrastructure. It changes no customer-facing language, interaction or product promise. It opens no adapter, database object, runtime connection, UI, deployment or external action.
