# G24 trusted canonical ingress R17 QA record

**State:** fully materialized local contract candidate; deterministic and mutation checks pending independent attack

## Verification scope

The R17 checker validates frozen R16 preservation and the unchanged 20-operation public ABI. It then checks the single canonical-JSON byte authority; result operation/export/selected-schema/branch binding; universal content fingerprint; versioned canonical response blob; exact response-to-result-to-success equalities; atomic three-record commit; sole case-authority writer, privileges, idempotency and compare-and-swap rotation; separate worker and lease ambiguity schemas; reason, recorder and transition-actor inseparability; the closed non-lease failure set; exact abort evidence; Release joins; and strict whole-document lifecycle vocabulary closure.

Thirty-three mutation probes deliberately corrupt each of those authorities. The lifecycle probe also confirms that `_reference` cannot evade the canonical `_ref` boundary rule.

## Honest limits

No adapter, schema migration, database procedure, runtime connection, proof resolver, outbox worker or external call implements R17. Passing these checks is not independent review, database proof, production proof, product intelligence, usability or customer value.

**Scope:** local contract only

**External actions:** none authorised or performed
