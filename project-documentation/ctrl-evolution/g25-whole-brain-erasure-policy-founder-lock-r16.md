# G25 whole-Brain erasure policy founder lock, R16

Decision: `DEC-20260917-g25-erasure-policy-r16`

Founder response: “yes” to the exact bundled policy question recorded in the [machine decision record](g25-whole-brain-erasure-policy-founder-lock-r16.json).

## Locked now

- Revoke CTRL access and active credentials before destructive erasure work.
- Keep any request with an unproved mandatory target in `resumable_pending`.
- Never turn partial completion into success plus an error list.
- Name each known customer-controlled destination and give one specific removal action.
- Never imply that CTRL can recall a copy controlled by the customer or another tool.

## Still unbound

Deletion-receipt retention remains a legal-policy gate. No duration, lawful basis or indefinite pseudonymous tombstone is approved by this decision. Runtime execution stays held until that policy is documented and accepted.

## Why the status is conditional

The approved interaction and completion behavior is active. The broader erasure runtime is conditional because its retention policy, live schema discovery, provider receipts, backup behavior and authenticated execution remain unproved.

## Durable-memory boundary

The established CTRL repository decision chain is the project authority for this lock. The generic cross-venture Supabase decision ledger is `STORE_UNAVAILABLE`: the current OS authority does not verify its live status, migration, grants, server-side adapter or authoritative readback. No ad hoc database row was created.

This lock authorizes the next local architecture and proof gate. It does not authorize a migration, database write, provider request, deployment, merge, release or customer-facing erasure claim.
