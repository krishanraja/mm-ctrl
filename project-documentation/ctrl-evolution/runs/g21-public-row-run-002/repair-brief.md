# G21 public-row run 002 repair brief

Status: frozen targeted repair brief

## Evidence and authority

- Input SHA-256: `7740f054205cc6284fadadf406d4735e98dfae04ba29e4bef4201af709e5242b`.
- Prior output SHA-256: `70497310ec7a27865115f0ac5eba79a092ff1695c04aa04757dfc807bb4d461f`.
- Run 001 was blocked by `COMPREHENSION-G21-01` after six passes and one valid hard veto.
- The standards prosecutor confirmed the block.
- Responsibility remains R1 Explore. No consequential recommendation is permitted.

## Failure to repair

The WPP and Microsoft top-layer frames and questions require a non-technical leader to decode specialist language before answering. The Microsoft question also measures returns without forcing the actual scarce-capacity priority decision.

The problem is not lack of analytical depth. It is that analysis and the immediate human interaction occupy the same layer.

## Protected strengths

- Keep all existing typed notices, evidence identifiers, unknowns and human boundaries unchanged.
- Keep cold and sparse abstention unchanged.
- Keep one question per profile.
- Preserve the WPP tension between integration as a growth engine and integration as cost reduction.
- Preserve the Microsoft tension between demand, lasting return, limited capacity and security.

## Exact repair scope

Create a new run 002 output from run 001. Change the top-level run and schema identifiers to run 002 and schema v2. Change only these six fields:

1. WPP `decisionFrame`.
2. WPP `routeChangingQuestion`.
3. WPP `answerWouldChange`.
4. Microsoft `decisionFrame`.
5. Microsoft `routeChangingQuestion`.
6. Microsoft `answerWouldChange`.

Use concrete language a bright 12-year-old can paraphrase without definitions. Prefer revenue, profit, customers, safety and limited computing capacity over analyst terminology. The questions must remain genuinely consequential, not simplistic.

The WPP question should reveal how much recent business the joined-up model made possible and how much profit it added.

The Microsoft question should reveal which customers or products receive limited AI computing capacity first and what proof of customer value, profit and safety earns that priority.

## Resolving test

The v2 deterministic contract must pass. Then the Human Comprehension judge must be able to paraphrase what is known, what matters and the one requested answer without defining specialist terms. All seven judges rerun against the new immutable artifact; a valid veto still blocks.
