# G21 internal range freeze 001 repair brief

Status: blocked artifact preserved; repair scope frozen for a new run

## Evidence and authority

- Artifact composite SHA-256: `95fbbcc467e07f31c852dc5ff70db9705af8643e87c44044c4def2e27f4b5156`.
- Seven-ruling composite SHA-256: `dcf23495a6503147f6102853a6f7cea1589d9c485d7a215de15f9246f46e28b1`.
- Human Agency and Consequential Usefulness passed.
- Epistemic Integrity, Subject, Audience and Lifecycle Safety, Human Comprehension and Access, Living Brain Integrity, and Behavioural and Implementation Reality each issued a valid hard veto.
- The Standards Prosecutor upheld all five vetoes and found one council-envelope defect plus adjacent validator false passes.
- Founder Calibration is additive only. It predicts the founder reaction but does not approve the repair.
- Responsibility remains R1 Explore. No consequential recommendation, production write, database change or deployment is authorised.

## Protected strengths

- Keep four matched fictional families, twelve profiles and thirty-six lifecycle inputs.
- Keep identity and external evidence fixed inside each family while internal evidence grows cumulatively.
- Keep the four consequential decisions, from about GBP 900,000 to GBP 18 million, and the exact human decision boundaries.
- Keep the fictional namespace, visible disclosure, `.invalid` external locators, `fixture://` internal locators and prohibition on simulated consent.
- Keep source records, valid and recorded time, explicit uncertainty, strongest countercases, evidence-linked notices and oracle-free model inputs.
- Keep the depth beneath the immediate question. Simplify the interaction, not the analysis.

## Exact repair scope

### 1. Frozen time boundary

Add one explicit evidence `asOf` value to every profile and blind input. Reject any internal, lifecycle or external timestamp later than that boundary. Test internal `validAt`, internal `recordedAt`, lifecycle times, external `publishedOn` and external `retrievedOn` separately.

### 2. Audience and subject authority

Runtime-enumerate `audience` and `subjectScope`. Add an evidence-specific audience-authority record that freezes the authorised audience independently of the evidence object. Reject leader-private to company-private widening without a matching authority change, reject public or unknown private audiences and reject unknown subject scopes. Prove the evidence ID to authorised-audience mapping remains exact in every emitted lifecycle input.

### 3. Claim-addressable correction

Source evidence remains immutable history. Decompose the claims inside evidence records and let a correction supersede exact claim IDs, never an entire multi-claim source. Migrate all four longitudinal corrections. The current-view resolver must retire only the changed claim, retain every unaffected claim with provenance and keep the original source record inspectable. Reject legacy record-level-only supersession.

### 4. Exact lifecycle binding

Bind `initial` to no added lifecycle evidence, `contradicted` to exactly the conflict and `corrected` to exactly conflict then correction. Reject a correction substituted into the contradicted state. Require unique evidence IDs across base and lifecycle records, and reject self or forward contradiction links.

### 5. Plain immediate questions

Rewrite the four vetoed questions and review CARE longitudinal under the same rule. The Brain performs diagnosis, metric definition, comparison design and audience trade-off. The leader receives one ordinary-language choice, threshold or fact only they can supply. Preserve rationale, provenance and consequences beneath the question.

Freeze the expected answer form before review. Keep the four rejected strings as negative regression fixtures. Run two independent blind reading-level checks over all twelve questions and preserve their raw paraphrases. Do not represent those checks as a study with real children. A consented human comprehension test with readers aged 11 to 13 remains required before claiming that level of demonstrated accessibility.

### 6. Council-envelope alignment

Create one versioned frozen-ruling schema that matches the exact eleven-field JSON envelope used by this range council. Validate all seven raw ruling files through it. Adjudication must be generated from those exact bytes and preserve every verdict, veto failure and resolving test verbatim. Do not silently rewrite a judge's veto in the adjudication.

### 7. Adjacent validator coverage

Also reject the prosecutor's located false passes:

- unknown external source types;
- a direct-statement standing without a direct source;
- duplicate or missing manifest source locators;
- generic substituted lifecycle notices;
- evidence ID collisions across base and lifecycle records; and
- self or forward contradiction links.

Add one adversarial regression for every path in both the direct gate and the Vitest suite.

## Re-freeze gate

The repaired candidate becomes `G21-INTERNAL-RANGE-FREEZE-002` with new artifact and ruling composites. Seven fresh isolated judges review it from scratch. The Standards Prosecutor then cross-examines the new rulings, followed by deterministic adjudication. A valid veto still blocks.

The restricted environment may prevent Vitest from collecting through the repository Vite config. If so, record that as an unresolved execution boundary, keep the direct TypeScript gate as separate evidence and do not describe an uncollected suite as passing.
