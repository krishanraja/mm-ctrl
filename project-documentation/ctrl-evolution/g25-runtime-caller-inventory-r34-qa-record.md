# G25 runtime caller inventory R34 QA record

Status: local source-tree absence proof. No runtime-integration claim.

## Evidence

- Browser source and Edge Function entrypoints contain zero references to the four active database functions.
- Browser source and Edge Function entrypoints contain zero references to the three retired runtime functions.
- Browser source and Edge Function entrypoints contain zero imports of the seven dormant prepared-intelligence modules.
- Committed migrations contain zero prepared-intelligence candidate functions or custody receipt tables.
- The seven dormant shared modules are individually hash-pinned.
- Existing SQL tests and local harnesses remain classified as verification, not application callers.

## Consequence

There is nothing to switch today. Runtime integration must be a deliberate new server-owned adapter, with browser-direct access remaining closed.

## Residuals

- External callers cannot be excluded by a source-tree scan.
- The future server endpoint, its authentication contract and encryption-key boundary are not designed here.
- Multi-connection races, Supabase parity and migration safety remain open.

No runtime adapter, migration, linked database use, deployment, merge, release or external action is authorised.
