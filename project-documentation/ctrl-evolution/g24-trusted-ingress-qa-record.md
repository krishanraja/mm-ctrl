# G24 trusted canonical ingress QA record

**State:** contract candidate only; deterministic document checks pass; independent attack pending

## Evidence

- The contract makes the command allowlist explicit and moves every material authority or standing field behind the server boundary.
- It requires one transactionally consistent snapshot, then a fresh atomic recheck before any consequential use.
- It separates same-process proof from durable restart rehydration.
- It makes enrichment-plan attempt allocation and terminal finality database-owned and concurrency-safe in the future implementation.
- It names twelve attack families and requires proof of rejection plus absence of side effects.
- The machine-readable contract and checker keep the authority boundary and core invariants from silently eroding.

## Honest limits

No adapter, database function, schema, branch, server capability, cryptographic proof, customer data, runtime connection or production behaviour exists from this contract. Supabase-specific implementation details must be checked against current official documentation immediately before implementation. Independent reviewers may veto this candidate before any local code contract is opened.

