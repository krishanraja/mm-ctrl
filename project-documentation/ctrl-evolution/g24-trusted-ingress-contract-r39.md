# G24 trusted canonical ingress R39

**Status:** fully materialized repair candidate under independent review

**Authority:** the exact generated R39 JSON contract

## What R39 repairs

R39 makes replay selection recovery executable without inventing fields in replay payloads. The five durable selection facts remain authoritative in the original committed registry, held registry or hold row, and the replay classifier. Twenty discriminator-aware typed bindings resolve exact source and destination schema variants and actual declared properties. No pseudo-path or `selected_variant` convention remains.

Four restart fixtures exercise committed, ordinary-held, verified-session-held and raw-session-held outcomes. Each reconstructs the exact selection row, proof family, branch class, evidence kind and selection schema version from fingerprint-bound durable state. Field splicing across otherwise valid records fails closed.

The authority manifest now seals its own envelope, row schema, enforcement constants, negative-fixture families and hash contract using independently pinned expectations in the checker and materializer. All content, dependency, graph and envelope hashes have closed domain-separated preimages and use Unicode-code-point-sorted canonical JSON encoded as UTF-8.

An explicit dependency-owner map replaces lexical dependency inference. Every cross-authority semantic reference resolves to exactly one manifested owner, including operation-authority workload and delegated aliases. Unresolved, multiply owned, empty-but-referenced and caller-selected authority references fail the focused gate.

## Boundary

R39 is invisible infrastructure. It adds no customer ceremony, administration, UI or intelligence claim. No adapter, database object, runtime connection, deployment or external action is authorised.
