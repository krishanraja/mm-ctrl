# G14 Decision Bench React R3 founder gate

Date: 8 September 2026

Status: direction approved; routine identity correction implemented locally; production navigation deferred

## Reviewed baseline

- React revision: `5409e4ea769953ff8f6906f615e9d83f33bef473`
- remote deployment: `dpl_9ywamPrcur1VZZoJPm2x9XjNit47`
- reviewed surface: synthetic Decision Bench at the direct cross-device preview route
- baseline source blobs:
  - `DecisionBenchPage.tsx`: `3acc5b864b3235a0dfa648ad6a6d8fee760f642b`
  - `DecisionBenchPage.css`: `128e920e1d7366744e5586f7c721ec7177dffcc3`
  - browser checker: `75939b77ac08dfb8b9f93250cebc92e91a0fa068`

## Unanchored founder reaction

> "ok looks good but it uses the old mindmaker wordmark, i just want the icon in here not the full wordmark. the sidebar buttons still do nothing but presuming that is becuase this is just a demo. shall we move on?"

## Gate decision

The corrected React Decision Bench direction is approved and the build should move on. The approval covers the product hierarchy, no-scroll operator instrument, current-versus-history comparison, evidence, focused Brain route, next-session preparation, customer projection and responsive model already reviewed.

Two qualifications carry forward:

1. The top-left identity is the Mindmaker icon only. The old full wordmark must not return to this shell.
2. The rail in the synthetic slice is a demonstrator selector. Its current active-panel emphasis is not accepted as finished navigation. Real navigation must earn its destinations from the operator workflow, visibly change context and be verified against those tasks before authenticated integration or release.

## Locked revision delta

Against baseline `06da30e3d0708e9df1e205699d951f70a06999a1`:

- replace `/mindmaker-full-logo.png` with `/mindmaker-favicon.png` in the Decision Bench brand control;
- remove the separate panel-open glyph so the icon is the only visual identity in that slot;
- reduce the brand column and icon dimensions to the compact shell footprint on desktop and mobile;
- update the browser assertion from full-wordmark presence to icon-only presence;
- preserve every other approved surface, fixture, state, interaction and privacy boundary.

## Authority and next gate

This founder gate closes the synthetic React surface review. It authorises the routine icon correction and progression to the real Brain substrate design. It does not approve the existing rail as production navigation and does not authorise schema application, real customer data, authenticated access, production deployment or release.

The next bounded action is the workspace-and-audience canary: turn the approved G13 physical model into a migration candidate and deterministic negative-access test pack, then bring the exact schema mutation to its separate approval gate before applying it anywhere.
