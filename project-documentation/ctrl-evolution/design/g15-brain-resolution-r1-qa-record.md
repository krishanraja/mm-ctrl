# G15 Brain Resolution R1 QA record

**Date:** 8 September 2026  
**Verdict:** Verified as a local synthetic product proof  
**Repository revision tested:** `25ba1f8`  
**Branch:** `codex/trust-containment-2026-09-05`  
**Local target:** `http://127.0.0.1:4189/g15-brain-resolution-proof-r1.html`  
**Production authority:** None

## Intended outcome

A leader can understand one meaningful way their Brain changed over time without encountering a score, completeness judgement, activity reward or technical evaluation console. They can inspect the correction and canonical evidence, see the exact limit, then make one low-load choice between two proposed handoff briefs. The choice remains local and cannot advance the Brain.

## Target identity and test data

- The page consumes `g15-judgement-resolution-fixture.json`.
- The fixture is explicitly synthetic and names Maya Chen of Aperture House.
- Runtime validation fails closed unless Delegation is revising and unscored, has no holdout or transfer references, Creative direction is transferable, Editorial voice is tested, Life outside work is intentionally out of scope, and the strong handcrafted baseline remains unrun.
- No production account, customer record, remote write, generated inference, send, deployment or durable mutation was used.

## Independent concept gate

- Three structurally different concepts were frozen before judging.
- Both independent judges selected The Becoming Receipt at 93 out of 100.
- One judge found that every generator had substituted candidate-specific evidence for the canonical fixture.
- The corrected synthesis removed all unsupported historical claims.
- A fresh-context rejudge initially returned `NOT READY` because audience metadata was unavailable and the proposed interaction selected a test format rather than presenting a real contrast.
- After both corrections, the same rejudge returned `READY` and found every hard failure cleared.

## Browser tasks exercised

The automated browser performed the complete path at 390 by 844 and 1440 by 900:

1. Open the current receipt from a clean state.
2. Read the changed interpretation and held-out limit.
3. Reveal Before and Now, including preservation of the replaced interpretation.
4. Reveal the exact `SRC-101`, `SRC-105` and `COR-02` evidence.
5. Confirm that no delegation holdout is implied.
6. Open the proposed next contrast.
7. Choose Brief A from two plausible synthetic handoff briefs plus `Neither is ready`.
8. Verify that the result says nothing changes until real work tests it.
9. Open correction, add a synthetic note and verify that it remains a local preview.
10. Exercise sparse, quiet, loading, stale, recoverable error and intentionally private states.
11. Reload and verify that the staged contrast did not persist.
12. Verify visible keyboard focus, reduced-motion equivalence and mobile touch targets.

## Observable results

`npm run brain:g15:check` passed the provisional construct and canonical fixture invariants.

`npm run brain:g15:r1-check` passed every fixture, mobile and desktop assertion:

- canonical fixture validation;
- exact corrected opening;
- visible held-out limit;
- no visible percentage or completion language;
- one opening primary action;
- no horizontal overflow;
- prior meaning preserved;
- exact canonical evidence;
- unavailable delegation evidence kept unavailable;
- genuine prospective contrast with refusal;
- local-only contrast and correction behaviour;
- six honest adverse states;
- non-persistence after reload;
- visible keyboard focus;
- mobile controls at least 42 pixels in both dimensions;
- reduced-motion behaviour; and
- no console or page errors.

The repository standards gate now passes after punctuation-only removal of pre-existing em dashes from five CTRL evolution documents. `npm run build` completed the production SPA build. The optional prerender fallback skipped because Windows reported the existing public background video as locked during a copy into `.prerender`; the build script handled that path as designed and shipped the SPA output. This proof does not depend on prerendering.

## Rendered inspection

Evidence images:

- `evidence/g15-r1-mobile-receipt.png`
- `evidence/g15-r1-mobile-evidence.png`
- `evidence/g15-r1-desktop-receipt.png`
- `evidence/g15-r1-desktop-evidence.png`

Visual inspection found:

- the current changed interpretation owns the mobile first frame;
- the paper receipt reads as one physical, editorial object rather than a dashboard;
- the visible state and limit remain adjacent to the claim;
- the mobile path becomes one natural column with practical tap targets;
- the desktop context supports the receipt without competing with it;
- no generic neural mesh, orbit map, gradient, glass treatment, sparkle symbol or mascot remains;
- purposeful line icons identify receipt, change, evidence, test and limit;
- history remains a quiet secondary route with no receipt count; and
- the selected contrast is visibly provisional and does not produce a result.

One visual correction was made after the first screenshot. An orbital territory graphic in the desktop context looked like a generic AI visualisation and weakened the singular receipt spine. It was replaced by a direct correction figure: `Less certainty. Better fit.` The full browser suite passed again after the change.

## Voice and trust check

- The copy uses plain British English and no em dashes.
- It avoids reassurance guff, motivational scoring, generic AI phrasing and stacked two-word authority fragments.
- Historical statements and quotations match the canonical fixture.
- Proposed handoff briefs are clearly future-facing synthetic work, not claims about Maya.
- `Your Brain does not get fuller. It comes into focus.` remains labelled candidate product language, not a validated promise.

## Not verified

- No cold human has yet confirmed comprehension, personal value or return desire.
- No real user data, model inference, held-out evaluation, persistence, correction cascade or Git export was exercised.
- No efficacy, moat, improved judgement or transfer claim has been established.
- No production implementation, account, database, deployment or release was changed.
- The G14 Private Brain Builder R1 founder reaction remains open.

## Next gate

Show the proof cold to Krish. Ask for the unanchored first reaction before explaining the metric theory. Specifically learn whether the receipt feels personally meaningful, whether `Brain Resolution` is a useful product idea, whether the experience creates honest return desire, and whether the screen feels like progress or merely a polished history item.

