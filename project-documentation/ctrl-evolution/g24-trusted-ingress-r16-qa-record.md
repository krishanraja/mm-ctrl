# G24 trusted canonical ingress R16 QA record

**State:** fully materialized local contract candidate; deterministic and mutation checks pending independent attack

## Verification scope

The R16 checker validates exact R15 preservation, public operation parity, the closed canonical result-blob row, bytes length, content hash, canonical schema parsing, blob fingerprint, complete committed-success fingerprint, atomic blob-to-success resolution, exact Release proof joins, scoped and uniquely current case authority, evaluator version and artifact identity, whole-document lifecycle vocabulary closure, exact ordered provider outcomes, selected-row transition authority and the single fail-safe route for an expired current invocation.

Thirty mutation probes open or detach the byte store, erase length, hash or schema validation, weaken success fingerprints and conditions, splice blob joins, un-scope case authority, detach current selection, unbind evaluator artifact, insert legacy lifecycle vocabulary into an omitted proof node, restore stale two-party language, reroute expiry, alter exact outcome actions or actors, broaden transition guards, restore reaper no-call authority, detach transition selection or remove compare-and-swap.

## Honest limits

No adapter, schema, database procedure, runtime connection, proof resolver, outbox worker or external call implements R16. Passing these checks is not independent review, database proof, production proof, product intelligence, usability or customer value.

**Scope:** local contract only

**External actions:** none authorised or performed
