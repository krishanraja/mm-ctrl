# G24 trusted canonical ingress contract R19

**State:** eighteenth local repair candidate for independent attack; no adapter, database or runtime implementation

R19 makes the authoritative terminal-consumption row the sole Release receipt and binds its reference and fingerprint into both result branches and the atomic `use_release` transaction. Replay is an exact historical projection of immutable success and result bytes; only replay time and fixed replay flags are new.

Single-action lifecycle `actor_role` now belongs to both fingerprints and equals the consumed action receipt role. Case control uses one subject-scoped key, fingerprints the authenticated actor, evaluates original-actor replay before fresh authority, and returns one closed committed, replayed or hold result. Declared paths are exactly `alias.field`; duplicate pairs, extended paths and lifecycle reference lookalikes fail.

R19 changes no visible product behavior and authorises no UI, database change, customer data, research, model/provider call, deployment, merge or release. Independent review remains blocking.
