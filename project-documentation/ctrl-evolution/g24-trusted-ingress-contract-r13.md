# G24 trusted canonical ingress contract R13

**State:** twelfth local repair candidate for independent attack; no adapter, database or runtime implementation

**Supersedes:** the rejected R12 candidate at `7749ce273239cb1c162310b597c899c56d0016f0`

## Purpose

R13 preserves R12's verified gains and repairs the executable paths that its two independent attacks proved were still unreachable, reusable or internally inconsistent. The checked-in JSON is the complete effective contract, generated byte-for-byte from the frozen R12 machine document.

## One reachable operation boundary

The public request discriminator, operation registry, result schemas and evaluator exports now expose the same twenty operations. Release compilation no longer asks for a future human acceptance. It atomically creates a pre-authority projection request and its pending projection from an exact selector result and current source snapshot.

Only after that projection exists can the named leader accept it. Acceptance and Release authority are unique for the exact projection. Either final outcome, pending delivery or invalidated before use, consumes that authority once in the same serializable transaction.

## Exact human identity and single-use answer authority

Case identity is a server-computed projection of the canonical case authority binding and current identity watermark, not a caller-authored source of truth. Answer authority binds the exact approved question, approval version, visible surface, visibility acknowledgement, named leader, authenticated principal and current identity versions.

Recording the answer atomically consumes that authority once. A second operation identifier cannot reuse the same visible question or authority to create another answer.

## Executable lifecycle genesis and evidence

`open_preparation` is now representable: its predecessor is null exactly when no current lifecycle row exists. Every later transition requires the exact current predecessor and catalogue `from_state`.

One authoritative lifecycle receipt family replaces the competing legacy names. A closed evaluator operation reads the exact catalogue row, case identity, predecessor or proven genesis absence, evidence-input set and current evaluator artifact. It writes evidence only on a satisfied closed branch. Every action-to-authority and authority-to-transition consumption has explicit owner joins and field equalities.

## Kernel-exact questions

The question contract now describes only what the frozen kernel enforces. Required visible strings may preserve their exact original bytes but must be nonblank after trimming. Options and non-null proposals must equal their trimmed nonempty values. Only ranked choice is capped at five. No invented control-character prohibition is claimed.

## Reconstructible provider target and total no-call handling

Provider target identity has one exact preimage derived from the selected current capability member. Origin, effect, reservation and transition vocabulary use provider operation class consistently. The stale genesis equality and embedded transition-event version are repaired.

The final provider-boundary recheck has a total outcome table. The current worker can append a no-call abort for a current-tip, unconsumed-capability failure; the reaper can do so only for the matching expired-lease case. A superseded tip causes no append and no call. A consumed capability becomes ambiguity rather than a false no-call claim.

## Boundary

R13 is unimplemented local trust infrastructure. It proves no production isolation, database concurrency, provider behaviour, product intelligence, experience quality or customer value. It authorises no UI, database change, customer data, external research, model or provider call, deployment, merge or release. Independent technical review remains blocking.
