# G24 trusted canonical ingress R52

**Status:** fully materialized local architecture repair; independent attack remains blocking

**Authority:** the exact generated R52 JSON contract

## What R52 repairs

R52 makes every declared fingerprint codec truthful. The 205 active fingerprint authorities that previously named field encoding while using canonical JSON now explicitly name the contract's canonical JSON UTF-8 encoder. Ten other fingerprint controls follow the same exact authority. The four proof-signature authorities remain on the separate canonical field encoder, and both Ed25519 signatures continue to pass only over those exact field bytes.

The persisted-store inventory is now derived in both directions from the durable store authority graph. Its 160 store shapes include the operation result, response and hold blob stores, live and presented principal stores, partition heads, both replay lookup variants, canonical content stores, opaque stores, nonce persistence, registries, receipts and committed target rows. Every discovered durable store is in the inventory, and every inventory row resolves to a durable store authority.

R52 replaces meta-container and placeholder target candidates with 374 finite concrete selectors. Each selected internal reference resolves to an exact schema path, variant, version and identity formula. Fifty-four stored wrappers remain in traversal, including `stored_row_value`; wrapper schema references are verified as schema references rather than artifact identities.

The semantic scanner now checks both the full tokenized reference vocabulary and every pinned non-suffix semantic field. This includes control words such as `then` and `source`. The owner graph and manifest are regenerated from that final reference set.

## Boundary

R52 is invisible infrastructure. It changes no customer-facing language, interaction or product promise. It opens no adapter, database object, runtime connection, UI, deployment or external action.
