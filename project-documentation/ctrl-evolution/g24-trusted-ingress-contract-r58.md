# G24 trusted canonical ingress R58

**Status:** fully materialized local architecture repair; independent attack remains blocking

**Authority:** the exact generated R58 JSON contract

## What R58 repairs

R58 stops treating detached values as proof of a relationship between real artifacts. The conditional-rule registry retains all 103 exact rules, but claims only what the frozen fixtures prove. Twenty-five closed local predicates execute against every selected fixture that uses them. Zero detached context bundles are counted as proof. Seventy-eight cross-artifact, prior-row, transaction, database, session, worker, provider or cryptographic rules remain explicitly unproved, and every affected identity is excluded from full conditional-semantic completeness.

The recursive fixture interpreter now gives optional fields their actual meaning: every required field must be present, present fields must belong to the closed keyset, and optional fields may be absent. It rejects unsupported schema types, wrong generic JSON types, lone Unicode surrogates where valid scalar values are required, noncanonical base64url and impossible Gregorian timestamps. Its frozen-parent type inventory distinguishes the recursive schema vocabulary from control and container labels that are not claimed as fixture types.

Terminal-consumption fixtures omit `valid_until` at creation and require `valid_from` to equal `consumed_at`. Intervention atoms decode their payload, bind inner and outer content, recompute the question contract fingerprint from the declared question authority, and recompute the atom fingerprint from its declared semantic authority. Root bootstrap signer references remain distinct.

R58 preserves the canonical shared equality evidence from R57. Seventy linked identities dereference 38 complete source-target fixtures, including the receipt-precommit identities `identity_0929`, `identity_0958` and `identity_0973`, without per-authority overwrite.

The focused checker independently validates the candidate and rejects 254 attacks. It covers 1,228 native persisted identities, 374 selectors over 2,516 source-schema-valid contexts, 57,901 semantic-reference occurrences and 242 manifest rows.

## Honest semantic boundary

Schema validity and closed local conditional validity are proved. Cross-row or live enforcement is not converted into a documentation pass. The 78 unproved rules stay visible in the machine contract with exact schema sites and affected identity IDs so later bounded fixtures or runtime gates can close them without losing chronology.

## Product boundary

R58 is invisible infrastructure for the one canonical Brain and leader-owned authority. It changes no customer-facing language, interaction, approval step or product promise. It opens no adapter, database object, runtime connection, UI, deployment or external action.
