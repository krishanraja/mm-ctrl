# G24 trusted canonical ingress contract R9

**State:** eighth local repair candidate for independent attack; no adapter, database or runtime implementation

**Supersedes:** the rejected R8 candidate at `87447e58c956bfb0e1ca989bb2f9aa98bbf40426`

## Purpose

R9 preserves the product and authority model while closing the four implementation roots found in R8. The checked-in JSON is the complete effective contract. Its generator reads the exact frozen R8 bytes, applies every replacement, rejects undefined values and emits one byte-stable document.

R1 through R8 remain immutable failure evidence. This repair changes no historical verdict and opens no runtime work.

## Boundary

R9 remains invisible trust infrastructure for canonical ingress into the verified local headless kernel. It does not implement or prove product intelligence, comprehension, delight, decision quality, the final call, Brain learning, export or the continuing relationship. It authorises no UI, database object, customer data, external research, model call, provider call, deployment, merge, release or old-backend deletion.

## A change in lineage or version invalidates the release

Every base and applicable-control watermark now includes its kind, lineage and version. The identity projection includes all four dimensions, so a version-only or lineage-only change cannot reuse an old complete-set fingerprint. The release result and invalidation receipt continue to use the same structural union.

## Evaluator exports are data validated by closed schemas

The operation and proof export maps remain live data. Separate closed schemas specify their exact keys and constant values. Each of the seventeen operation exports equals both the result payload schema and the operation specification. Each of the eight proof exports equals its concrete proof-extension schema.

## A retry is the same human attestation, not a new claim about attention

The visibility retry identity is a closed projection of case, stable actor, presentation challenge, challenge fingerprint, exact foreground attestation and nonce. The server recomputes its fingerprint. It checks for an existing acknowledgement before applying the fresh no-prior-ack predicate.

An exact retry commits a new operation-registry success with the existing result bytes and no new receipt. A conflicting projection commits a named hold. The receipt binds the secondary-idempotency fingerprint. This proves only that the authenticated operator made the exact acknowledgement against the exact challenge. It does not claim the system measured attention, comprehension or agreement.

## Proofs reconstruct meaning, not locator shells

Nineteen append-only row schemas now contain the material needed to recompute their semantic claim: selector candidates and policy text; intervention content; answer kind and value; authority and visibility evidence; correction chains and dependency edges; lifecycle state and preconditions; pending projection bytes and release authority; and enrichment plan, policy, state and execution evidence.

Every row has two distinct digests. The semantic fingerprint covers its content-bearing fields. The row-envelope fingerprint covers scope, version and valid time as well as the semantic fields. Neither can stand in for the other.

Selector candidates, answer-chain members, dependency edges and lifecycle preconditions have typed closed member schemas and their own fingerprints. Candidate sets contain all four routes. Answer chains are ordered by an actual append ordinal. Dependencies name both ends and their versions. Preconditions name their evidence and evaluator.

Each proof family has one canonical owner. Every dependency value must equal the extension, the owner join and the dependency row within the same workspace, subject, case and snapshot. Every set seal must equal both the owner field and a recomputed complete typed set. Rows from different valid histories cannot be spliced into a valid proof.

## The outbox can distinguish “never called” from “may have called”

Effect creation atomically appends one immutable effect and one unique pending creation event. Every later transition names an exact event kind, an exact closed payload schema, a payload reference and recomputed payload fingerprint. It also binds the exact predecessor reference and fingerprint. A serializable compare-and-swap admits one successor only when the predecessor is the unique current tip and the transition matches the complete state table.

Dispatch does not call a provider. A separate invocation-start event must commit first. Only the winning process then receives a non-serializable, in-process, single-use capability bound to that exact invocation event. The provider entrypoint refuses calls without the unconsumed capability.

If a worker disappears before invocation-start commits, the system can append `abandoned_not_invoked`: the provider definitely was not called, so a later claim is safe. If it disappears after invocation-start commits, the system records ambiguity and never automatically calls again unless the exact provider idempotency guarantee permits a bounded retry. Reapers and reconcilers cannot send. Three committed reservations remain the absolute ceiling.

## Claim boundary

R9 is a fully materialized but unimplemented local contract. Its checks cannot prove Postgres isolation, permissions, private module boundaries, provider behavior, production behavior, intelligence, usability or customer value. Independent technical review remains blocking before any separately scoped adapter and adversarial-fixture implementation can open.
