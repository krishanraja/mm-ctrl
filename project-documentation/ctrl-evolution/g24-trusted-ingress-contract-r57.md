# G24 trusted canonical ingress R57

**Status:** fully materialized local architecture repair; independent attack remains blocking

**Authority:** the exact generated R57 JSON contract

## What R57 repairs

R57 makes the fixture schema interpreter closed and total for the schema vocabulary used by the frozen contract. Generic strings are type checked, canonical timestamps use an exact Gregorian parse and round trip, and unknown types or conditional rules fail closed. Intervention question atoms must carry the exact decoded question contract and byte length. Root bootstrap proofs require distinct signer references.

The 103 conditional rules now have one exact registry. Twenty-seven local predicates execute against every selected fixture that uses them. Sixty-eight deterministic cross-row or contract relations execute against materialized context bundles across 15 predicate families, each bound to the exact frozen schema sites and challenged by a negative mutation. Eight rules remain explicitly unproved because their truth depends on live cryptographic verification, serializable database enforcement, authenticated session or workload state, or live success-only row creation. Affected identities are excluded from full conditional-semantic completeness rather than counted as passes.

R57 also repairs split joint evidence. Each specialized linked identity dereferences one canonical shared complete-joint fixture. Role-specific companion fields use an explicit target-field mapping without overwriting the shared target identity, byte hash or fingerprint. The known receipt-precommit cases `identity_0929`, `identity_0958` and `identity_0973` and cross-authority splices are attacked directly.

The focused checker independently validates the candidate and rejects 247 attacks. It covers 1,228 native persisted identities, 70 linked identities, 38 complete equality fixtures, 374 selectors over 2,516 source-schema-valid contexts, 57,792 semantic-reference occurrences and 242 manifest rows.

## Boundary

R57 is invisible infrastructure. It changes no customer-facing language, interaction or product promise. It opens no adapter, database object, runtime connection, UI, deployment or external action.
