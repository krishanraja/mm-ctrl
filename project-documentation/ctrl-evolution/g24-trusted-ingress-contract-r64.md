# G24 trusted canonical ingress R64

**Status:** fully materialized local architecture repair; independent attack remains blocking

**Authority:** the exact generated R64 JSON contract

## What R64 repairs

R64 closes the evaluator export regression in frozen R63. The authoritative `evaluate_lifecycle_preconditions` result remains `ctrl.g24.result.evaluate-lifecycle-preconditions.r63.v1`, with the evaluate-stage proof-set seal absent. R64 now binds that same result version across the operation specification, evaluator ABI map, closed ABI schema constant, one selected current evaluator registry member, canonical evaluator manifest and loaded evaluator artifact.

The evaluator manifest contains the exact canonical bytes and SHA-256 for every exported result and proof schema. The loaded evaluator artifact binds the same export maps and exact manifest identity. Updating those immutable evaluator identities recomputes the selected evaluator member, evidence-row evaluator lineage, semantic and row-envelope fingerprints, row wrapper content address, lifecycle proof member, evaluate result and planned transition-stage owner-bound set seal.

The checker enumerates all twenty operations and proves seven-way export equality for each one. A zero, duplicate, stale, missing or cross-operation evaluator export holds before kernel execution. A second current evaluator cannot bypass the exact-one selection rule.

All twenty-one ordering fixtures now derive their source path, containing schema, schema version, ordered field and selected-spec hash from the current frozen occurrence. The two lifecycle fixtures bind the R63 result schema rather than stale R13 metadata. Their full canonical singleton payload and duplicate rejection remain active. The other nineteen full payloads and ordering negatives remain byte-identical to the previously verified fixtures.

The focused checker rejects 90 attacks. The final semantic snapshot contains 58,338 reference occurrences sealed across 247 manifest rows.

## Product boundary

R64 changes only invisible evaluator export and evidence lineage. R63's human-owned seal boundary, exact evidence semantics, catalogue cardinality and singleton honesty remain intact. R64 adds no customer ceremony, visible product surface, adapter, database object, runtime connection, deployment or external action.
