# G24 trusted canonical ingress contract R8

**State:** seventh local repair candidate for independent attack; no adapter, database or runtime implementation

**Supersedes:** the rejected R7 candidate at `a95f210be0d4de611e8862821c050907add88d00`

## Purpose

R8 preserves the R7 product model and closes only the six implementation roots found by both technical reviewers. The checked-in JSON is the complete effective contract. Its generator reads the exact frozen R7 bytes, applies every replacement, rejects undefined values and emits one byte-stable document.

R1 through R7 remain immutable failure evidence. This repair changes no historical verdict and opens no runtime work.

## Boundary

R8 remains invisible trust infrastructure for canonical ingress into the verified local headless kernel. It does not implement or prove product intelligence, comprehension, delight, decision quality, the final call, Brain learning, export or the continuing relationship. It authorises no UI, database object, customer data, external research, model call, provider call, deployment, merge, release or old-backend deletion.

## Control identifiers remain whole

An applicable-control watermark is now a structural object, not a prefixed string with a narrower grammar. A base member contains its fixed base kind. An applicable-control member contains the complete canonical `identifier` value. Unicode, `/` and the full legal byte length therefore behave identically in the control universe and the required watermark set. Canonical ordering remains deterministic.

## Evaluator parity is exact

All seventeen operations have exactly one evaluator result export, and every export equals the operation's concrete result schema version. The presentation-challenge operation is present and visibility acknowledgement points to R7 rather than the stale R6 result.

## Foreground review is an honest human attestation

The challenge issuer accepts only atom and version references. The server resolves the current case operator as viewer, recomputes the content fingerprint, creates a fresh nonce and derives expiry as exactly five minutes after server issue time.

Visibility acknowledgement requires the authenticated actor, challenge viewer and current engagement operator to be the same person. Case, atom, version, content fingerprint, challenge fingerprint and expiry must match. No prior acknowledgement may exist. The person submits one exact foreground attestation. This proves the operator attested to seeing that exact foregrounded version. It does not claim passive telemetry proves attention, comprehension or agreement.

A repeated operation with the same stable attestation commits a new registry success containing the exact existing receipt result, without a second receipt. A conflicting attestation commits a named hold. Approval, hold or suppress atomically appends a unique visibility-consumption record. Only approve confers approval.

## Lifecycle retries have stable identity

Lifecycle secondary idempotency now fingerprints only case, stable actor, actor class, authority version, transition, predecessor and nonce. It excludes server-generated action reference, issue time, expiry and record fingerprint. A matching logical retry under a new operation ID commits a new registry success with the exact existing result and no new authority row. A different projection for the same nonce commits `authority_nonce_conflict_hold`. Reusing the same joint pair behaves the same way; pairing either action elsewhere commits `authority_pair_conflict_hold`.

## Proofs resolve every authority dependency

Each proof family names one owner row, every dependent row and every sealed set. The map covers selector candidates and policy; intervention atom and visibility; answer leader authority and visibility; correction chain and dependency graph; lifecycle snapshot, authority and preconditions; release authority and complete watermarks; enrichment budget and terminal state; and execution terminal state.

Every authoritative row has a closed family-specific schema, a deterministic four-part partition, an exact valid-time selection rule, one linked non-circular fingerprint and a unique-current-or-hold rule. Every extension field equals a named owner column, dependent row column or recomputed complete set seal. Scope equality covers workspace, subject, case and snapshot.

## Fingerprints cannot float free

Presentation challenge, visibility acknowledgement, visibility consumption, lifecycle action, joint receipt, both lifecycle consumption records, lifecycle secondary idempotency, outbox reservation, dispatch, terminal evidence, state transition, claim, pre-provider failure and unknown events all link their fingerprint fields to exact fingerprint definitions. Domains are unique. Each preimage contains every schema field except its own fingerprint field, in schema order.

## One effect vocabulary

The operation branch maps are the sole authority for approval and release writes. Product semantics reference those maps but do not duplicate them. Release invalidation uses the same receipt name and forbidden-write set everywhere.

## Outbox state has one causal successor

The outbox is an append-only event graph. Every transition contains its exact payload plus one transition event in the same serializable transaction. The unique key `(outbox_effect_ref, causal_predecessor_event_ref)` permits only one successor from each committed state event. A losing concurrent outcome commits neither its payload nor another successor.

Claim, pre-provider failure and unknown now have closed payload schemas. Reservations and dispatches are linked to their fingerprint rules. Every provider outcome binds the exact dispatch, attempt, fence, worker, provider operation, idempotency key and payload fingerprint.

An ambiguous attempt returns to `claimed`, never directly to reservation. That claim requires a current idempotency guarantee, exact retry identity and fewer than three prior reservations. The reaper and reconciler never call the provider. A lease or worker change never resets the count.

## Claim boundary

R8 is a fully materialized but unimplemented local contract. Its checks cannot prove Postgres isolation, permissions, private module boundaries, provider behavior, production behavior, intelligence, usability or customer value. Exact independent technical review remains blocking before any separately scoped adapter and adversarial-fixture implementation can open.
