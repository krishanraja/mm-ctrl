# G24 trusted canonical ingress R6 QA record

**State:** fully materialized local contract candidate; deterministic and mutation checks pending independent attack

## Repair summary

- One deterministic generator now produces the complete effective R6 JSON. Runtime overlays and inherited interpretation are forbidden.
- Five authority-bearing actions join the eleven ingress operations and receive the same registry, authority, replay, snapshot, limit and result treatment.
- Edited interventions are staged, then visibly presented, then approved in a later operation. No operation can create its own proof of presentation and approval together.
- Lifecycle actions have exact fifteen-minute server TTLs, nonce uniqueness, single action-pair combination and atomic consumption.
- Replay has one meaning: a distinct historical envelope containing the exact original payload bytes, never a claim that the whole response is unchanged or currently authoritative.
- Release watermarks have twenty required base kinds plus deterministic kinds for every applicable control, a closed member union, one set fingerprint, one projection-version type and one idempotent invalidation receipt per projection version.
- R6 replaces caller-carried proof bytes with server-resolved canonical row references and fingerprints under exact proof lineage and bundle hashes.
- Every new authority-bearing fingerprint has a domain, complete ordered preimage and explicit self-field exclusion.
- Request-owned byte limits all reject before admission. Serialization exhaustion does not commit a hold.
- Outbox attempts now commit `reserved`, atomically transition once to `dispatched`, and permit exactly one call for each ordinal from one through three. A fourth reservation or call is impossible.

## Verification

The R6 checker requires exact generated bytes, preserves exact R5 failure evidence, validates the complete sixteen-operation and thirteen-set schema graph and attacks every R5 bypass. Its mutation set covers materialization, operation registration, staged visibility, lifecycle TTL and combination, registry conflict and replay, watermark types and order, invalidation uniqueness, set seals, snapshot and hold hashes, proof references and hashes, authority-record fingerprinting, limit phases, attempt reservation, dispatch finality, provider rechecks and three-attempt behavior.

## Honest limits

No adapter, schema, database procedure, runtime connection, proof resolver, outbox worker or external call implements R6. Passing these checks is not independent review, database proof, production proof, product intelligence, usability or customer value.
