# G24 trusted canonical ingress contract R11

**State:** tenth local repair candidate for independent attack; no adapter, database or runtime implementation

**Supersedes:** the rejected R10 candidate at `2785675b75c808e01eef9ab2d950510f355ca9b8`

## Purpose

R11 preserves R10's verified gains and replaces the five remaining descriptive invariants with closed derivation tables or exact equality sets. The checked-in JSON is the complete effective contract. Its generator reads the exact frozen R10 bytes and emits one byte-stable document.

## Boundary

This is invisible trust infrastructure. It does not implement or prove intelligence, comprehension, delight, decision quality, the final call, Brain learning, export or customer value. It authorises no UI, database object, customer data, external research, model call, provider call, deployment, merge, release or old-backend deletion.

## Canonical bytes are complete

Every new fingerprint value now has one injective representation. Finite nonnegative numbers use RFC 8785 JSON number serialization and reject negative zero and non-finite values. Null, nullable absence or value, optional absence or presence, base64url bytes and discriminated answer variants have distinct exact encodings. A discriminated value binds the discriminator name and value before the selected variant bytes. Unknown or unresolved types fail closed.

## The selector is a derivation, not advice

The candidate set is the exact zero-to-four routes actually presented. Zero candidates is valid and produces a hold when no route can resolve the gap. Every candidate stores both input rejection reasons and the exact generated reasons for its capability, deadline, budget, evidence, audience, provenance, counterevidence and reuse state. Eligibility is the sorted unique union being empty.

One ordered table reproduces the locked kernel: invalid input first; then no material effect or low consequence; then `reuse` only for sufficient evidence; otherwise the least-burden eligible `enrich`, `ask` or `session` route; otherwise an exact contradiction or unknowable hold. The owner stores the evidence state, consequence and projected alternatives. Candidate and controlling-watermark sets are recomputed under exact owner versions and sealed into the proof.

## An answer can only do what the approved question declared

The intervention atom stores the exact versioned canonical payload bytes and, for a question, its closed question contract. The approval receipt binds the same atom version, bytes, authority version and question-contract fingerprint. Leader authority binds that same atom and contract.

The answer kind and grammar mechanically select one effect key: an honest exit uses its own key, a single choice uses the chosen option and ranking, bounded text, voice or scoped write-in use `default`. The receipt's case effect, visible consequence, pending human proposal and retired interventions must equal that exact effect entry. Honest exits cannot create adverse effects. Missing, stale, ambiguous or mismatched authority holds.

## Lifecycle and Release authority cannot be spliced

Lifecycle transition, authority and precondition proof rows project one exact row from the thirteen-transition catalogue. Before state, after state, actor class, acting human, actor set, authority text, precondition, invalidation and receipt type are byte-equal to the catalogue and current identity controls. Actor-class rules determine whether authority is single or joint and which current people must be present.

Release authority now carries and equals the exact pending-projection fingerprint as well as its identity and version.

## Watermark changes have one meaning

Bound and current sides are each either wholly present or wholly absent. Partial-null, both-absent and unchanged entries are invalid. Removal and new applicability precede lineage and version comparison; lineage change wins when lineage and version both differ. Base controls must exist on both sides. Set members encode their variant name and variant-specific fingerprint, preventing different watermark kinds from sharing the same set bytes.

## Delivery crosses an external boundary only with current authority

Payload consumption requires field-by-field equality among the payload row, transition and unique consumption row. Reconciled success and failure transitions point to their distinct schemas. The three-reservation “never invoked” terminal is legal only when the total provider-invocation-event count for the effect is zero.

Immediately before a provider call, the process must still own the current fenced claim, an unexpired dispatch lease, the unique invocation tip with no successor and an unexpired, unconsumed capability bound to the exact effect, dispatch, worker and fence. Any reaper, ambiguity or terminal successor blocks the call.

Every outbox effect also has an immutable origin row written atomically with one exact successful operation result, its canonical payload bytes, the effect and the creation event. Hold, ineligible, invalidated, no-op and failed branches cannot create effects.

## Claim boundary

R11 is a fully materialized but unimplemented local contract. Passing local checks cannot prove database isolation, provider behavior, production safety, experience quality or customer value. Independent technical review remains blocking before any bounded local adapter and fixture implementation can open.
