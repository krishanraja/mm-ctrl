# G24 trusted canonical ingress contract R10

**State:** ninth local repair candidate for independent attack; no adapter, database or runtime implementation

**Supersedes:** the rejected R9 candidate at `c1a0fd1909598157559ab4e6e4737374216bdba2`

## Purpose

R10 preserves R9's verified gains and closes only the three remaining implementation roots. The checked-in JSON is the complete effective contract. Its generator reads the exact frozen R9 bytes and emits one byte-stable document.

## Boundary

This is invisible trust infrastructure. It does not implement or prove intelligence, comprehension, delight, decision quality, the final call, Brain learning, export or customer value. It authorises no UI, database object, customer data, external research, model call, provider call, deployment, merge, release or old-backend deletion.

## One watermark authority

The type registry is the sole watermark schema. Both variants carry exact R10 versions, identity, lineage and version. Each variant has its own non-generic fingerprint preimage. Invalidation records an exact comparison entry containing the bound and current values, with null representing absence, so changed, removed and newly applicable controls cannot be confused.

## Proofs reproduce the locked kernel

Selector candidates use the real routes: `reuse`, `enrich`, `ask` and `session`. Each candidate carries the exact capability and policy booleans, provenance and reuse fields, trusted-evaluation version, finite nonnegative burden, canonical rejection reasons and derived eligibility used by the locked headless selector. The candidate set contains every candidate actually presented, never an invented fourth route. The chosen route is either the canonical least-burden eligible route or `abstain_hold`.

The selector owner contains the decision frame, evidence state and gap, expected material effect, controls, trusted evaluation, challenger result, watermarks, expiry and actionable outcome. Its proof therefore reproduces why a route was chosen rather than merely naming it.

The answer row carries the full locked human-owned receipt: intervention and approval identity, the answer-kind-discriminated value, immutable case-evidence status, case effect, pending proposal, retired interventions, visible consequence and explicit prohibition of automatic re-asking or session escalation.

Lifecycle authority names the acting human as well as the permitted actor class and actor set. One exact thirteen-transition catalogue supplies the valid before and after state, actor class, authority, precondition, invalidation and receipt type. Proof members bind actual precondition evidence to that catalogue.

Answer chains have an exact null-predecessor genesis. Dependency edges name the target kind, version and fingerprint. Every set has a schema version and an exact owner lineage/version source. Cross-row equality binds approved content to visibility, answers to the same intervention authority and surface, Release authority to the same pending projection and lifecycle authority and state to the same transition.

## Every delivery payload belongs to one causal path

Every transition payload exposes its effect reference, which must equal the transition effect. The same transaction appends the exact payload, the transition and a unique payload-consumption record. A payload cannot be reused by a second transition or another effect.

Reconciled success and reconciled failure have separate closed schemas with constant outcomes. An abandoned dispatch with three spent reservations has an explicit terminal failure that proves no invocation occurred. The invocation-start event is the durable consumption of call authority. Only the process that committed it receives the non-serializable single-use capability, and it must recheck payload, fence, provider capability, operation authority and attempt count immediately before crossing the provider boundary.

## Claim boundary

R10 is a fully materialized but unimplemented local contract. Passing local checks cannot prove database isolation, provider behavior, production safety, experience quality or customer value. Independent technical review remains blocking before any bounded local adapter and fixture implementation can open.
