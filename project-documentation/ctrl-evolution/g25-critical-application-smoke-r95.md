# G25 critical application smoke R95

**Status:** Every currently executable critical layer passes. A complete hosted product smoke is correctly blocked by the non-schema machinery that the blank replay intentionally did not restore.

## What now runs

The isolated database completed one self-cleaning critical loop:

- export included the current Brain fact and excluded the non-current fact;
- qualified context returned only the supported current fact;
- an authenticated owner pinned a decision and read it through the track record;
- three negative reactions to one briefing lens item became one reversible steering rule with evidence count `3` and weight `-0.4`.

R93 already proves real Supabase Auth and HTTP ownership boundaries. R94 proves the exact scheduling, asynchronous HTTP and semantic-vector primitives. Together these are a substantive backend path, not object-existence theatre.

The local product machinery also passes 205 focused tests across 17 files: AI-native news, source admission, clustering, synthesis, trends, briefing conversation, decision reliability and interfaces, plus deterministic skill and export packaging.

Vite transformed 2,812 modules and produced the production bundle. It reported two non-fatal architecture warnings: the Supabase client is imported both statically and dynamically, and the main generated chunk is large. The formal `npm run build` stopped before Vite because the repository standards gate found nine inherited em-dash violations in unchanged historical records. That is a real repository gate defect, but it is not a product compilation failure and R95 does not rewrite immutable historical evidence to conceal it.

## Why the end-to-end verdict remains closed

The isolated project contains zero Edge Functions, zero Storage buckets, zero production schedules and zero Vault entries. Production has 183 functions, five buckets, fifteen schedules and two Vault names. The recovery target also lacks production’s Realtime publication membership.

Those absences prevent an honest hosted smoke of generated news, audio creation and playback, scheduled briefing return, email, signed Storage access and downloadable exports. Passing local tests does not make remote machinery exist.

R95 therefore closes the executable schema and code layers while preserving the exact remaining gate: restore and verify the non-schema plane without copying secrets into Git or deploying unreviewed live-only code.
