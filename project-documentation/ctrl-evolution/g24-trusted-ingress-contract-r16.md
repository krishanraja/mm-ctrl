# G24 trusted canonical ingress contract R16

**State:** fifteenth local repair candidate for independent attack; no adapter, database or runtime implementation

**Supersedes:** the rejected R15 candidate at `0977caef12a0ef525702e23e0b248170b22c417d`

## Purpose

R16 closes the five executable failures found across both R15 attacks. The checked-in JSON is the complete effective contract, generated byte-for-byte from the frozen R15 machine document.

## Resolvable result truth

Every successful operation result now resolves to one closed append-only blob row. That row carries the exact canonical payload bytes as base64url, decoded byte length, content hash, result schema and a complete blob fingerprint. Its reference is unique within the workspace and cannot be rebound.

The canonical committed-success row joins the same workspace, blob reference, result schema and content hash. It also has a complete success-envelope fingerprint covering operation scope, principal, request identity, response identity, result identity and commit time. The blob and success row commit together or neither exists.

Release proof resolution now joins both records. It verifies exact bytes before parsing the declared branch, recomputing the branch fingerprint and extracting the branch-specific result and terminal receipt.

## Deterministic case authority

Case authority is now a workspace, subject and case-scoped append-only row. It has a stable binding reference, semantic and envelope fingerprints, version, validity interval, uniqueness constraints and one deterministic current-selection rule. Case identity can therefore resolve exactly which authority was current at its snapshot.

## Complete lifecycle and evaluator identity

The omitted two-party predicate now uses action-receipt fingerprints, authority-receipt fingerprint and nullable predecessor-version reference. Legacy lifecycle vocabulary is rejected across the whole materialized effective document after removing only the literal forbidden-token declaration. There is no hand-maintained active-root allowlist.

Lifecycle precondition results now bind both evaluator version and evaluator artifact hash to every written evidence row and the exact current evaluator-registry member. A result cannot name a different evaluator binary from the evidence it returns.

## Honest dead-worker recovery

A vanished process cannot prove the state of a non-serializable capability. Every expired current `invoking` tip without a committed successor therefore routes to reaper-owned lease-expiry ambiguity. The reaper can no longer claim a no-call abort.

Worker ambiguity and no-call abort require a live lease and the exact selected outcome row. Transition authorization is derived from the same ordered table, and every append still compares and swaps the unchanged current tip from that snapshot. The provider-call row consumes its single-use capability at the immediate call boundary.

## Visible experience

Nothing in R16 becomes interface copy, a customer state or another approval. It protects the same natural question, answer and Release actions by making replay and recovery truthful underneath them.

## Boundary

R16 remains unimplemented local trust infrastructure. It authorises no UI, database change, customer data, research, model or provider call, deployment, merge or release. Independent technical review remains blocking.
