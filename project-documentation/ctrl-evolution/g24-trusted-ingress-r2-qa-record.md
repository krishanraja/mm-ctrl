# G24 trusted canonical ingress R2 QA record

**State:** R2 repair candidate; exact human, machine and QA bytes plus executable invariants pass deterministic checks; independent attack pending

## Repaired seams

- A closed ten-operation union now carries exact human intent without accepting authority or standing from the caller.
- Authentication is transport context and cannot enter persisted operation bytes, logs or receipts.
- One operation registry binds actor, live session, case-derived workspace, class, intent and original result.
- Every selector snapshot contains exactly one candidate for each route, current server-derived burdens and complete capability and control set seals.
- One full snapshot fingerprint covers the intent, canonical read set, evaluator state, chain tips, plan state and database time.
- The future protocol is one serializable operation with locks, compare-and-swap keys, uniqueness and three bounded identical retries.
- A canonical owner map, direct-DML denial and dedicated least-privilege ingress role replace circular database provenance.
- A private per-type proof bridge is required before any restart claim, including terminal-state registration before plan use.
- A transactional outbox and explicit unknown outcome replace impossible exactly-once delivery language.
- Eighteen explicit local limits and fifteen attack families remove implementation guesswork.
- The checker will bind complete R2 human and machine bytes, so flipping an unchecked invariant cannot silently pass.

## Preserved limits

No adapter, proof bridge, database procedure, schema, role, outbox, Supabase resource, runtime connection, model call, customer data or external effect exists. R2 remains unverified until independent reviewers attack its exact frozen bytes.
