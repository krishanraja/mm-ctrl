# G24 trusted canonical ingress R56

**Status:** fully materialized local architecture repair; independent attack remains blocking

**Authority:** the exact generated R56 JSON contract

## What R56 repairs

R56 closes the remaining fixture-validator gaps from R55. The recursive interpreter resolves `enum_ref` to the exact named authoritative array and enforces identifier normalization, minimum and maximum UTF-8 bytes, patterns, canonical base64url decode and re-encode, decoded-byte limits, and nullable values as either null or recursively valid under `value_schema`. Conditional rules are drawn from one closed registry, and the committed `use_release` result-reference rule has an explicit positive fixture and negative mutation.

Linked identity evidence is no longer identity-field theatre. The 70 linked identities share 38 complete equality fixtures. Each fixture binds the source reference and every declared bytes or fingerprint companion to one exact target before the source row is resealed. Empty arrays are valid only when the equality declaration itself has no companions. Ordinary held results therefore prove `result_ref`, `result_bytes_sha256` and `result_fingerprint` against the same target artifact.

Selector evidence records the actual values present in the source fixture. Every applicable operation, branch, proof-family, branch-class, evidence-kind, target-store and selection-row dimension must match. A reduced projection cannot hide a disagreement.

The focused checker independently validates the candidate and rejects 233 attacks. It covers the exact R55 reproductions, including eight `enum_ref` mutations, empty and 257-byte identifiers, a non-matching 32-hex pattern, noncanonical base64url `A`, valid and invalid non-null nullable values, the committed `use_release` condition, vacuous companion arrays, cross-target companion splices and selector-dimension omissions. The final semantic closure contains 57,702 reference occurrences sealed across 239 manifest rows.

## Boundary

R56 is invisible infrastructure. It changes no customer-facing language, interaction or product promise. It opens no adapter, database object, runtime connection, UI, deployment or external action.
