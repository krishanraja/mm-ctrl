# G24 trusted canonical ingress R43

**Status:** fully materialized repair candidate under independent review

**Authority:** the exact generated R43 JSON contract

## What R43 repairs

R43 replaces projection-only restart stand-ins with selected durable store rows. Request, target, proof, dual-proof bundle, nonce receipt, authority read set, raw input, result, session evidence, historical response, replay payload and replay envelope artifacts carry their exact schema and variant, canonical bytes, byte hash, declared content fingerprint and stored row. Every content-addressed reference equals the selected canonical byte hash, and restart independently resolves and recomputes the stored evidence before classification.

The four branch fixtures contain 37 content-addressed lineage artifacts and 44 total durable lineage rows. A 201-row correlation graph is generated from selected closed schema properties and content-address rules. It binds operation, request, idempotency, result, hold, durable selection, proof, nonce, bundle, raw evidence, registry, history, payload and envelope facts. It rejects proof-byte, nonce-fingerprint, raw-evaluator, operation, operation-id and hold-branch splicing even when a changed subset is resealed.

Owned snapshots define every copied array index and object field as an own data property through a captured trusted primitive. Numeric setters or getters installed on `Array.prototype` cannot intercept the copy. Property-key bytes and value bytes share exact per-string and cumulative UTF-8 limits before output allocation or child traversal.

Every one of 5,620 semantic-reference occurrences now records either an exact nested target path with the nearest target schema version, or an explicit runtime, sentinel, wildcard or closed-compound discriminator. No reference is credited to a top-level fallback merely because its precise nested target was not resolved.

## Boundary

R43 is invisible infrastructure. It changes no customer-facing language, interaction or product promise. It opens no adapter, database object, runtime connection, UI, deployment or external action.
