# G24 trusted canonical ingress R10 QA record

**State:** fully materialized local contract candidate; deterministic and mutation checks pending independent attack

## Repair summary

- One R10 watermark schema now owns identity, lineage, version and variant fingerprinting.
- Release invalidation distinguishes exact bound and current values, including absence.
- Selector rows reproduce the locked routes, eligibility inputs, provenance, burden, rejections, trusted evaluation and least-burden result.
- Answer rows reproduce the full human-owned receipt and its automatic-action prohibitions.
- Lifecycle authority names the acting human and resolves against the exact thirteen-transition catalogue.
- Proof sets have schema versions, owner sources, exact genesis and typed dependency targets.
- Cross-row equalities bind every material owner and dependency to the same semantic history.
- Every outbox payload is effect-bound and uniquely consumed; reconciliation branches have distinct schemas.
- Invocation consumes one durable call authority and performs exact current rechecks before any provider boundary.

## Verification

The R10 checker validates exact R9 preservation, the closed reference and type graph, evaluator parity, single watermark authority, comparison semantics, locked selector and answer fields, lifecycle catalogue, cross-row equality, set ownership, payload-effect equality, unique payload consumption, exact reconciliation, the no-invocation budget terminal and invocation authority.

Thirty-six mutation probes attack each R9 bypass, including a duplicate watermark schema, stale variant version, generic watermark preimage, invented routes, weakened burden, missing eligibility, empty candidates, missing least-burden selection, undiscriminated answers, automatic action, missing acting human, arbitrary lifecycle precondition, broken anti-splicing equality, unversioned proof sets, vague genesis, untyped dependencies, cross-wired or reusable payloads, arbitrary reconciliation, false provider-call semantics, missing budget terminal, reconstructible capability and omitted current rechecks.

## Honest limits

No adapter, schema, database procedure, runtime connection, proof resolver, outbox worker or external call implements R10. Passing these checks is not independent review, database proof, production proof, product intelligence, usability or customer value.

**Scope:** local contract only

**External actions:** none authorised or performed
