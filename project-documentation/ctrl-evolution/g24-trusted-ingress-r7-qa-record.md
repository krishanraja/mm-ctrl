# G24 trusted canonical ingress R7 QA record

**State:** fully materialized local contract candidate; deterministic and mutation checks pending independent attack

## Repair summary

- One closed type and reference graph replaces silent JSON omission and dangling outcomes.
- Registry lookup now occurs before fresh authority; historical replay runs disclosure checks only.
- Approval, release use and lifecycle transition expose exact per-branch effects.
- A server-issued five-minute challenge and explicit human acknowledgement replace caller-asserted presentation.
- Lifecycle issue receipts are immutable; combination and transition consumption are append-only records with exact collision behavior.
- Every proof family maps its common identity to a named authoritative table, row schema, extension equality, current rule and failure.
- Canonical bytes are defined for every supported scalar, optional, array and object shape.
- Outbox reservation, dispatch, success, failure, ambiguity and reconciliation are immutable events bound to one attempt identity, with an exact lease-expiry reaper path.

## Verification

The R7 checker verifies generated-byte equality, all seventeen registered operations, the complete type and reference graph, canonical encoding, admission phase order, presentation protocol, branch effects, lifecycle collisions and freshness, proof row maps, all thirteen fingerprint families and the complete outbox transition table.

Fifty-two adversarial mutations cover the reviewer bypasses, including unknown types, dangling schema and fingerprint references, malformed request outcomes, fresh authority before registry lookup, automated or hidden presentation, unconditional branch writes, mutable lifecycle receipts, nonce and pair reuse, omitted proof equalities, boolean fingerprints, orphaned dispatch, reaper calls, reconciliation sends and retry-count resets.

## Honest limits

No adapter, schema, database procedure, runtime connection, proof resolver, outbox worker or external call implements R7. Passing these checks is not independent review, database proof, production proof, product intelligence, usability or customer value.

**Scope:** local contract only

**External actions:** none authorised or performed
