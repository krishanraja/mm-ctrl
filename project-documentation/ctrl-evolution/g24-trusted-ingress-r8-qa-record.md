# G24 trusted canonical ingress R8 QA record

**State:** fully materialized local contract candidate; deterministic and mutation checks pending independent attack

## Repair summary

- Applicable-control watermarks now carry the complete canonical identifier in a structural variant.
- Evaluator exports have exact parity with all seventeen operation result schemas.
- Presentation binds server-resolved viewer, case, atom, version, fingerprint and exact expiry to an explicit foreground human attestation and unique later consumption.
- Approval and release use one authoritative branch-effect vocabulary.
- Lifecycle secondary idempotency excludes generated record fields and commits phase-valid success or hold results under every new operation ID.
- Every proof extension authority field resolves through a closed family-specific owner row, dependent row or sealed set with exact scope and selection.
- Every authority record links to one unique-domain fingerprint whose preimage covers every non-fingerprint schema field.
- Every outbox transition appends a typed payload and one causal transition event atomically; unique predecessor identity prevents conflicting successors.

## Verification

The R8 checker traverses references with own-property semantics, verifies operation and evaluator parity, structural watermark equivalence, complete presentation predicates, stable lifecycle collision identity, proof-field coverage, family row schemas, non-circular fingerprints, unique fingerprint domains, the causal outbox transition table and no-send reaper and reconciliation paths.

Forty-nine mutation probes cover every R7 bypass reported by the reviewers, including inherited `__proto__` references, stale evaluator exports, narrowed control IDs, caller-selected viewers, weakened expiry or actor equality, missing consumption uniqueness, duplicate effect maps, server fields in secondary idempotency, unmapped proof authority, circular row fingerprints, missing seals, orphaned record fingerprints, collapsed domains, incomplete preimages, missing causal uniqueness, removed claim authority, weakened reaper conditions, direct ambiguous-to-reserved retry, provider sends and fourth attempts.

## Honest limits

No adapter, schema, database procedure, runtime connection, proof resolver, outbox worker or external call implements R8. Passing these checks is not independent review, database proof, production proof, product intelligence, usability or customer value.

**Scope:** local contract only

**External actions:** none authorised or performed
