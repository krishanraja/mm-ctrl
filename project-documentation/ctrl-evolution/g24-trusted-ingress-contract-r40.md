# G24 trusted canonical ingress R40

**Status:** fully materialized repair candidate under independent review

**Authority:** the exact generated R40 JSON contract

## What R40 repairs

R40 removes every remaining replay `selected_variant` pseudo-reference. Committed, ordinary-held, session-held, replayed and replayed-held bindings name exact source schema, discriminator variant, source property, destination schema, destination variant and destination property. The materializer and checker resolve each binding to actual declared properties and reject missing, extra or type-mismatched fields.

Replay derivation binds the exact R40 registry and resolution-authority versions. The classifier fingerprint has one closed preimage with exactly `domain_ascii`, `schema_version` and `classifier_row_without_fingerprint`; all ninety classifier rows are regenerated and independently recomputed.

Canonical hashing now rejects non-finite numbers, negative zero, undefined, bigint, functions, symbols, sparse arrays, non-plain objects and cycles before canonicalization. Accepted objects use Unicode-code-point-sorted keys, preserved array order and UTF-8.

The authority graph enumerates 1,766 semantic-reference occurrences by exact source authority, field path, literal reference and unique manifested owner. Operation case, derived and workload predicates create explicit dependency edges. Unknown, nonliteral, unresolved, multiply owned and caller-controlled semantic references fail materialization.

Four restart fixtures now contain canonical stored registry and hold row values, bytes, byte hashes, fingerprint schemas, fingerprint preimages, recorded fingerprints and classifier rows. Restart checks re-resolve the discriminated schema, canonical bytes and fingerprint before recovering the five durable selection facts. Projection-only fixtures and cross-record splicing fail closed.

## Boundary

R40 is invisible infrastructure. It adds no customer ceremony, administration, UI or intelligence claim. No adapter, database object, runtime connection, deployment or external action is authorised.
