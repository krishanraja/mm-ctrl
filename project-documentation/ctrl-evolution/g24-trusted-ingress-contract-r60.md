# G24 trusted canonical ingress R60

**Status:** fully materialized local architecture repair; independent attack remains blocking

**Authority:** the exact generated R60 JSON contract

## What R60 repairs

R60 makes evidence ordering semantic rather than positional theatre. The lifecycle-precondition fingerprint array is accepted only with a closed resolved pair for every same-position evidence reference. Each pair must resolve the exact reference and exact fingerprint; swapped fingerprints, missing pairs, substituted references and duplicate reference resolution fail closed.

Every one of the 21 frozen ordering declarations now has its own canonical positive and negative fixture. The checker independently reconstructs those fixtures from the exact frozen R59 schema paths and executes the selected handler. This preserves R59's five noncompanion ordering handlers while proving the companion relationship rather than only sorting the reference array.

Intervention `payload_schema_version` is now derived from `atom_kind`. Question atoms bind exactly to `ctrl.g24.question-contract.r13.v1`; session atoms bind exactly to the R60 session payload schema. The two canonical identity fixtures exercise different kinds, and arbitrary or cross-kind version substitutions fail after coherent atom resealing.

Question display fields again preserve the leader's exact original bytes while requiring nonblank content after trim. Padded wording is valid. Blank-only wording is not. Exact trimming remains mandatory for visible consequences and pending human-owned proposals.

Conditional exclusion derivation recursively follows each actual selected fixture schema through nullable values, schema references, arrays and discriminated variants. It records the exact identity, rule, selected schema path and fixture value path. The closure now identifies 129 conditionally touched identities, one with fully closed local semantics and 128 honest exclusions spanning 371 exact unproved rule-path occurrences. Question identity `identity_0042` includes its question-contract and five nested answer-effect paths; session identity `identity_0043` does not inherit the unreachable question branch.

The focused checker independently validates the candidate and rejects 289 attacks. It covers 1,228 native persisted identities, 374 selectors over 2,516 source-schema-valid contexts, 58,278 semantic-reference occurrences and 244 manifest rows.

## Honest semantic boundary

Twenty-five closed local predicates execute, zero detached contexts count as proof and 78 cross-artifact or live rules remain explicitly unproved. Recursive traversal makes the exclusion set more complete without pretending that documentation fixtures prove runtime enforcement.

## Product boundary

R60 is invisible infrastructure for the one canonical Brain and leader-owned authority. It changes no customer-facing interaction, approval step or product promise. It opens no adapter, database object, runtime connection, UI, deployment or external action.
