import { createHash } from 'node:crypto'
import { readFileSync, writeFileSync } from 'node:fs'
import { join, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
import { types as nodeUtilTypes } from 'node:util'
import { materializedR43, r43SemanticAuthorityPaths } from './materialize-ctrl-g24-trusted-ingress-r43.mjs'

const root = process.cwd()
const inputPath = 'project-documentation/ctrl-evolution/g24-trusted-ingress-contract-r43.json'
const outputPath = 'project-documentation/ctrl-evolution/g24-trusted-ingress-contract-r44.json'
const inputBytes = readFileSync(join(root, inputPath), 'utf8')

// Captured during isolated module bootstrap, before any caller-controlled value is accepted.
const PrimordialReflectApply = Reflect.apply
const PrimordialReflectOwnKeys = Reflect.ownKeys
const PrimordialReflectGetOwnPropertyDescriptor = Reflect.getOwnPropertyDescriptor
const PrimordialObjectGetPrototypeOf = Object.getPrototypeOf
const PrimordialObjectFreeze = Object.freeze
const PrimordialObjectCreate = Object.create
const PrimordialObjectDefineProperty = Object.defineProperty
const PrimordialObjectHasOwn = Object.hasOwn
const PrimordialObjectIs = Object.is
const PrimordialArrayIsArray = Array.isArray
const PrimordialJSONstringify = JSON.stringify
const PrimordialNumberIsFinite = Number.isFinite
const PrimordialNumberIsInteger = Number.isInteger
const PrimordialStringCharCodeAt = String.prototype.charCodeAt
const PrimordialBufferByteLength = Buffer.byteLength
const PrimordialIsProxy = nodeUtilTypes.isProxy
const PrimordialStructuredClone = globalThis.structuredClone
const PrimordialWeakSetHas = WeakSet.prototype.has
const PrimordialWeakSetAdd = WeakSet.prototype.add
const PrimordialWeakSetDelete = WeakSet.prototype.delete
const PrimordialWeakSet = WeakSet
const PrimordialBufferFrom = Buffer.from
const apply = (fn, receiver, args) => PrimordialReflectApply(fn, receiver, args)
const defineOwn = (target, key, value) => apply(PrimordialObjectDefineProperty, Object, [target, key, { value, enumerable: true, writable: true, configurable: true }])
const shaBytes = bytes => createHash('sha256').update(bytes).digest('hex')
const fp = { type: 'sha256' }
const id = { type: 'identifier' }
const LIMITS = { max_depth: 128, max_nodes: 1000000, max_string_utf8_bytes: 8388608, max_total_string_utf8_bytes: 67108864, max_array_length: 100000, max_object_keys: 100000 }

function validUnicode(value) { for (let index = 0; index < value.length; index += 1) { const unit = apply(PrimordialStringCharCodeAt, value, [index]); if (unit >= 0xd800 && unit <= 0xdbff) { const next = apply(PrimordialStringCharCodeAt, value, [index + 1]); if (!(next >= 0xdc00 && next <= 0xdfff)) return false; index += 1 } else if (unit >= 0xdc00 && unit <= 0xdfff) return false } return true }
function accountString(value, state, path) { if (!validUnicode(value)) throw new Error(`invalid_unicode:${path}`); const bytes = apply(PrimordialBufferByteLength, Buffer, [value, 'utf8']); if (bytes > LIMITS.max_string_utf8_bytes) throw new Error(`max_string_bytes:${path}`); state.stringBytes += bytes; if (state.stringBytes > LIMITS.max_total_string_utf8_bytes) throw new Error(`max_total_string_bytes:${path}`) }
function snapshotOwned(value, state = { nodes: 0, stringBytes: 0, ancestors: new PrimordialWeakSet() }, depth = 0, path = '$') {
  if (depth > LIMITS.max_depth) throw new Error(`max_depth:${path}`)
  state.nodes += 1
  if (state.nodes > LIMITS.max_nodes) throw new Error(`max_nodes:${path}`)
  if (value === null || typeof value === 'boolean') return value
  if (typeof value === 'string') { accountString(value, state, path); return value }
  if (typeof value === 'number') { if (!apply(PrimordialNumberIsFinite, Number, [value]) || apply(PrimordialObjectIs, Object, [value, -0])) throw new Error(`invalid_number:${path}`); return value }
  if (typeof value !== 'object') throw new Error(`unsupported_value:${path}:${typeof value}`)
  if (apply(PrimordialIsProxy, nodeUtilTypes, [value])) throw new Error(`proxy:${path}`)
  if (apply(PrimordialWeakSetHas, state.ancestors, [value])) throw new Error(`cycle:${path}`)
  const array = apply(PrimordialArrayIsArray, Array, [value])
  const prototype = apply(PrimordialObjectGetPrototypeOf, Object, [value])
  if ((array && prototype !== Array.prototype) || (!array && prototype !== Object.prototype && prototype !== null)) throw new Error(`unsupported_prototype:${path}`)
  if (array) {
    let lengthDescriptor
    try { lengthDescriptor = apply(PrimordialReflectGetOwnPropertyDescriptor, Reflect, [value, 'length']) } catch { throw new Error(`throwing_length_descriptor:${path}`) }
    if (!lengthDescriptor || !apply(PrimordialObjectHasOwn, Object, [lengthDescriptor, 'value']) || !apply(PrimordialNumberIsInteger, Number, [lengthDescriptor.value]) || lengthDescriptor.value < 0 || lengthDescriptor.value > LIMITS.max_array_length) throw new Error(`array_length_bound:${path}`)
  }
  let keys
  try { keys = apply(PrimordialReflectOwnKeys, Reflect, [value]) } catch { throw new Error(`throwing_reflection:${path}`) }
  if (keys.length > LIMITS.max_object_keys + (array ? 1 : 0)) throw new Error(`max_keys:${path}`)
  for (let index = 0; index < keys.length; index += 1) { const key = keys[index]; if (typeof key === 'symbol') throw new Error(`symbol_key:${path}`); accountString(key, state, `${path}.<key:${index}>`) }
  apply(PrimordialWeakSetAdd, state.ancestors, [value])
  if (array) {
    const lengthDescriptor = apply(PrimordialReflectGetOwnPropertyDescriptor, Reflect, [value, 'length'])
    const length = lengthDescriptor.value
    if (keys.length !== length + 1) throw new Error(`sparse_or_extra_array_key:${path}`)
    const seen = apply(PrimordialObjectCreate, Object, [null])
    for (let index = 0; index < keys.length; index += 1) { const key = keys[index]; if (key !== 'length') { if (apply(PrimordialObjectHasOwn, Object, [seen, key])) throw new Error(`duplicate_array_key:${path}`); defineOwn(seen, key, true) } }
    const output = []
    for (let index = 0; index < length; index += 1) { const key = `${index}`; if (!seen[key]) throw new Error(`sparse_array:${path}[${index}]`); let descriptor; try { descriptor = apply(PrimordialReflectGetOwnPropertyDescriptor, Reflect, [value, key]) } catch { throw new Error(`throwing_descriptor:${path}[${index}]`) } if (!descriptor || !descriptor.enumerable || !apply(PrimordialObjectHasOwn, Object, [descriptor, 'value']) || descriptor.get || descriptor.set) throw new Error(`invalid_array_descriptor:${path}[${index}]`); defineOwn(output, key, snapshotOwned(descriptor.value, state, depth + 1, `${path}[${index}]`)); const own = apply(PrimordialReflectGetOwnPropertyDescriptor, Reflect, [output, key]); if (!own || own.get || own.set || !apply(PrimordialObjectHasOwn, Object, [own, 'value'])) throw new Error(`output_array_not_dense_data:${path}[${index}]`) }
    apply(PrimordialWeakSetDelete, state.ancestors, [value])
    return apply(PrimordialObjectFreeze, Object, [output])
  }
  if (keys.length > LIMITS.max_object_keys) throw new Error(`max_object_keys:${path}`)
  const output = apply(PrimordialObjectCreate, Object, [null])
  for (let index = 0; index < keys.length; index += 1) { const key = keys[index]; let descriptor; try { descriptor = apply(PrimordialReflectGetOwnPropertyDescriptor, Reflect, [value, key]) } catch { throw new Error(`throwing_descriptor:${path}.${key}`) } if (!descriptor || !descriptor.enumerable || !apply(PrimordialObjectHasOwn, Object, [descriptor, 'value']) || descriptor.get || descriptor.set) throw new Error(`invalid_object_descriptor:${path}.${key}`); defineOwn(output, key, snapshotOwned(descriptor.value, state, depth + 1, `${path}.${key}`)) }
  apply(PrimordialWeakSetDelete, state.ancestors, [value])
  return apply(PrimordialObjectFreeze, Object, [output])
}
function codePointAt(value, index) { const first = apply(PrimordialStringCharCodeAt, value, [index]); if (first >= 0xd800 && first <= 0xdbff && index + 1 < value.length) { const second = apply(PrimordialStringCharCodeAt, value, [index + 1]); if (second >= 0xdc00 && second <= 0xdfff) return [((first - 0xd800) * 0x400) + second - 0xdc00 + 0x10000, 2] } return [first, 1] }
function codePointCompare(left, right) { let li = 0, ri = 0; while (li < left.length && ri < right.length) { const leftPoint = codePointAt(left, li), rightPoint = codePointAt(right, ri), lc = leftPoint[0], ln = leftPoint[1], rc = rightPoint[0], rn = rightPoint[1]; if (lc !== rc) return lc - rc; li += ln; ri += rn } return left.length - right.length }
function sortedKeys(value) { const keys = apply(PrimordialReflectOwnKeys, Reflect, [value]), strings = []; for (let index = 0; index < keys.length; index += 1) { if (typeof keys[index] === 'symbol') throw new Error('snapshot_symbol'); defineOwn(strings, `${index}`, keys[index]) } for (let index = 1; index < strings.length; index += 1) { const current = strings[index]; let prior = index - 1; while (prior >= 0 && codePointCompare(strings[prior], current) > 0) { defineOwn(strings, `${prior + 1}`, strings[prior]); prior -= 1 } defineOwn(strings, `${prior + 1}`, current) } return strings }
function canonicalSnapshot(value) { if (value === null || typeof value !== 'object') return apply(PrimordialJSONstringify, JSON, [value]); if (apply(PrimordialArrayIsArray, Array, [value])) { let output = '['; for (let index = 0; index < value.length; index += 1) { if (index) output += ','; output += canonicalSnapshot(value[index]) } return `${output}]` } const keys = sortedKeys(value); let output = '{'; for (let index = 0; index < keys.length; index += 1) { if (index) output += ','; output += `${apply(PrimordialJSONstringify, JSON, [keys[index]])}:${canonicalSnapshot(value[keys[index]])}` } return `${output}}` }
function canonical(value) { return canonicalSnapshot(snapshotOwned(value)) }
const hashCanonical = value => shaBytes(apply(PrimordialBufferFrom, Buffer, [canonical(value), 'utf8']))
function closed(version, properties, extras = {}) { const optional = extras.optional ?? [], keys = Object.keys(properties), value = { schema_version: version, type: 'object', exact_keys: keys, required: keys.filter(key => !optional.includes(key)), additional_properties: false, properties, ...extras }; if (!optional.length) delete value.optional; return value }
function resolvePath(object, path) { let value = object; const parts = path.split('.'); for (let index = 0; index < parts.length; index += 1) { if (!value || !Object.hasOwn(value, parts[index])) return undefined; value = value[parts[index]] } return value }
function schemaAt(contract, schemaRef, variant = 'UNAVAILABLE') { let schema = resolvePath(contract, schemaRef); if (variant !== 'UNAVAILABLE') schema = schema?.variants?.[variant]; return schema }
function semanticVersion(path, value) { return value?.schema_version ?? `ctrl.g24.runtime-semantic.${path.replaceAll('_', '-')}.r44.v1` }
function kindOf(value) { return Array.isArray(value) ? 'array' : value === null ? 'null' : typeof value === 'object' ? 'object' : typeof value }
function keysetOf(value) { if (Array.isArray(value)) return [`$array_length:${value.length}`]; if (value && typeof value === 'object') return Object.keys(value).sort(codePointCompare); return ['$scalar'] }

const r44 = apply(PrimordialStructuredClone, undefined, [materializedR43])
r44.schema_version = 'ctrl.g24.trusted-ingress.r44.effective.v1'
r44.status = 'forty_fourth_repair_candidate_under_independent_review'
r44.supersedes = { commit: '2c7df0c4214750bce6bae73ffb740268abc14e94', tree: '85dfd2a433eac82cd4bfae13d111d45ab1144bc5', human_blob: 'e71cd4471c37f404bf02ca9496c723bffcae5b65', machine_blob: '33ebc0ed50396296422241b01d1f5a78cc252705', qa_blob: 'ef358bcc9e6703d4fa37fae60d5a27120967db7d', checker_blob: 'cd6664a5099b036cfe8f6863260ba3a5e143d41c', materializer_blob: 'aced119abd94df5eb7625b27e7d72e19dc6213f7', founder_checker_blob: 'c674f2dbaff0b23636b287d04a5b0b795ff1f2df', adjudication: 'veto' }
r44.materialization = { authority: 'this_complete_generated_effective_document', generator: 'scripts/materialize-ctrl-g24-trusted-ingress-r44.mjs', frozen_input: { path: inputPath, sha256: shaBytes(inputBytes) }, conceptual_overlay_allowed: false, runtime_inheritance_allowed: false, generated_document_must_equal_generator_output_byte_for_byte: true, isolated_bootstrap_assumption: 'trusted_node_module_realm_captures_primordials_before_any_caller_controlled_value_or_prototype_mutation', snapshot_before_validation_hash_or_use: true, caller_writer_or_precedence_extensions: 'forbidden' }
r44.owned_immutable_canonical_snapshot_pipeline = { schema_version: 'ctrl.g24.owned-immutable-canonical-snapshot-pipeline.r44.v1', bootstrap: r44.materialization.isolated_bootstrap_assumption, captured_primordials: ['Reflect.apply', 'Reflect.ownKeys', 'Reflect.getOwnPropertyDescriptor', 'Object.getPrototypeOf', 'Object.freeze', 'Array.isArray', 'JSON.stringify', 'Number.isFinite', 'String.prototype.charCodeAt', 'Buffer.byteLength', 'util.types.isProxy', 'structuredClone'], caller_mutable_intrinsics_after_bootstrap: 'ignored', traversal: 'owned_descriptor_values_read_once_after_exact_resource_preflight', accepted_prototypes: ['null', 'Object.prototype', 'Array.prototype_for_arrays_only'], rejected: ['proxy', 'throwing_reflection', 'symbol_key', 'non_enumerable_property', 'accessor', 'getter', 'setter', 'array_named_or_extra_key', 'sparse_array', 'cycle', 'nonfinite_number', 'negative_zero', 'unsupported_type', 'invalid_unicode_scalar_or_lone_surrogate_in_key_or_value', 'resource_limit'], output: 'null_prototype_owned_recursively_frozen_snapshot_or_frozen_dense_array', validation_hash_and_use_input: 'owned_snapshot_only', failure: 'abort_before_validation_hash_use_write_or_disclosure' }
r44.canonical_snapshot_resource_limits = { schema_version: 'ctrl.g24.canonical-snapshot-resource-limits.r44.v1', ...LIMITS, array_preflight_order: ['read_own_length_data_descriptor', 'reject_length_above_max_before_ownKeys_or_output_allocation', 'Reflect.ownKeys_once', 'reject_key_count_mismatch_before_output_allocation', 'validate_dense_exact_indices', 'allocate_and_copy'], object_preflight_order: ['Reflect.ownKeys_once', 'reject_key_count_above_max_before_output_allocation', 'validate_own_enumerable_data_descriptors', 'allocate_and_copy'], cumulative_limits_apply_before_next_value_copy: true }

for (const [variantName, schema] of Object.entries(r44.authority_operation_registry.row_union.variants)) {
  schema.schema_version = `ctrl.g24.authority-operation-registry-${variantName.replaceAll('_', '-')}.r44.v1`
  schema.row_ref_schema_version = 'ctrl.g24.authority-operation-registry-row-identity-preimage.r44.v1'
  schema.row_ref_domain_ascii = `CTRL-G24-AUTHORITY-OPERATION-REGISTRY-${variantName.toUpperCase()}-ROW-REF-R44`
  schema.row_ref_preimage_included_fields = schema.exact_keys.filter(field => !['registry_row_ref', 'registry_fingerprint'].includes(field))
  schema.row_ref_preimage_excluded_fields = ['registry_row_ref', 'registry_fingerprint']
  schema.row_ref_preimage = 'domain_ascii_schema_version_and_ordered_exact_row_fields_excluding_precisely_registry_row_ref_and_registry_fingerprint'
}
r44.authority_operation_registry.row_union.schema_version = 'ctrl.g24.authority-operation-registry-row-union.r44.v1'
r44.authority_operation_registry.schema_version = 'ctrl.g24.authority-operation-registry.r44.v1'
for (const [variantName, schema] of Object.entries(r44.authority_operation_hold_store.row_union.variants)) {
  schema.schema_version = `ctrl.g24.authority-operation-hold-${variantName.replaceAll('_', '-')}.r44.v1`
  schema.row_ref_schema_version = 'ctrl.g24.authority-operation-hold-row-identity-preimage.r44.v1'
  schema.row_ref_domain_ascii = `CTRL-G24-AUTHORITY-OPERATION-HOLD-${variantName.toUpperCase()}-ROW-REF-R44`
  schema.row_ref_preimage = 'domain_ascii_schema_version_and_ordered_exact_declared_row_ref_preimage_included_fields'
}
r44.authority_operation_hold_store.row_union.schema_version = 'ctrl.g24.authority-operation-hold-row-union.r44.v1'
r44.authority_operation_hold_store.schema_version = 'ctrl.g24.authority-operation-hold-store.r44.v1'
r44.normative_row_ref_derivation = { schema_version: 'ctrl.g24.normative-row-ref-derivation.r44.v1', registry: 'read_selected_registry_variant.row_ref_domain_ascii_row_ref_schema_version_and_row_ref_preimage_included_fields', hold: 'read_selected_hold_variant.row_ref_domain_ascii_row_ref_schema_version_and_row_ref_preimage_included_fields', preimage_shape: ['domain_ascii', 'schema_version', 'ordered_fields'], ordered_field_shape: ['field', 'value'], fixture_specific_formula: 'forbidden', fingerprint_then_bytes_order: ['derive_row_ref', 'assemble_fingerprint_preimage', 'derive_fingerprint', 'assemble_final_row', 'canonicalize_final_row', 'derive_final_bytes_sha256'] }

function valueForSpec(spec, seed) {
  if (Object.hasOwn(spec ?? {}, 'const')) return spec.const
  if (Array.isArray(spec?.enum)) return spec.enum[0]
  if (spec?.type === 'enum' && Array.isArray(spec.values)) return spec.values[0]
  if (spec?.type === 'sha256') return shaBytes(seed)
  if (spec?.type === 'sha256_or_exact_literal') return spec.literal
  if (spec?.type === 'canonical_timestamp') return '2026-09-14T00:00:00.000Z'
  if (spec?.type === 'nullable') return null
  if (spec?.type === 'boolean') return false
  if (spec?.type === 'positive_integer') return 1
  if (spec?.type === 'integer' || spec?.type === 'nonnegative_integer' || spec?.type === 'safe_nonnegative_integer') return 0
  if (String(spec?.type).includes('array')) return []
  return `r44_${shaBytes(seed).slice(0, 24)}`
}
function validateSpec(value, spec, path) {
  if (Object.hasOwn(spec ?? {}, 'const')) { if (value !== spec.const) throw new Error(`const:${path}`); return }
  if (Array.isArray(spec?.enum)) { if (!spec.enum.includes(value)) throw new Error(`enum:${path}`); return }
  if (spec?.type === 'enum') { if (!spec.values.includes(value)) throw new Error(`typed_enum:${path}`); return }
  if (spec?.type === 'sha256') { if (typeof value !== 'string' || !/^[0-9a-f]{64}$/.test(value)) throw new Error(`sha:${path}`); return }
  if (spec?.type === 'sha256_or_exact_literal') { if (value !== spec.literal && (typeof value !== 'string' || !/^[0-9a-f]{64}$/.test(value))) throw new Error(`sha_literal:${path}`); return }
  if (spec?.type === 'canonical_timestamp') { if (typeof value !== 'string' || new Date(value).toISOString() !== value) throw new Error(`timestamp:${path}`); return }
  if (spec?.type === 'base64url_without_padding') { if (typeof value !== 'string' || !/^[A-Za-z0-9_-]*$/.test(value) || value.includes('=')) throw new Error(`base64url:${path}`); return }
  if (spec?.type === 'nullable') { if (value !== null) validateSpec(value, spec.value_schema, path); return }
  if (spec?.type === 'identifier' || spec?.schema_ref) { if (typeof value !== 'string' || !value.length) throw new Error(`identifier:${path}`); return }
  if (spec?.type === 'boolean') { if (typeof value !== 'boolean') throw new Error(`boolean:${path}`); return }
  if (spec?.type === 'positive_integer') { if (!Number.isInteger(value) || value <= 0) throw new Error(`positive:${path}`); return }
  if (spec?.type === 'integer' || spec?.type === 'nonnegative_integer' || spec?.type === 'safe_nonnegative_integer') { if (!Number.isSafeInteger(value) || Object.is(value, -0) || ((spec.type === 'nonnegative_integer' || spec.type === 'safe_nonnegative_integer') && value < 0)) throw new Error(`integer:${path}`); return }
  if (String(spec?.type).includes('array')) { if (!Array.isArray(value)) throw new Error(`array:${path}`); return }
  throw new Error(`unsupported_spec:${path}`)
}
function orderedFields(row, fields) { const result = []; for (const field of fields) { if (!Object.hasOwn(row, field)) throw new Error(`missing_row_ref_operand:${field}`); result.push({ field, value: row[field] }) } return result }
function deriveNormativeRowRef(schema, row) { return hashCanonical({ domain_ascii: schema.row_ref_domain_ascii, schema_version: schema.row_ref_schema_version, ordered_fields: orderedFields(row, schema.row_ref_preimage_included_fields) }) }
function validateRow(schema, row, label) { const keys = Object.keys(row); if (schema.type !== 'object' || schema.additional_properties !== false || canonical(keys) !== canonical(schema.exact_keys)) throw new Error(`shape:${label}`); for (const field of schema.required) if (!Object.hasOwn(row, field)) throw new Error(`required:${label}:${field}`); for (const [field, spec] of Object.entries(schema.properties)) validateSpec(row[field], spec, `${label}.${field}`) }
function fingerprintFor(contract, schema, row) { const fingerprintSchema = resolvePath(contract, schema.fingerprint_ref), preimage = {}; for (const field of fingerprintSchema.preimage_order) { if (field !== 'domain_ascii' && !Object.hasOwn(row, field)) throw new Error(`fingerprint_operand:${field}`); preimage[field] = field === 'domain_ascii' ? fingerprintSchema.domain_ascii : row[field] } return { ref: schema.fingerprint_ref, preimage, fingerprint: hashCanonical(preimage) } }
function makeArtifact(contract, fixtureId, schemaRef, variant, overrides, withNormativeRowRef = false) {
  const schema = schemaAt(contract, schemaRef, variant), row = {}
  for (const [field, spec] of Object.entries(schema.properties)) row[field] = valueForSpec(spec, `${fixtureId}:${schemaRef}:${variant}:${field}`)
  Object.assign(row, overrides)
  if (withNormativeRowRef) { const rowRefField = variant.startsWith('original_') ? 'registry_row_ref' : 'hold_row_ref'; row[rowRefField] = deriveNormativeRowRef(schema, row) }
  const derived = fingerprintFor(contract, schema, row)
  row[schema.fingerprint_field] = derived.fingerprint
  validateRow(schema, row, `${fixtureId}:${variant}`)
  const bytes = canonical(row), bytesSha = shaBytes(Buffer.from(bytes, 'utf8'))
  return { fixture_artifact_id: `${fixtureId}:${variant}`, schema_ref: schemaRef, schema_variant: variant, schema_version: schema.schema_version, normative_row_ref_preimage: withNormativeRowRef ? { domain_ascii: schema.row_ref_domain_ascii, schema_version: schema.row_ref_schema_version, ordered_fields: orderedFields(row, schema.row_ref_preimage_included_fields) } : 'UNAVAILABLE', canonical_row_value: row, canonical_row_bytes_utf8: bytes, canonical_row_bytes_sha256: bytesSha, content_addressed_artifact_ref: bytesSha, fingerprint_schema_ref: derived.ref, fingerprint_preimage: derived.preimage, recorded_fingerprint: derived.fingerprint }
}
function selectionFor(operationName, resultBranch) { const row = r44.authority_operation_fresh_branch_class_selection.rows.find(item => item.operation_name === operationName && item.result_branch === resultBranch); if (!row) throw new Error(`selection:${operationName}:${resultBranch}`); return { fresh_selection_row_id: row.selection_row_id, proof_family: row.proof_family, branch_class: row.branch_class, evidence_kind: row.evidence_kind, fresh_selection_schema_version: row.selector_schema_version } }
const plans = [
  ['restart_committed', 'bootstrap_case_session_root_anchor', 'committed', 'original_committed', 'UNAVAILABLE'],
  ['restart_ordinary_held', 'bootstrap_case_session_root_anchor', 'authorization_hold', 'original_persisted_hold', 'ordinary_single_proof'],
  ['restart_verified_session_held', 'issue_server_session_principal', 'stale_head_hold', 'original_persisted_hold', 'session_dual_proof'],
  ['restart_raw_session_held', 'issue_server_session_principal', 'invalid_proof_hold', 'original_persisted_hold', 'session_dual_proof'],
]
function targetStore(operationName) { return operationName === 'bootstrap_case_session_root_anchor' ? 'case_session_root_trust_anchors' : 'case_server_session_principal_evidence' }
function triple(seed) { return { ref: shaBytes(`${seed}:ref`), bytes_sha256: shaBytes(`${seed}:bytes`), fingerprint: shaBytes(`${seed}:fingerprint`) } }
function historyArtifact(fixtureId, operationName, resultBranch, operationId, result) { const matrix = r44.authority_operation_historical_response_schema_matrix.rows.find(row => row.operation_name === operationName && row.result_branch === resultBranch); return makeArtifact(r44, fixtureId, 'authority_operation_historical_response_schema', 'UNAVAILABLE', { operation_name: operationName, operation_id: operationId, result_branch: resultBranch, response_schema_ref: matrix.response_schema_ref, response_schema_version: matrix.response_schema_version, response_payload_ref: result.ref, response_payload_bytes_sha256: result.bytes_sha256, result_ref: result.ref, result_bytes_sha256: result.bytes_sha256, result_fingerprint: result.fingerprint }) }
function payloadArtifact(fixtureId, operationName, resultBranch, operationId, result, history, registry, hold) { const held = hold !== 'UNAVAILABLE'; return makeArtifact(r44, fixtureId, 'authority_operation_replay_payload_schema', held ? 'replayed_held' : 'replayed', { replay_kind: held ? 'replayed_held' : 'replayed', operation_name: operationName, operation_id: operationId, historical_result_branch: resultBranch, historical_result_ref: result.ref, historical_result_bytes_sha256: result.bytes_sha256, historical_result_fingerprint: result.fingerprint, stored_historical_response_ref: history.content_addressed_artifact_ref, stored_historical_response_bytes_sha256: history.canonical_row_bytes_sha256, stored_historical_response_fingerprint: history.recorded_fingerprint, ...(held ? { held_registry_row_ref: registry.canonical_row_value.registry_row_ref, held_registry_row_fingerprint: registry.recorded_fingerprint, hold_row_ref: hold.canonical_row_value.hold_row_ref, hold_row_fingerprint: hold.recorded_fingerprint, session_hold_evidence_ref_or_unavailable: registry.canonical_row_value.session_hold_evidence_ref_or_unavailable, session_hold_evidence_bytes_sha256_or_unavailable: registry.canonical_row_value.session_hold_evidence_bytes_sha256_or_unavailable, session_hold_evidence_fingerprint_or_unavailable: registry.canonical_row_value.session_hold_evidence_fingerprint_or_unavailable, held_at: registry.canonical_row_value.held_at } : { committed_registry_row_ref: registry.canonical_row_value.registry_row_ref, committed_registry_row_fingerprint: registry.recorded_fingerprint }) }) }
function envelopeArtifact(fixtureId, operationName, resultBranch, operationId, history, payload, held) { return makeArtifact(r44, fixtureId, 'authority_operation_replay_envelope_schema', 'UNAVAILABLE', { envelope_kind: 'authority_operation_replay', replay_kind: held ? 'replayed_held' : 'replayed', operation_name: operationName, operation_id: operationId, historical_result_branch: resultBranch, payload_ref: payload.content_addressed_artifact_ref, payload_bytes_sha256: payload.canonical_row_bytes_sha256, payload_fingerprint: payload.recorded_fingerprint, replay_response_ref: history.content_addressed_artifact_ref, replay_response_bytes_sha256: history.canonical_row_bytes_sha256, replay_response_fingerprint: history.recorded_fingerprint }) }

function sessionEvidenceArtifact(fixtureId, operationName, resultBranch, operationId, evidenceKind, raw) {
  if (evidenceKind === 'verified_session_hold_evidence') {
    const bundle = triple(`${fixtureId}:bundle`), projection = triple(`${fixtureId}:projection`), issuerProof = triple(`${fixtureId}:issuer-proof`), evaluatorProof = triple(`${fixtureId}:evaluator-proof`), issuerNonce = triple(`${fixtureId}:issuer-nonce`), evaluatorNonce = triple(`${fixtureId}:evaluator-nonce`), readSet = triple(`${fixtureId}:read-set`), target = triple(`${fixtureId}:target-evidence`)
    const artifact = makeArtifact(r44, fixtureId, 'session_hold_evidence_schema', 'verified_consuming', { evidence_kind: 'verified_consuming', operation_name: operationName, operation_id: operationId, hold_branch: resultBranch, bundle_ref: bundle.ref, bundle_bytes_sha256: bundle.bytes_sha256, bundle_fingerprint: bundle.fingerprint, bundle_truth_projection_ref: projection.ref, bundle_truth_projection_bytes_sha256: projection.bytes_sha256, bundle_truth_projection_fingerprint: projection.fingerprint, issuer_proof_ref: issuerProof.ref, issuer_proof_bytes_sha256: issuerProof.bytes_sha256, issuer_proof_fingerprint: issuerProof.fingerprint, evaluator_proof_ref: evaluatorProof.ref, evaluator_proof_bytes_sha256: evaluatorProof.bytes_sha256, evaluator_proof_fingerprint: evaluatorProof.fingerprint, issuer_nonce_receipt_ref: issuerNonce.ref, issuer_nonce_receipt_fingerprint: issuerNonce.fingerprint, evaluator_nonce_receipt_ref: evaluatorNonce.ref, evaluator_nonce_receipt_fingerprint: evaluatorNonce.fingerprint, authority_read_set_ref: readSet.ref, authority_read_set_bytes_sha256: readSet.bytes_sha256, authority_read_set_fingerprint: readSet.fingerprint, partition_head_fingerprint: shaBytes(`${fixtureId}:head`), target_evidence_ref: target.ref, target_evidence_bytes_sha256: target.bytes_sha256, target_evidence_fingerprint: target.fingerprint })
    return { artifact, proof_evidence: { issuer_proof: issuerProof, evaluator_proof: evaluatorProof, issuer_nonce_receipt: issuerNonce, evaluator_nonce_receipt: evaluatorNonce, exact_nonce_count: 2 } }
  }
  const artifact = makeArtifact(r44, fixtureId, 'session_hold_evidence_schema', 'raw_non_consuming', { evidence_kind: 'raw_non_consuming', operation_name: operationName, operation_id: operationId, hold_branch: resultBranch, raw_target_ref_or_unavailable: raw.target.ref, raw_target_sha256_or_unavailable: raw.target.bytes_sha256, raw_bundle_ref_or_unavailable: raw.bundle.ref, raw_bundle_sha256_or_unavailable: raw.bundle.bytes_sha256, raw_issuer_proof_ref_or_unavailable: raw.issuer.ref, raw_issuer_proof_sha256_or_unavailable: raw.issuer.bytes_sha256, raw_evaluator_proof_ref_or_unavailable: raw.evaluator.ref, raw_evaluator_proof_sha256_or_unavailable: raw.evaluator.bytes_sha256 })
  return { artifact, proof_evidence: { issuer_proof: 'UNAVAILABLE', evaluator_proof: raw.evaluator, issuer_nonce_receipt: 'UNAVAILABLE', evaluator_nonce_receipt: 'UNAVAILABLE', exact_nonce_count: 0 } }
}
const unavailable = { ref: 'UNAVAILABLE', bytes_sha256: 'UNAVAILABLE', fingerprint: 'UNAVAILABLE' }
const fixtures = plans.map(([fixtureId, operationName, resultBranch, registryVariant, holdVariant]) => {
  const operationId = `op_${fixtureId}`, idempotencyKey = shaBytes(`${fixtureId}:idempotency`), requestFingerprint = shaBytes(`${fixtureId}:request-fingerprint`), request = triple(`${fixtureId}:request`), result = triple(`${fixtureId}:result`), selected = selectionFor(operationName, resultBranch), target_store = targetStore(operationName), heldAt = '2026-09-14T00:00:00.000Z'
  const history = historyArtifact(fixtureId, operationName, resultBranch, operationId, result)
  const raw = holdVariant === 'session_dual_proof' && selected.evidence_kind === 'raw_session_hold_evidence' ? { target: unavailable, bundle: triple(`${fixtureId}:raw-bundle`), issuer: unavailable, evaluator: triple(`${fixtureId}:raw-evaluator`) } : { target: unavailable, bundle: unavailable, issuer: unavailable, evaluator: unavailable }
  const sessionEvidence = holdVariant === 'session_dual_proof' ? sessionEvidenceArtifact(fixtureId, operationName, resultBranch, operationId, selected.evidence_kind, raw) : 'UNAVAILABLE'
  const sessionTriple = sessionEvidence === 'UNAVAILABLE' ? unavailable : { ref: sessionEvidence.artifact.content_addressed_artifact_ref, bytes_sha256: sessionEvidence.artifact.canonical_row_bytes_sha256, fingerprint: sessionEvidence.artifact.recorded_fingerprint }
  const common = { target_store, operation_name: operationName, operation_id: operationId, idempotency_key: idempotencyKey, request_fingerprint: requestFingerprint }
  let hold = 'UNAVAILABLE'
  if (holdVariant !== 'UNAVAILABLE') {
    const rawOverrides = holdVariant === 'ordinary_single_proof'
      ? { raw_target_availability: 'unavailable', raw_target_ref_or_unavailable: 'UNAVAILABLE', raw_target_sha256_or_unavailable: 'UNAVAILABLE', raw_proof_availability: 'unavailable', raw_proof_ref_or_unavailable: 'UNAVAILABLE', raw_proof_sha256_or_unavailable: 'UNAVAILABLE' }
      : { raw_target_availability: raw.target.ref === 'UNAVAILABLE' ? 'unavailable' : 'available', raw_target_ref_or_unavailable: raw.target.ref, raw_target_sha256_or_unavailable: raw.target.bytes_sha256, raw_bundle_availability: raw.bundle.ref === 'UNAVAILABLE' ? 'unavailable' : 'available', raw_bundle_ref_or_unavailable: raw.bundle.ref, raw_bundle_sha256_or_unavailable: raw.bundle.bytes_sha256, raw_issuer_proof_availability: raw.issuer.ref === 'UNAVAILABLE' ? 'unavailable' : 'available', raw_issuer_proof_ref_or_unavailable: raw.issuer.ref, raw_issuer_proof_sha256_or_unavailable: raw.issuer.bytes_sha256, raw_evaluator_proof_availability: raw.evaluator.ref === 'UNAVAILABLE' ? 'unavailable' : 'available', raw_evaluator_proof_ref_or_unavailable: raw.evaluator.ref, raw_evaluator_proof_sha256_or_unavailable: raw.evaluator.bytes_sha256, session_hold_evidence_ref: sessionTriple.ref, session_hold_evidence_bytes_sha256: sessionTriple.bytes_sha256, session_hold_evidence_fingerprint: sessionTriple.fingerprint }
    hold = makeArtifact(r44, fixtureId, 'authority_operation_hold_store.row_union', holdVariant, { ...common, ...selected, hold_branch: resultBranch, request_artifact_ref: request.ref, request_artifact_sha256: request.bytes_sha256, result_ref: result.ref, result_bytes_sha256: result.bytes_sha256, result_fingerprint: result.fingerprint, held_at: heldAt, ...rawOverrides }, true)
  }
  const registryOverrides = registryVariant === 'original_committed'
    ? { ...common, ...selected, request_bytes_ref: request.ref, request_bytes_sha256: request.bytes_sha256, target_intent_bytes_ref: shaBytes(`${fixtureId}:target-intent-ref`), target_intent_bytes_sha256: shaBytes(`${fixtureId}:target-intent-bytes`), committed_target_row_ref: shaBytes(`${fixtureId}:target-row-ref`), committed_target_row_bytes_sha256: shaBytes(`${fixtureId}:target-row-bytes`), committed_target_row_fingerprint: shaBytes(`${fixtureId}:target-row-fp`), result_branch: resultBranch, result_ref: result.ref, result_bytes_sha256: result.bytes_sha256, result_fingerprint: result.fingerprint, receipt_ref: shaBytes(`${fixtureId}:receipt-ref`), receipt_precommit_fingerprint: shaBytes(`${fixtureId}:receipt-precommit`), receipt_fingerprint: shaBytes(`${fixtureId}:receipt-fp`), server_committed_at: heldAt, historical_response_ref: history.content_addressed_artifact_ref, historical_response_bytes_sha256: history.canonical_row_bytes_sha256, historical_response_fingerprint: history.recorded_fingerprint }
    : { ...common, ...selected, request_bytes_ref: request.ref, request_bytes_sha256: request.bytes_sha256, hold_branch: resultBranch, raw_input_artifact_refs_fingerprint: shaBytes(`${fixtureId}:raw-input-set`), hold_result_ref: result.ref, hold_result_bytes_sha256: result.bytes_sha256, hold_result_fingerprint: result.fingerprint, held_at: heldAt, hold_fingerprint: hold.recorded_fingerprint, hold_schema_ref: `authority_operation_hold_store.row_union.variants.${holdVariant}`, historical_response_ref: history.content_addressed_artifact_ref, historical_response_bytes_sha256: history.canonical_row_bytes_sha256, historical_response_fingerprint: history.recorded_fingerprint, hold_row_ref: hold.canonical_row_value.hold_row_ref, session_hold_evidence_ref_or_unavailable: sessionTriple.ref, session_hold_evidence_bytes_sha256_or_unavailable: sessionTriple.bytes_sha256, session_hold_evidence_fingerprint_or_unavailable: sessionTriple.fingerprint }
  const registry = makeArtifact(r44, fixtureId, 'authority_operation_registry.row_union', registryVariant, registryOverrides, true)
  const payload = payloadArtifact(fixtureId, operationName, resultBranch, operationId, result, history, registry, hold)
  const envelope = envelopeArtifact(fixtureId, operationName, resultBranch, operationId, history, payload, hold !== 'UNAVAILABLE')
  const classifierRow = r44.authority_operation_replay_classifier.rows.find(row => row.expected_selection_row_id === selected.fresh_selection_row_id && row.operation_name === operationName && row.result_branch === resultBranch)
  return { fixture_id: fixtureId, operation_name: operationName, result_branch: resultBranch, request_fingerprint: requestFingerprint, request_artifact: request, result_artifact: result, historical_response_artifact: history, registry_artifact: registry, hold_artifact_or_UNAVAILABLE: hold, session_evidence_or_UNAVAILABLE: sessionEvidence, replay_payload_artifact: payload, replay_envelope_artifact: envelope, classifier_row: classifierRow, proof_and_nonce_evidence: sessionEvidence === 'UNAVAILABLE' ? { issuer_proof: 'UNAVAILABLE', evaluator_proof: 'UNAVAILABLE', issuer_nonce_receipt: 'UNAVAILABLE', evaluator_nonce_receipt: 'UNAVAILABLE', exact_nonce_count: 0 } : sessionEvidence.proof_evidence, expected_reconstructed_selection: selected }
})

r44.authority_operation_restart_correlation_authority = {
  schema_version: 'ctrl.g24.authority-operation-restart-correlation-authority.r44.v1',
  evaluation: 'all_applicable_rows_before_issue_commit_or_replay_first_mismatch_holds_without_disclosure_or_write',
  rows: [
    { correlation_id: 'request', applies_to: 'all', exact_equalities: ['fixture.request_fingerprint=registry.request_fingerprint', 'fixture.request_artifact.ref=registry.request_bytes_ref', 'fixture.request_artifact.bytes_sha256=registry.request_bytes_sha256', 'held.fixture.request_artifact.ref=hold.request_artifact_ref', 'held.fixture.request_artifact.bytes_sha256=hold.request_artifact_sha256'] },
    { correlation_id: 'operation_identity', applies_to: 'all', exact_equalities: ['fixture.operation_name=registry.operation_name=history.operation_name=payload.operation_name=envelope.operation_name', 'registry.operation_id=history.operation_id=payload.operation_id=envelope.operation_id', 'held.registry.operation_id=hold.operation_id', 'held.registry.idempotency_key=hold.idempotency_key'] },
    { correlation_id: 'selection', applies_to: 'all', exact_equalities: ['registry.selection_five=selected_fresh_row.selection_five=classifier.selection_five', 'held.registry.selection_five=hold.selection_five'] },
    { correlation_id: 'result_and_history', applies_to: 'all', exact_equalities: ['registry.result_triple=fixture.result_artifact', 'history.result_triple=fixture.result_artifact', 'history.response_payload_ref_and_hash=fixture.result_ref_and_hash', 'payload.historical_result_triple=fixture.result_artifact', 'registry.history_triple=stored_history_artifact'] },
    { correlation_id: 'hold', applies_to: 'held', exact_equalities: ['registry.hold_ref_and_fingerprint=hold.hold_ref_and_fingerprint', 'registry.hold_result_triple=hold.result_triple=fixture.result_artifact', 'registry.held_at=hold.held_at=payload.held_at'] },
    { correlation_id: 'session_evidence', applies_to: 'session_held', exact_equalities: ['registry.session_evidence_triple=hold.session_evidence_triple=stored_session_evidence_artifact', 'verified_session_evidence.proof_and_two_nonce_receipts=fixture.proof_and_nonce_evidence', 'raw_session_evidence.raw_role_slots=hold.raw_role_slots', 'raw_session_evidence.exact_nonce_count=0', 'verified_session_evidence.exact_nonce_count=2'] },
    { correlation_id: 'payload', applies_to: 'all', exact_equalities: ['payload.registry_ref_and_fingerprint=registry.row_ref_and_fingerprint', 'payload.history_triple=stored_history_artifact', 'held.payload.hold_ref_and_fingerprint=hold.row_ref_and_fingerprint', 'payload.result_triple=fixture.result_artifact'] },
    { correlation_id: 'envelope', applies_to: 'all', exact_equalities: ['envelope.payload_triple=stored_payload_artifact', 'envelope.response_triple=stored_history_artifact', 'envelope.operation_branch_kind=payload.operation_branch_kind'] },
  ],
  forbidden: ['cross_row_request_splice', 'resealed_artifact_splice', 'history_splice', 'result_splice', 'evidence_splice', 'nonce_or_proof_splice', 'payload_or_envelope_splice'],
}
function assertEqual(label, ...values) { const first = canonical(values[0]); for (let index = 1; index < values.length; index += 1) if (canonical(values[index]) !== first) throw new Error(`correlation:${label}`) }
function selection(row) { return { fresh_selection_row_id: row.fresh_selection_row_id, proof_family: row.proof_family, branch_class: row.branch_class, evidence_kind: row.evidence_kind, fresh_selection_schema_version: row.fresh_selection_schema_version } }
function validateCorrelations(fixture) {
  const registry = fixture.registry_artifact.canonical_row_value, history = fixture.historical_response_artifact, historyRow = history.canonical_row_value, payload = fixture.replay_payload_artifact, payloadRow = payload.canonical_row_value, envelopeRow = fixture.replay_envelope_artifact.canonical_row_value, result = fixture.result_artifact
  assertEqual('request fingerprint', fixture.request_fingerprint, registry.request_fingerprint)
  assertEqual('request ref', fixture.request_artifact.ref, registry.request_bytes_ref)
  assertEqual('request hash', fixture.request_artifact.bytes_sha256, registry.request_bytes_sha256)
  assertEqual('operation', fixture.operation_name, registry.operation_name, historyRow.operation_name, payloadRow.operation_name, envelopeRow.operation_name)
  assertEqual('operation id', registry.operation_id, historyRow.operation_id, payloadRow.operation_id, envelopeRow.operation_id)
  assertEqual('branch', fixture.result_branch, historyRow.result_branch, payloadRow.historical_result_branch, envelopeRow.historical_result_branch)
  assertEqual('result ref', result.ref, historyRow.result_ref, historyRow.response_payload_ref, payloadRow.historical_result_ref, registry.result_ref ?? registry.hold_result_ref)
  assertEqual('result bytes', result.bytes_sha256, historyRow.result_bytes_sha256, historyRow.response_payload_bytes_sha256, payloadRow.historical_result_bytes_sha256, registry.result_bytes_sha256 ?? registry.hold_result_bytes_sha256)
  assertEqual('result fingerprint', result.fingerprint, historyRow.result_fingerprint, payloadRow.historical_result_fingerprint, registry.result_fingerprint ?? registry.hold_result_fingerprint)
  assertEqual('history ref', history.content_addressed_artifact_ref, registry.historical_response_ref, payloadRow.stored_historical_response_ref, envelopeRow.replay_response_ref)
  assertEqual('history bytes', history.canonical_row_bytes_sha256, registry.historical_response_bytes_sha256, payloadRow.stored_historical_response_bytes_sha256, envelopeRow.replay_response_bytes_sha256)
  assertEqual('history fingerprint', history.recorded_fingerprint, registry.historical_response_fingerprint, payloadRow.stored_historical_response_fingerprint, envelopeRow.replay_response_fingerprint)
  assertEqual('payload ref', payload.content_addressed_artifact_ref, envelopeRow.payload_ref)
  assertEqual('payload bytes', payload.canonical_row_bytes_sha256, envelopeRow.payload_bytes_sha256)
  assertEqual('payload fingerprint', payload.recorded_fingerprint, envelopeRow.payload_fingerprint)
  assertEqual('selection', selection(registry), fixture.expected_reconstructed_selection)
  const classifier = fixture.classifier_row
  if (!classifier || classifier.expected_selection_row_id !== registry.fresh_selection_row_id || classifier.expected_proof_family !== registry.proof_family || classifier.expected_branch_class !== registry.branch_class || classifier.expected_evidence_kind !== registry.evidence_kind || classifier.expected_selector_version !== registry.fresh_selection_schema_version) throw new Error(`classifier:${fixture.fixture_id}`)
  if (fixture.hold_artifact_or_UNAVAILABLE === 'UNAVAILABLE') {
    assertEqual('committed payload registry ref', registry.registry_row_ref, payloadRow.committed_registry_row_ref)
    assertEqual('committed payload registry fingerprint', registry.registry_fingerprint, payloadRow.committed_registry_row_fingerprint)
    if (fixture.proof_and_nonce_evidence.exact_nonce_count !== 0) throw new Error(`committed_fixture_nonce:${fixture.fixture_id}`)
    return
  }
  const hold = fixture.hold_artifact_or_UNAVAILABLE.canonical_row_value
  assertEqual('held request ref', fixture.request_artifact.ref, hold.request_artifact_ref)
  assertEqual('held request hash', fixture.request_artifact.bytes_sha256, hold.request_artifact_sha256)
  assertEqual('held operation', registry.operation_id, hold.operation_id)
  assertEqual('held idempotency', registry.idempotency_key, hold.idempotency_key)
  assertEqual('held selection', selection(registry), selection(hold))
  assertEqual('held row ref', registry.hold_row_ref, hold.hold_row_ref, payloadRow.hold_row_ref)
  assertEqual('held fingerprint', registry.hold_fingerprint, hold.hold_fingerprint, payloadRow.hold_row_fingerprint)
  assertEqual('held timestamp', registry.held_at, hold.held_at, payloadRow.held_at)
  assertEqual('held registry ref', registry.registry_row_ref, payloadRow.held_registry_row_ref)
  assertEqual('held registry fingerprint', registry.registry_fingerprint, payloadRow.held_registry_row_fingerprint)
  if (hold.hold_kind === 'session_dual_proof') {
    const evidence = fixture.session_evidence_or_UNAVAILABLE.artifact, evidenceRow = evidence.canonical_row_value
    assertEqual('session evidence ref', evidence.content_addressed_artifact_ref, registry.session_hold_evidence_ref_or_unavailable, hold.session_hold_evidence_ref, payloadRow.session_hold_evidence_ref_or_unavailable)
    assertEqual('session evidence bytes', evidence.canonical_row_bytes_sha256, registry.session_hold_evidence_bytes_sha256_or_unavailable, hold.session_hold_evidence_bytes_sha256, payloadRow.session_hold_evidence_bytes_sha256_or_unavailable)
    assertEqual('session evidence fp', evidence.recorded_fingerprint, registry.session_hold_evidence_fingerprint_or_unavailable, hold.session_hold_evidence_fingerprint, payloadRow.session_hold_evidence_fingerprint_or_unavailable)
    if (registry.evidence_kind === 'verified_session_hold_evidence') { if (fixture.proof_and_nonce_evidence.exact_nonce_count !== 2 || evidenceRow.issuer_proof_ref !== fixture.proof_and_nonce_evidence.issuer_proof.ref || evidenceRow.evaluator_proof_ref !== fixture.proof_and_nonce_evidence.evaluator_proof.ref || evidenceRow.issuer_nonce_receipt_ref !== fixture.proof_and_nonce_evidence.issuer_nonce_receipt.ref || evidenceRow.evaluator_nonce_receipt_ref !== fixture.proof_and_nonce_evidence.evaluator_nonce_receipt.ref) throw new Error(`verified_session_evidence:${fixture.fixture_id}`) }
    else { if (fixture.proof_and_nonce_evidence.exact_nonce_count !== 0 || evidenceRow.raw_bundle_ref_or_unavailable !== hold.raw_bundle_ref_or_unavailable || evidenceRow.raw_evaluator_proof_ref_or_unavailable !== hold.raw_evaluator_proof_ref_or_unavailable) throw new Error(`raw_session_evidence:${fixture.fixture_id}`) }
  } else if (registry.session_hold_evidence_ref_or_unavailable !== 'UNAVAILABLE') throw new Error(`ordinary_session_evidence:${fixture.fixture_id}`)
}
for (const fixture of fixtures) validateCorrelations(fixture)
for (const field of ['operation_id', 'idempotency_key', 'registry_row_ref']) { const values = fixtures.map(fixture => fixture.registry_artifact.canonical_row_value[field]); if (new Set(values).size !== fixtures.length) throw new Error(`fixture_unique:${field}`) }
r44.authority_operation_replay_restart_fixtures = { schema_version: 'ctrl.g24.authority-operation-replay-restart-fixtures.r44.v1', registry_and_hold_artifact_count: 7, complete_lineage_artifact_count: fixtures.reduce((count, fixture) => count + 4 + (fixture.hold_artifact_or_UNAVAILABLE === 'UNAVAILABLE' ? 0 : 1) + (fixture.session_evidence_or_UNAVAILABLE === 'UNAVAILABLE' ? 0 : 1), 0), exact_fixture_ids: plans.map(row => row[0]), fixtures, normative_row_ref_derivation_ref: 'normative_row_ref_derivation', correlation_authority_ref: 'authority_operation_restart_correlation_authority', reconstruction_algorithm: ['owned_bounded_immutable_snapshot', 'validate_exact_selected_schema', 'derive_normative_row_ref_from_selected_schema_declaration', 'derive_fingerprint_then_final_bytes_hash', 'resolve_and_validate_complete_correlation_table', 'resolve_classifier_from_durable_selection'], any_mismatch_splice_or_instability: 'hold_without_disclosure_or_write' }
r44.fixture_schema_validator = { schema_version: 'ctrl.g24.fixture-schema-validator.r44.v1', supported_schema_forms: ['const', 'enum_array', 'typed_enum_values', 'sha256', 'sha256_or_exact_literal', 'identifier', 'canonical_timestamp', 'nullable', 'integer', 'boolean', 'array', 'schema_ref', 'discriminated_variant'], validation_scope: 'all_7_registry_and_hold_rows_plus_history_payload_envelope_and_session_evidence_artifacts_and_full_cross_record_correlation_table', unsupported_schema_form: 'reject_fixture_materialization' }

// R44 replaces the inherited bootstrap triples above with rows selected from the real
// content-addressed stores. The bootstrap objects never enter the effective output.
function filled(schema, seed, overrides = {}) { const row = {}; for (const [field, spec] of Object.entries(schema.properties)) row[field] = valueForSpec(spec, `${seed}:${field}`); Object.assign(row, overrides); return row }
function resultFingerprint(operationName, schema, row) { const payload = {}; for (const field of schema.exact_keys) if (field !== 'result_fingerprint') payload[field] = row[field]; const rule = r44.case_session_authority_operation_protocols.operations[operationName].result_fingerprint; return hashCanonical({ domain_ascii: rule.domain_ascii, operation_name: row.operation_name, operation_id: row.operation_id, branch: row.branch, branch_specific_canonical_payload_sha256: hashCanonical(payload) }) }
function storeFor(family, schemaRef) {
  const grouped = r44.authority_operation_artifact_stores?.families?.[family]
  if (grouped) { const selected = Object.values(grouped.stores_by_schema_ref).find(row => row.canonical_schema_ref === schemaRef); if (!selected) throw new Error(`selected_store:${family}:${schemaRef}`); return selected }
  const names = { bundle: 'session_dual_proof_bundle_artifact_store', projection: 'session_dual_proof_bundle_truth_projection_artifact_store', read_set: 'case_session_authority_read_set_artifact_store', session_evidence: 'session_hold_evidence_artifact_store', history: 'authority_operation_historical_response_artifact_store', replay_payload: 'authority_operation_replay_payload_artifact_store', replay_envelope: 'authority_operation_replay_envelope_artifact_store' }
  const selected = r44[names[family]]; if (!selected || selected.canonical_schema_ref !== schemaRef) throw new Error(`selected_store:${family}:${schemaRef}`); return selected
}
function contentArtifact(fixtureId, role, family, schemaRef, variant, overrides = {}, explicitParsedFingerprint = undefined) {
  const payloadSchema = schemaAt(r44, schemaRef, variant), payload = filled(payloadSchema, `${fixtureId}:${role}`, overrides)
  let parsedFingerprint = explicitParsedFingerprint
  if (payloadSchema.fingerprint_field) { const derived = fingerprintFor(r44, payloadSchema, payload); payload[payloadSchema.fingerprint_field] = derived.fingerprint; parsedFingerprint = derived.fingerprint }
  if (role === 'result') { payload.result_fingerprint = resultFingerprint(payload.operation_name, payloadSchema, payload); parsedFingerprint = payload.result_fingerprint }
  validateRow(payloadSchema, payload, `${fixtureId}:${role}:payload`)
  const payloadBytes = canonical(payload), payloadHash = shaBytes(Buffer.from(payloadBytes, 'utf8'))
  if (!parsedFingerprint) parsedFingerprint = hashCanonical({ domain_ascii: 'CTRL-G24-R44-PARSED-CONTENT', schema_ref: schemaRef, canonical_bytes_sha256: payloadHash })
  const selectedStore = storeFor(family, schemaRef), wrapperSchema = selectedStore.row_schema
  const wrapper = filled(wrapperSchema, `${fixtureId}:${role}:store`, { artifact_ref: payloadHash, canonical_schema_ref: schemaRef, canonical_bytes_b64url: Buffer.from(payloadBytes, 'utf8').toString('base64url'), canonical_bytes_length: Buffer.byteLength(payloadBytes, 'utf8'), canonical_bytes_sha256: payloadHash, parsed_content_fingerprint: parsedFingerprint, stored_at: '2026-09-14T00:00:00.000Z' })
  const wrapperFingerprint = fingerprintFor(r44, wrapperSchema, wrapper); wrapper[wrapperSchema.fingerprint_field] = wrapperFingerprint.fingerprint; validateRow(wrapperSchema, wrapper, `${fixtureId}:${role}:stored_row`)
  return { artifact_role: role, selected_store_schema_version: selectedStore.schema_version ?? wrapperSchema.schema_version, stored_row_schema_version: wrapperSchema.schema_version, payload_schema_ref: schemaRef, payload_schema_variant: variant, payload_schema_version: payloadSchema.schema_version, payload_value: payload, payload_canonical_bytes_utf8: payloadBytes, payload_bytes_sha256: payloadHash, payload_fingerprint: parsedFingerprint, ref: payloadHash, bytes_sha256: payloadHash, fingerprint: parsedFingerprint, stored_row_value: wrapper, stored_row_canonical_bytes_sha256: hashCanonical(wrapper), stored_row_fingerprint: wrapperFingerprint.fingerprint, content_address_rule_verified: wrapper.artifact_ref === wrapper.canonical_bytes_sha256 && wrapper.artifact_ref === payloadHash }
}
function opaqueArtifact(fixtureId, role, slot, bytesText) { const selectedStore = r44.authority_opaque_raw_input_stores[slot], schema = selectedStore.row_schema, bytes = Buffer.from(bytesText, 'utf8'), bytesHash = shaBytes(bytes), row = filled(schema, `${fixtureId}:${role}`, { artifact_ref: bytesHash, opaque_bytes_b64url: bytes.toString('base64url'), opaque_bytes_length: bytes.length, opaque_bytes_sha256: bytesHash, stored_at: '2026-09-14T00:00:00.000Z' }); const derived = fingerprintFor(r44, schema, row); row[schema.fingerprint_field] = derived.fingerprint; validateRow(schema, row, `${fixtureId}:${role}:opaque`); return { artifact_role: role, selected_store_schema_version: selectedStore.schema_version, stored_row_schema_version: schema.schema_version, payload_schema_ref: 'opaque_bounded_bytes', payload_schema_variant: slot, payload_schema_version: 'ctrl.g24.opaque-bounded-bytes.r44.v1', payload_value: bytesText, payload_canonical_bytes_utf8: bytesText, payload_bytes_sha256: bytesHash, payload_fingerprint: derived.fingerprint, ref: bytesHash, bytes_sha256: bytesHash, fingerprint: derived.fingerprint, stored_row_value: row, stored_row_canonical_bytes_sha256: hashCanonical(row), stored_row_fingerprint: derived.fingerprint, content_address_rule_verified: row.artifact_ref === row.opaque_bytes_sha256 } }
function nonceArtifact(fixtureId, role, proofFamily, proof, operationName, operationId, branch, nonce, subject, verifier) { const payloadSchema = r44.proof_nonce_receipt_payload_schema, payload = filled(payloadSchema, `${fixtureId}:${role}`, { proof_family: proofFamily, nonce_subject_fingerprint: subject, nonce, proof_fingerprint: proof.fingerprint, verifier_identity_fingerprint: verifier, target_store: targetStore(operationName), operation_id: operationId, consumed_on_branch: branch, consumed_at: '2026-09-14T00:00:00.000Z' }); validateRow(payloadSchema, payload, `${fixtureId}:${role}:payload`); const payloadBytes = canonical(payload), receiptRef = shaBytes(Buffer.from(payloadBytes, 'utf8')), rowSchema = r44.proof_nonce_ledger.row_schema, row = { ...payload, nonce_receipt_fingerprint: shaBytes(`${fixtureId}:${role}:pending`), nonce_receipt_ref: receiptRef }; const derived = fingerprintFor(r44, rowSchema, row); row.nonce_receipt_fingerprint = derived.fingerprint; validateRow(rowSchema, row, `${fixtureId}:${role}:row`); return { artifact_role: role, selected_store_schema_version: r44.proof_nonce_receipt_store.schema_version, stored_row_schema_version: rowSchema.schema_version, payload_schema_ref: 'proof_nonce_receipt_payload_schema', payload_schema_variant: 'UNAVAILABLE', payload_schema_version: payloadSchema.schema_version, payload_value: payload, payload_canonical_bytes_utf8: payloadBytes, payload_bytes_sha256: receiptRef, payload_fingerprint: derived.fingerprint, ref: receiptRef, bytes_sha256: receiptRef, fingerprint: derived.fingerprint, stored_row_value: row, stored_row_canonical_bytes_sha256: hashCanonical(row), stored_row_fingerprint: derived.fingerprint, content_address_rule_verified: row.nonce_receipt_ref === receiptRef } }
function requestFingerprint(operationName, payload) { const rule = r44.case_session_authority_operation_protocols.operations[operationName].request_fingerprint, preimage = {}; for (const field of rule.preimage_order) preimage[field] = field === 'domain_ascii' ? rule.domain_ascii : payload[field]; return hashCanonical(preimage) }
function makeRequestArtifact(fixtureId, operationName, overrides) { const schemaRef = `case_session_authority_operation_protocols.operations.${operationName}.request_schema`, schema = schemaAt(r44, schemaRef), payload = filled(schema, `${fixtureId}:request`, overrides), fpValue = requestFingerprint(operationName, payload); return contentArtifact(fixtureId, 'request', 'requests', schemaRef, 'UNAVAILABLE', payload, fpValue) }
function preliminaryHoldRef(fixtureId, holdVariant, overrides) { const schema = schemaAt(r44, 'authority_operation_hold_store.row_union', holdVariant), row = filled(schema, `${fixtureId}:hold-precommit`, overrides); return deriveNormativeRowRef(schema, row) }
function actualFixture([fixtureId, operationName, resultBranch, registryVariant, holdVariant]) {
  const operation = r44.case_session_authority_operation_protocols.operations[operationName], operationId = `op_${fixtureId}`, idempotencyKey = shaBytes(`${fixtureId}:idempotency`), selected = selectionFor(operationName, resultBranch), store = targetStore(operationName), at = '2026-09-14T00:00:00.000Z', schemaTarget = `case_session_authority_operation_protocols.operations.${operationName}.target_intent_schema`
  const target = contentArtifact(fixtureId, 'target', 'targets', schemaTarget, 'UNAVAILABLE', {})
  let proof = 'UNAVAILABLE', issuerProof = 'UNAVAILABLE', evaluatorProof = 'UNAVAILABLE', bundle = 'UNAVAILABLE', projection = 'UNAVAILABLE', issuerNonce = 'UNAVAILABLE', evaluatorNonce = 'UNAVAILABLE', readSet = 'UNAVAILABLE', rawBundle = 'UNAVAILABLE', rawEvaluator = 'UNAVAILABLE'
  if (holdVariant !== 'session_dual_proof') proof = contentArtifact(fixtureId, 'proof', 'proofs', 'case_session_root_bootstrap_proof_schema', 'UNAVAILABLE', { target_intent_sha256: target.bytes_sha256, target_partition_fingerprint: shaBytes(`${fixtureId}:partition`) })
  else if (selected.evidence_kind === 'verified_session_hold_evidence') {
    const proofCommon = { scope_operation_name: operationName, scope_target_store: store, scope_partition_fingerprint: shaBytes(`${fixtureId}:partition`), audience: 'ctrl_case_session_authority_operation' }
    issuerProof = contentArtifact(fixtureId, 'issuer_proof', 'proofs', 'case_session_issuer_capability_proof_schema', 'UNAVAILABLE', proofCommon)
    evaluatorProof = contentArtifact(fixtureId, 'evaluator_proof', 'proofs', 'case_session_evaluator_capability_proof_schema', 'UNAVAILABLE', proofCommon)
    const bundleOverrides = { operation_name: operationName, workspace_ref: 'workspace_restart', target_partition_fingerprint: shaBytes(`${fixtureId}:partition`), audience: 'ctrl_case_session_authority_operation', issuer_proof_ref: issuerProof.ref, issuer_proof_bytes_sha256: issuerProof.bytes_sha256, issuer_proof_fingerprint: issuerProof.fingerprint, issuer_nonce: issuerProof.payload_value.nonce, issuer_nonce_subject_fingerprint: shaBytes(`${fixtureId}:issuer-subject`), issuer_verifier_identity_fingerprint: shaBytes(`${fixtureId}:issuer-verifier`), evaluator_proof_ref: evaluatorProof.ref, evaluator_proof_bytes_sha256: evaluatorProof.bytes_sha256, evaluator_proof_fingerprint: evaluatorProof.fingerprint, evaluator_nonce: evaluatorProof.payload_value.nonce, evaluator_nonce_subject_fingerprint: shaBytes(`${fixtureId}:evaluator-subject`), evaluator_verifier_identity_fingerprint: shaBytes(`${fixtureId}:evaluator-verifier`) }
    bundle = contentArtifact(fixtureId, 'bundle', 'bundle', 'session_dual_proof_bundle_schema', 'UNAVAILABLE', bundleOverrides)
    projection = contentArtifact(fixtureId, 'bundle_truth_projection', 'projection', 'session_dual_proof_bundle_truth_projection_schema', 'UNAVAILABLE', { target_intent_bytes_ref: target.ref, target_intent_bytes_sha256: target.bytes_sha256, decoded_target_workspace_ref: 'workspace_restart', selected_current_partition_workspace_ref: 'workspace_restart', operation_name: operationName, workspace_ref: 'workspace_restart', target_partition_fingerprint: bundleOverrides.target_partition_fingerprint, audience: bundleOverrides.audience, ...bundleOverrides })
    issuerNonce = nonceArtifact(fixtureId, 'issuer_nonce_receipt', 'issuer', issuerProof, operationName, operationId, resultBranch, bundleOverrides.issuer_nonce, bundleOverrides.issuer_nonce_subject_fingerprint, bundleOverrides.issuer_verifier_identity_fingerprint)
    evaluatorNonce = nonceArtifact(fixtureId, 'evaluator_nonce_receipt', 'evaluator', evaluatorProof, operationName, operationId, resultBranch, bundleOverrides.evaluator_nonce, bundleOverrides.evaluator_nonce_subject_fingerprint, bundleOverrides.evaluator_verifier_identity_fingerprint)
    readSet = contentArtifact(fixtureId, 'authority_read_set', 'read_set', 'case_session_authority_read_set_schema', 'session_dual_proof', { read_set_kind: 'session_dual_proof', workspace_ref: 'workspace_restart', dual_proof_bundle_ref: bundle.ref, dual_proof_bundle_bytes_sha256: bundle.bytes_sha256, dual_proof_bundle_fingerprint: bundle.fingerprint, issuer_proof_ref: issuerProof.ref, issuer_proof_bytes_sha256: issuerProof.bytes_sha256, issuer_proof_fingerprint: issuerProof.fingerprint, issuer_nonce_receipt_ref: issuerNonce.ref, issuer_nonce_receipt_fingerprint: issuerNonce.fingerprint, evaluator_proof_ref: evaluatorProof.ref, evaluator_proof_bytes_sha256: evaluatorProof.bytes_sha256, evaluator_proof_fingerprint: evaluatorProof.fingerprint, evaluator_nonce_receipt_ref: evaluatorNonce.ref, evaluator_nonce_receipt_fingerprint: evaluatorNonce.fingerprint, bundle_truth_projection_ref: projection.ref, bundle_truth_projection_bytes_sha256: projection.bytes_sha256, bundle_truth_projection_fingerprint: projection.fingerprint })
  } else { rawBundle = opaqueArtifact(fixtureId, 'raw_bundle', 'proof_bundle', `${fixtureId}:malformed-bundle`); rawEvaluator = opaqueArtifact(fixtureId, 'raw_evaluator_proof', 'proof_evaluator', `${fixtureId}:malformed-evaluator`) }
  const requestOverrides = { operation_name: operationName, operation_id: operationId, idempotency_key: idempotencyKey, target_store: store, target_partition_fingerprint: shaBytes(`${fixtureId}:partition`), target_intent_schema_ref: schemaTarget, target_intent_bytes_ref: target.ref, target_intent_bytes_sha256: target.bytes_sha256, expected_head_row_version_ref: null, expected_head_order: 0, expected_head_row_fingerprint: null, expected_head_fingerprint: null, ...(holdVariant === 'session_dual_proof' ? { dual_proof_bundle_schema_ref: 'session_dual_proof_bundle_schema', dual_proof_bundle_bytes_ref: bundle === 'UNAVAILABLE' ? rawBundle.ref : bundle.ref, dual_proof_bundle_bytes_sha256: bundle === 'UNAVAILABLE' ? rawBundle.bytes_sha256 : bundle.bytes_sha256, dual_proof_bundle_fingerprint: bundle === 'UNAVAILABLE' ? rawBundle.fingerprint : bundle.fingerprint } : { authority_proof_schema_ref: 'case_session_root_bootstrap_proof_schema', authority_proof_bytes_ref: proof.ref, authority_proof_bytes_sha256: proof.bytes_sha256 }) }
  const request = makeRequestArtifact(fixtureId, operationName, requestOverrides), common = { target_store: store, operation_name: operationName, operation_id: operationId, idempotency_key: idempotencyKey, request_fingerprint: request.fingerprint }, unavailableTriple = { ref: 'UNAVAILABLE', bytes_sha256: 'UNAVAILABLE', fingerprint: 'UNAVAILABLE' }
  let evidence = 'UNAVAILABLE'
  if (holdVariant === 'session_dual_proof') {
    if (selected.evidence_kind === 'verified_session_hold_evidence') evidence = contentArtifact(fixtureId, 'session_evidence', 'session_evidence', 'session_hold_evidence_schema', 'verified_consuming', { evidence_kind: 'verified_consuming', operation_name: operationName, operation_id: operationId, hold_branch: resultBranch, bundle_ref: bundle.ref, bundle_bytes_sha256: bundle.bytes_sha256, bundle_fingerprint: bundle.fingerprint, bundle_truth_projection_ref: projection.ref, bundle_truth_projection_bytes_sha256: projection.bytes_sha256, bundle_truth_projection_fingerprint: projection.fingerprint, issuer_proof_ref: issuerProof.ref, issuer_proof_bytes_sha256: issuerProof.bytes_sha256, issuer_proof_fingerprint: issuerProof.fingerprint, evaluator_proof_ref: evaluatorProof.ref, evaluator_proof_bytes_sha256: evaluatorProof.bytes_sha256, evaluator_proof_fingerprint: evaluatorProof.fingerprint, issuer_nonce_receipt_ref: issuerNonce.ref, issuer_nonce_receipt_fingerprint: issuerNonce.fingerprint, evaluator_nonce_receipt_ref: evaluatorNonce.ref, evaluator_nonce_receipt_fingerprint: evaluatorNonce.fingerprint, authority_read_set_ref: readSet.ref, authority_read_set_bytes_sha256: readSet.bytes_sha256, authority_read_set_fingerprint: readSet.fingerprint, partition_head_fingerprint: shaBytes(`${fixtureId}:head`), target_evidence_ref: target.ref, target_evidence_bytes_sha256: target.bytes_sha256, target_evidence_fingerprint: target.fingerprint })
    else evidence = contentArtifact(fixtureId, 'session_evidence', 'session_evidence', 'session_hold_evidence_schema', 'raw_non_consuming', { evidence_kind: 'raw_non_consuming', operation_name: operationName, operation_id: operationId, hold_branch: resultBranch, raw_target_ref_or_unavailable: 'UNAVAILABLE', raw_target_sha256_or_unavailable: 'UNAVAILABLE', raw_bundle_ref_or_unavailable: rawBundle.ref, raw_bundle_sha256_or_unavailable: rawBundle.bytes_sha256, raw_issuer_proof_ref_or_unavailable: 'UNAVAILABLE', raw_issuer_proof_sha256_or_unavailable: 'UNAVAILABLE', raw_evaluator_proof_ref_or_unavailable: rawEvaluator.ref, raw_evaluator_proof_sha256_or_unavailable: rawEvaluator.bytes_sha256 })
  }
  const evidenceTriple = evidence === 'UNAVAILABLE' ? unavailableTriple : { ref: evidence.ref, bytes_sha256: evidence.bytes_sha256, fingerprint: evidence.fingerprint }
  const holdBase = holdVariant === 'UNAVAILABLE' ? null : { ...common, ...selected, hold_branch: resultBranch, request_artifact_ref: request.ref, request_artifact_sha256: request.bytes_sha256, result_ref: shaBytes(`${fixtureId}:pending-result-ref`), result_bytes_sha256: shaBytes(`${fixtureId}:pending-result-bytes`), result_fingerprint: shaBytes(`${fixtureId}:pending-result-fp`), held_at: at, ...(holdVariant === 'ordinary_single_proof' ? { raw_target_availability: 'unavailable', raw_target_ref_or_unavailable: 'UNAVAILABLE', raw_target_sha256_or_unavailable: 'UNAVAILABLE', raw_proof_availability: 'unavailable', raw_proof_ref_or_unavailable: 'UNAVAILABLE', raw_proof_sha256_or_unavailable: 'UNAVAILABLE' } : { raw_target_availability: 'unavailable', raw_target_ref_or_unavailable: 'UNAVAILABLE', raw_target_sha256_or_unavailable: 'UNAVAILABLE', raw_bundle_availability: rawBundle === 'UNAVAILABLE' ? 'unavailable' : 'available', raw_bundle_ref_or_unavailable: rawBundle === 'UNAVAILABLE' ? 'UNAVAILABLE' : rawBundle.ref, raw_bundle_sha256_or_unavailable: rawBundle === 'UNAVAILABLE' ? 'UNAVAILABLE' : rawBundle.bytes_sha256, raw_issuer_proof_availability: 'unavailable', raw_issuer_proof_ref_or_unavailable: 'UNAVAILABLE', raw_issuer_proof_sha256_or_unavailable: 'UNAVAILABLE', raw_evaluator_proof_availability: rawEvaluator === 'UNAVAILABLE' ? 'unavailable' : 'available', raw_evaluator_proof_ref_or_unavailable: rawEvaluator === 'UNAVAILABLE' ? 'UNAVAILABLE' : rawEvaluator.ref, raw_evaluator_proof_sha256_or_unavailable: rawEvaluator === 'UNAVAILABLE' ? 'UNAVAILABLE' : rawEvaluator.bytes_sha256, session_hold_evidence_ref: evidenceTriple.ref, session_hold_evidence_bytes_sha256: evidenceTriple.bytes_sha256, session_hold_evidence_fingerprint: evidenceTriple.fingerprint }) }
  const holdRowRef = holdBase ? preliminaryHoldRef(fixtureId, holdVariant, holdBase) : 'UNAVAILABLE'
  let receiptRef = shaBytes(`${fixtureId}:receipt-ref`), receiptPrecommit = shaBytes(`${fixtureId}:receipt-precommit`)
  const branchSelection = r44.authority_operation_fresh_branch_class_selection.rows.find(row => row.operation_name === operationName && row.result_branch === resultBranch), resultSchemaRef = branchSelection.result_schema_ref, resultOverrides = resultBranch === 'committed' ? { operation_name: operationName, operation_id: operationId, branch: resultBranch, target_store: store, target_row_version_ref: target.ref, target_row_fingerprint: target.fingerprint, receipt_ref: receiptRef, receipt_precommit_fingerprint: receiptPrecommit, committed_at: at } : { operation_name: operationName, operation_id: operationId, branch: resultBranch, hold_reason_code: resultBranch, held_at: at, hold_row_ref: holdRowRef, ...(holdVariant === 'session_dual_proof' ? { session_hold_evidence_ref: evidence.ref, session_hold_evidence_bytes_sha256: evidence.bytes_sha256, session_hold_evidence_fingerprint: evidence.fingerprint } : {}) }
  const result = contentArtifact(fixtureId, 'result', 'results', resultSchemaRef, 'UNAVAILABLE', resultOverrides)
  const history = contentArtifact(fixtureId, 'history', 'history', 'authority_operation_historical_response_schema', 'UNAVAILABLE', { operation_name: operationName, operation_id: operationId, result_branch: resultBranch, response_schema_ref: r44.authority_operation_historical_response_schema_matrix.rows.find(row => row.operation_name === operationName && row.result_branch === resultBranch).response_schema_ref, response_schema_version: r44.authority_operation_historical_response_schema_matrix.rows.find(row => row.operation_name === operationName && row.result_branch === resultBranch).response_schema_version, response_payload_ref: result.ref, response_payload_bytes_sha256: result.bytes_sha256, result_ref: result.ref, result_bytes_sha256: result.bytes_sha256, result_fingerprint: result.fingerprint })
  const hold = holdBase ? makeArtifact(r44, fixtureId, 'authority_operation_hold_store.row_union', holdVariant, { ...holdBase, result_ref: result.ref, result_bytes_sha256: result.bytes_sha256, result_fingerprint: result.fingerprint }, true) : 'UNAVAILABLE'
  const registryOverrides = registryVariant === 'original_committed' ? { ...common, ...selected, request_bytes_ref: request.ref, request_bytes_sha256: request.bytes_sha256, target_intent_bytes_ref: target.ref, target_intent_bytes_sha256: target.bytes_sha256, committed_target_row_ref: target.ref, committed_target_row_bytes_sha256: target.bytes_sha256, committed_target_row_fingerprint: target.fingerprint, result_branch: resultBranch, result_ref: result.ref, result_bytes_sha256: result.bytes_sha256, result_fingerprint: result.fingerprint, receipt_ref: receiptRef, receipt_precommit_fingerprint: receiptPrecommit, receipt_fingerprint: shaBytes(`${fixtureId}:receipt-final`), server_committed_at: at, historical_response_ref: history.ref, historical_response_bytes_sha256: history.bytes_sha256, historical_response_fingerprint: history.fingerprint } : { ...common, ...selected, request_bytes_ref: request.ref, request_bytes_sha256: request.bytes_sha256, hold_branch: resultBranch, raw_input_artifact_refs_fingerprint: hashCanonical([rawBundle === 'UNAVAILABLE' ? 'UNAVAILABLE' : rawBundle.ref, rawEvaluator === 'UNAVAILABLE' ? 'UNAVAILABLE' : rawEvaluator.ref]), hold_result_ref: result.ref, hold_result_bytes_sha256: result.bytes_sha256, hold_result_fingerprint: result.fingerprint, held_at: at, hold_fingerprint: hold.recorded_fingerprint, hold_schema_ref: branchSelection.hold_schema_ref, historical_response_ref: history.ref, historical_response_bytes_sha256: history.bytes_sha256, historical_response_fingerprint: history.fingerprint, hold_row_ref: hold.canonical_row_value.hold_row_ref, session_hold_evidence_ref_or_unavailable: evidenceTriple.ref, session_hold_evidence_bytes_sha256_or_unavailable: evidenceTriple.bytes_sha256, session_hold_evidence_fingerprint_or_unavailable: evidenceTriple.fingerprint }
  const registry = makeArtifact(r44, fixtureId, 'authority_operation_registry.row_union', registryVariant, registryOverrides, true)
  const payload = contentArtifact(fixtureId, 'replay_payload', 'replay_payload', 'authority_operation_replay_payload_schema', hold === 'UNAVAILABLE' ? 'replayed' : 'replayed_held', hold === 'UNAVAILABLE' ? { replay_kind: 'replayed', operation_name: operationName, operation_id: operationId, historical_result_branch: resultBranch, historical_result_ref: result.ref, historical_result_bytes_sha256: result.bytes_sha256, historical_result_fingerprint: result.fingerprint, stored_historical_response_ref: history.ref, stored_historical_response_bytes_sha256: history.bytes_sha256, stored_historical_response_fingerprint: history.fingerprint, committed_registry_row_ref: registry.canonical_row_value.registry_row_ref, committed_registry_row_fingerprint: registry.recorded_fingerprint } : { replay_kind: 'replayed_held', operation_name: operationName, operation_id: operationId, historical_result_branch: resultBranch, historical_result_ref: result.ref, historical_result_bytes_sha256: result.bytes_sha256, historical_result_fingerprint: result.fingerprint, stored_historical_response_ref: history.ref, stored_historical_response_bytes_sha256: history.bytes_sha256, stored_historical_response_fingerprint: history.fingerprint, held_registry_row_ref: registry.canonical_row_value.registry_row_ref, held_registry_row_fingerprint: registry.recorded_fingerprint, hold_row_ref: hold.canonical_row_value.hold_row_ref, hold_row_fingerprint: hold.recorded_fingerprint, session_hold_evidence_ref_or_unavailable: evidenceTriple.ref, session_hold_evidence_bytes_sha256_or_unavailable: evidenceTriple.bytes_sha256, session_hold_evidence_fingerprint_or_unavailable: evidenceTriple.fingerprint, held_at: at })
  const envelope = contentArtifact(fixtureId, 'replay_envelope', 'replay_envelope', 'authority_operation_replay_envelope_schema', 'UNAVAILABLE', { envelope_kind: 'authority_operation_replay', replay_kind: hold === 'UNAVAILABLE' ? 'replayed' : 'replayed_held', operation_name: operationName, operation_id: operationId, historical_result_branch: resultBranch, payload_ref: payload.ref, payload_bytes_sha256: payload.bytes_sha256, payload_fingerprint: payload.fingerprint, replay_response_ref: history.ref, replay_response_bytes_sha256: history.bytes_sha256, replay_response_fingerprint: history.fingerprint })
  const artifacts = [target, request, result, history, payload, envelope, ...(proof === 'UNAVAILABLE' ? [] : [proof]), ...(issuerProof === 'UNAVAILABLE' ? [] : [issuerProof, evaluatorProof, bundle, projection, issuerNonce, evaluatorNonce, readSet]), ...(rawBundle === 'UNAVAILABLE' ? [] : [rawBundle, rawEvaluator]), ...(evidence === 'UNAVAILABLE' ? [] : [evidence])]
  if (artifacts.some(row => !row.content_address_rule_verified)) throw new Error(`content_address:${fixtureId}`)
  return { fixture_id: fixtureId, operation_name: operationName, result_branch: resultBranch, request_fingerprint: request.fingerprint, request_artifact: request, target_artifact: target, proof_artifact_or_UNAVAILABLE: proof, issuer_proof_artifact_or_UNAVAILABLE: issuerProof, evaluator_proof_artifact_or_UNAVAILABLE: evaluatorProof, dual_proof_bundle_artifact_or_UNAVAILABLE: bundle, issuer_nonce_artifact_or_UNAVAILABLE: issuerNonce, evaluator_nonce_artifact_or_UNAVAILABLE: evaluatorNonce, raw_bundle_artifact_or_UNAVAILABLE: rawBundle, raw_evaluator_artifact_or_UNAVAILABLE: rawEvaluator, result_artifact: result, historical_response_artifact: history, registry_artifact: registry, hold_artifact_or_UNAVAILABLE: hold, session_evidence_or_UNAVAILABLE: evidence, replay_payload_artifact: payload, replay_envelope_artifact: envelope, classifier_row: r44.authority_operation_replay_classifier.rows.find(row => row.expected_selection_row_id === selected.fresh_selection_row_id && row.operation_name === operationName && row.result_branch === resultBranch), expected_reconstructed_selection: selected, persisted_lineage_artifacts: artifacts }
}
const persistedFixtures = plans.map(actualFixture)
function getDot(value, path) { let current = value; for (const part of path.split('.')) { if (current === 'UNAVAILABLE' || current === undefined || current === null) return undefined; current = current[part] } return current }
const correlationRows = []
function correlate(fixture, category, leftPath, rightPath, type = 'exact') { const left = getDot(fixture, leftPath), right = getDot(fixture, rightPath); if (canonical(left) !== canonical(right)) throw new Error(`R44_correlation:${fixture.fixture_id}:${category}:${leftPath}:${rightPath}`); correlationRows.push({ correlation_id: `${fixture.fixture_id}:${String(correlationRows.length + 1).padStart(3, '0')}`, fixture_id: fixture.fixture_id, category, left_path: leftPath, right_path: rightPath, equality_type: type, schema_declared_source: 'resolved_selected_closed_schema_property_or_store_content_address_rule' }) }
for (const f of persistedFixtures) {
  const held = f.hold_artifact_or_UNAVAILABLE !== 'UNAVAILABLE', registryResultPrefix = held ? 'hold_result' : 'result'
  for (const [category, left, right] of [
    ['operation_name','operation_name','registry_artifact.canonical_row_value.operation_name'],['operation_name','operation_name','result_artifact.payload_value.operation_name'],['operation_id','registry_artifact.canonical_row_value.operation_id','result_artifact.payload_value.operation_id'],['idempotency','registry_artifact.canonical_row_value.idempotency_key','request_artifact.payload_value.idempotency_key'],['request_ref','request_artifact.ref','registry_artifact.canonical_row_value.request_bytes_ref'],['request_bytes','request_artifact.bytes_sha256','registry_artifact.canonical_row_value.request_bytes_sha256'],['request_fingerprint','request_artifact.fingerprint','registry_artifact.canonical_row_value.request_fingerprint'],['result_ref','result_artifact.ref',`registry_artifact.canonical_row_value.${registryResultPrefix}_ref`],['result_bytes','result_artifact.bytes_sha256',`registry_artifact.canonical_row_value.${registryResultPrefix}_bytes_sha256`],['result_fingerprint','result_artifact.fingerprint',`registry_artifact.canonical_row_value.${registryResultPrefix}_fingerprint`],['history_ref','historical_response_artifact.ref','registry_artifact.canonical_row_value.historical_response_ref'],['history_result_ref','result_artifact.ref','historical_response_artifact.payload_value.result_ref'],['history_result_bytes','result_artifact.bytes_sha256','historical_response_artifact.payload_value.result_bytes_sha256'],['history_result_fingerprint','result_artifact.fingerprint','historical_response_artifact.payload_value.result_fingerprint'],['payload_result_ref','result_artifact.ref','replay_payload_artifact.payload_value.historical_result_ref'],['payload_result_bytes','result_artifact.bytes_sha256','replay_payload_artifact.payload_value.historical_result_bytes_sha256'],['payload_history_ref','historical_response_artifact.ref','replay_payload_artifact.payload_value.stored_historical_response_ref'],['envelope_payload_ref','replay_payload_artifact.ref','replay_envelope_artifact.payload_value.payload_ref'],['envelope_payload_bytes','replay_payload_artifact.bytes_sha256','replay_envelope_artifact.payload_value.payload_bytes_sha256'],['envelope_history_ref','historical_response_artifact.ref','replay_envelope_artifact.payload_value.replay_response_ref'],['selection_row','expected_reconstructed_selection.fresh_selection_row_id','registry_artifact.canonical_row_value.fresh_selection_row_id'],['selection_family','expected_reconstructed_selection.proof_family','registry_artifact.canonical_row_value.proof_family'],['selection_class','expected_reconstructed_selection.branch_class','registry_artifact.canonical_row_value.branch_class'],['selection_evidence','expected_reconstructed_selection.evidence_kind','registry_artifact.canonical_row_value.evidence_kind'],['selection_version','expected_reconstructed_selection.fresh_selection_schema_version','registry_artifact.canonical_row_value.fresh_selection_schema_version']
  ]) correlate(f, category, left, right)
  for (const artifact of f.persisted_lineage_artifacts) { const index = f.persisted_lineage_artifacts.indexOf(artifact), opaque = artifact.payload_schema_ref === 'opaque_bounded_bytes', nonce = artifact.payload_schema_ref === 'proof_nonce_receipt_payload_schema', refField = nonce ? 'nonce_receipt_ref' : 'artifact_ref'; correlate(f, 'content_address', `persisted_lineage_artifacts.${index}.ref`, `persisted_lineage_artifacts.${index}.stored_row_value.${refField}`); if (!nonce) correlate(f, 'content_hash', `persisted_lineage_artifacts.${index}.bytes_sha256`, `persisted_lineage_artifacts.${index}.stored_row_value.${opaque ? 'opaque_bytes_sha256' : 'canonical_bytes_sha256'}`, opaque ? 'opaque_bytes_hash' : 'canonical_bytes_hash') }
  if (held) { for (const [category,left,right] of [['hold_ref','hold_artifact_or_UNAVAILABLE.canonical_row_value.hold_row_ref','registry_artifact.canonical_row_value.hold_row_ref'],['hold_result_ref','result_artifact.ref','hold_artifact_or_UNAVAILABLE.canonical_row_value.result_ref'],['hold_result_bytes','result_artifact.bytes_sha256','hold_artifact_or_UNAVAILABLE.canonical_row_value.result_bytes_sha256'],['hold_result_fingerprint','result_artifact.fingerprint','hold_artifact_or_UNAVAILABLE.canonical_row_value.result_fingerprint'],['hold_branch','result_branch','hold_artifact_or_UNAVAILABLE.canonical_row_value.hold_branch'],['held_at','result_artifact.payload_value.held_at','hold_artifact_or_UNAVAILABLE.canonical_row_value.held_at']]) correlate(f,category,left,right) }
  if (f.issuer_proof_artifact_or_UNAVAILABLE !== 'UNAVAILABLE') { for (const [category,left,right] of [['issuer_proof_ref','issuer_proof_artifact_or_UNAVAILABLE.ref','dual_proof_bundle_artifact_or_UNAVAILABLE.payload_value.issuer_proof_ref'],['issuer_proof_bytes','issuer_proof_artifact_or_UNAVAILABLE.bytes_sha256','dual_proof_bundle_artifact_or_UNAVAILABLE.payload_value.issuer_proof_bytes_sha256'],['issuer_proof_fingerprint','issuer_proof_artifact_or_UNAVAILABLE.fingerprint','dual_proof_bundle_artifact_or_UNAVAILABLE.payload_value.issuer_proof_fingerprint'],['issuer_nonce_fingerprint','issuer_proof_artifact_or_UNAVAILABLE.fingerprint','issuer_nonce_artifact_or_UNAVAILABLE.stored_row_value.proof_fingerprint'],['evaluator_proof_ref','evaluator_proof_artifact_or_UNAVAILABLE.ref','dual_proof_bundle_artifact_or_UNAVAILABLE.payload_value.evaluator_proof_ref'],['evaluator_proof_bytes','evaluator_proof_artifact_or_UNAVAILABLE.bytes_sha256','dual_proof_bundle_artifact_or_UNAVAILABLE.payload_value.evaluator_proof_bytes_sha256'],['evaluator_proof_fingerprint','evaluator_proof_artifact_or_UNAVAILABLE.fingerprint','evaluator_nonce_artifact_or_UNAVAILABLE.stored_row_value.proof_fingerprint'],['evidence_issuer_nonce','issuer_nonce_artifact_or_UNAVAILABLE.ref','session_evidence_or_UNAVAILABLE.payload_value.issuer_nonce_receipt_ref'],['evidence_evaluator_nonce','evaluator_nonce_artifact_or_UNAVAILABLE.ref','session_evidence_or_UNAVAILABLE.payload_value.evaluator_nonce_receipt_ref']]) correlate(f,category,left,right) }
  if (f.raw_bundle_artifact_or_UNAVAILABLE !== 'UNAVAILABLE') { correlate(f,'raw_bundle_bytes','raw_bundle_artifact_or_UNAVAILABLE.bytes_sha256','session_evidence_or_UNAVAILABLE.payload_value.raw_bundle_sha256_or_unavailable'); correlate(f,'raw_evaluator_bytes','raw_evaluator_artifact_or_UNAVAILABLE.bytes_sha256','session_evidence_or_UNAVAILABLE.payload_value.raw_evaluator_proof_sha256_or_unavailable') }
}
r44.authority_operation_restart_correlation_authority = { schema_version: 'ctrl.g24.authority-operation-restart-correlation-authority.r44.v2', derivation: 'mechanically_generated_from_selected_closed_schema_properties_store_content_address_rules_and_branch_discriminators', row_schema: closed('ctrl.g24.authority-operation-restart-correlation-row.r44.v1', { correlation_id: id, fixture_id: id, category: id, left_path: id, right_path: id, equality_type: id, schema_declared_source: id }), exact_categories: [...new Set(correlationRows.map(row => row.category))].sort(codePointCompare), exact_row_count: correlationRows.length, rows: correlationRows, coverage: 'all_selected_branch_lineage_fields_and_every_persisted_content_address_are_compared_before_issue_or_replay', mismatch: 'hold_without_disclosure_or_write' }
r44.authority_operation_replay_restart_fixtures = { schema_version: 'ctrl.g24.authority-operation-replay-restart-fixtures.r44.v2', registry_and_hold_artifact_count: 7, complete_lineage_artifact_count: persistedFixtures.reduce((sum, fixture) => sum + fixture.persisted_lineage_artifacts.length + 1 + (fixture.hold_artifact_or_UNAVAILABLE === 'UNAVAILABLE' ? 0 : 1), 0), content_addressed_lineage_artifact_count: persistedFixtures.reduce((sum, fixture) => sum + fixture.persisted_lineage_artifacts.length, 0), exact_fixture_ids: plans.map(row => row[0]), fixtures: persistedFixtures, normative_row_ref_derivation_ref: 'normative_row_ref_derivation', correlation_authority_ref: 'authority_operation_restart_correlation_authority', reconstruction_algorithm: ['resolve_every_persisted_ref_to_exact_selected_store_row', 'decode_and_validate_selected_schema_and_variant', 'recompute_canonical_bytes_hash_declared_fingerprint_and_content_address', 'recompute_normative_registry_or_hold_row_ref', 'evaluate_complete_mechanical_correlation_registry', 'resolve_classifier_from_fingerprint_bound_durable_selection'], synthetic_triple_or_projection_only_fixture: 'forbidden', any_mismatch_splice_or_unavailability: 'hold_without_disclosure_or_write' }
r44.fixture_schema_validator = { ...r44.fixture_schema_validator, schema_version: 'ctrl.g24.fixture-schema-validator.r44.v2', validation_scope: 'all_registry_hold_and_every_persisted_lineage_store_row_payload_schema_fingerprint_content_address_complete_correlation_graph_and_four_restart_paths', exact_persisted_lineage_count: r44.authority_operation_replay_restart_fixtures.complete_lineage_artifact_count }

// One role-keyed store is the only fixture artifact truth in R44. Named views are
// role identifiers, never copied artifact objects.
const branchRoleSets = {
  restart_committed: ['target', 'proof', 'request', 'result', 'history', 'registry', 'replay_payload', 'replay_envelope'],
  restart_ordinary_held: ['target', 'proof', 'request', 'result', 'history', 'hold', 'registry', 'replay_payload', 'replay_envelope'],
  restart_verified_session_held: ['target', 'issuer_proof', 'evaluator_proof', 'bundle_truth_projection', 'bundle', 'issuer_nonce_receipt', 'evaluator_nonce_receipt', 'authority_read_set', 'request', 'session_evidence', 'result', 'history', 'hold', 'registry', 'replay_payload', 'replay_envelope'],
  restart_raw_session_held: ['target', 'raw_bundle', 'raw_evaluator_proof', 'request', 'session_evidence', 'result', 'history', 'hold', 'registry', 'replay_payload', 'replay_envelope'],
}
function roleArtifacts(fixture) { return { target: fixture.target_artifact, proof: fixture.proof_artifact_or_UNAVAILABLE, issuer_proof: fixture.issuer_proof_artifact_or_UNAVAILABLE, evaluator_proof: fixture.evaluator_proof_artifact_or_UNAVAILABLE, bundle_truth_projection: fixture.persisted_lineage_artifacts.find(row => row.artifact_role === 'bundle_truth_projection') ?? 'UNAVAILABLE', bundle: fixture.dual_proof_bundle_artifact_or_UNAVAILABLE, issuer_nonce_receipt: fixture.issuer_nonce_artifact_or_UNAVAILABLE, evaluator_nonce_receipt: fixture.evaluator_nonce_artifact_or_UNAVAILABLE, authority_read_set: fixture.persisted_lineage_artifacts.find(row => row.artifact_role === 'authority_read_set') ?? 'UNAVAILABLE', raw_bundle: fixture.raw_bundle_artifact_or_UNAVAILABLE, raw_evaluator_proof: fixture.raw_evaluator_artifact_or_UNAVAILABLE, request: fixture.request_artifact, session_evidence: fixture.session_evidence_or_UNAVAILABLE, result: fixture.result_artifact, history: fixture.historical_response_artifact, hold: fixture.hold_artifact_or_UNAVAILABLE, registry: fixture.registry_artifact, replay_payload: fixture.replay_payload_artifact, replay_envelope: fixture.replay_envelope_artifact } }
function artifactSchemaIdentity(role, artifact) { if (['registry', 'hold'].includes(role)) return { schema_ref: artifact.schema_ref, schema_variant: artifact.schema_variant, schema_version: artifact.schema_version }; return { schema_ref: artifact.payload_schema_ref, schema_variant: artifact.payload_schema_variant, schema_version: artifact.payload_schema_version } }
function declaredPayloadFingerprint(role, artifact) {
  if (['registry', 'hold'].includes(role)) return { schema_ref: artifact.fingerprint_schema_ref, preimage: artifact.fingerprint_preimage, fingerprint: artifact.recorded_fingerprint }
  if (artifact.payload_schema_ref === 'opaque_bounded_bytes' || artifact.payload_schema_ref === 'proof_nonce_receipt_payload_schema') {
    const schema = artifact.payload_schema_ref === 'opaque_bounded_bytes' ? r44.authority_opaque_raw_input_stores[artifact.payload_schema_variant].row_schema : r44.proof_nonce_ledger.row_schema
    const derived = fingerprintFor(r44, schema, artifact.stored_row_value)
    return { schema_ref: schema.fingerprint_ref, preimage: derived.preimage, fingerprint: derived.fingerprint }
  }
  const schema = schemaAt(r44, artifact.payload_schema_ref, artifact.payload_schema_variant)
  if (schema.fingerprint_field) { const derived = fingerprintFor(r44, schema, artifact.payload_value); return { schema_ref: schema.fingerprint_ref, preimage: derived.preimage, fingerprint: derived.fingerprint } }
  if (role === 'result') {
    const rule = r44.case_session_authority_operation_protocols.operations[artifact.payload_value.operation_name].result_fingerprint
    const branchPayload = {}; for (const field of schema.exact_keys) if (field !== 'result_fingerprint') branchPayload[field] = artifact.payload_value[field]
    const preimage = { domain_ascii: rule.domain_ascii, operation_name: artifact.payload_value.operation_name, operation_id: artifact.payload_value.operation_id, branch: artifact.payload_value.branch, branch_specific_canonical_payload_sha256: hashCanonical(branchPayload) }
    return { schema_ref: `case_session_authority_operation_protocols.operations.${artifact.payload_value.operation_name}.result_fingerprint`, preimage, fingerprint: hashCanonical(preimage) }
  }
  if (role === 'request') {
    const rule = r44.case_session_authority_operation_protocols.operations[artifact.payload_value.operation_name].request_fingerprint, preimage = {}
    for (const field of rule.preimage_order) preimage[field] = field === 'domain_ascii' ? rule.domain_ascii : artifact.payload_value[field]
    return { schema_ref: `case_session_authority_operation_protocols.operations.${artifact.payload_value.operation_name}.request_fingerprint`, preimage, fingerprint: hashCanonical(preimage) }
  }
  const preimage = { domain_ascii: 'CTRL-G24-R44-PARSED-CONTENT', schema_ref: artifact.payload_schema_ref, canonical_bytes_sha256: artifact.payload_bytes_sha256 }
  return { schema_ref: 'authority_operation_fixture_payload_fingerprint_authority.generic_content', preimage, fingerprint: hashCanonical(preimage) }
}
function annotateArtifact(role, artifact) {
  const annotated = apply(PrimordialStructuredClone, undefined, [artifact]), declared = declaredPayloadFingerprint(role, annotated)
  annotated.artifact_role = role
  annotated.fixture_wrapper_variant = role === 'registry' ? 'registry_row' : role === 'hold' ? 'hold_row' : 'content_addressed'
  annotated.declared_payload_fingerprint_schema_ref = declared.schema_ref
  annotated.declared_payload_fingerprint_preimage = declared.preimage
  if (!['registry', 'hold'].includes(role)) {
    if (declared.fingerprint !== annotated.payload_fingerprint) throw new Error(`R44_declared_payload_fingerprint:${role}`)
    const selectedStore = annotated.payload_schema_ref === 'opaque_bounded_bytes' ? r44.authority_opaque_raw_input_stores[annotated.payload_schema_variant] : annotated.payload_schema_ref === 'proof_nonce_receipt_payload_schema' ? r44.proof_nonce_receipt_store : storeFor(role === 'bundle' ? 'bundle' : role === 'bundle_truth_projection' ? 'projection' : role === 'authority_read_set' ? 'read_set' : role === 'session_evidence' ? 'session_evidence' : role === 'history' ? 'history' : role === 'replay_payload' ? 'replay_payload' : role === 'replay_envelope' ? 'replay_envelope' : role === 'request' ? 'requests' : role === 'result' ? 'results' : role === 'target' ? 'targets' : 'proofs', annotated.payload_schema_ref)
    annotated.selected_store_authority_schema_version = selectedStore.schema_version ?? selectedStore.row_schema.schema_version
    annotated.selected_wrapper_schema_version = selectedStore.row_schema?.schema_version ?? r44.proof_nonce_ledger.row_schema.schema_version
    const wrapperSchema = annotated.payload_schema_ref === 'proof_nonce_receipt_payload_schema' ? r44.proof_nonce_ledger.row_schema : selectedStore.row_schema, wrapperDerived = fingerprintFor(r44, wrapperSchema, annotated.stored_row_value)
    annotated.declared_wrapper_fingerprint_schema_ref = wrapperSchema.fingerprint_ref
    annotated.declared_wrapper_fingerprint_preimage = wrapperDerived.preimage
    if (wrapperDerived.fingerprint !== annotated.stored_row_fingerprint) throw new Error(`R44_declared_wrapper_fingerprint:${role}`)
  }
  return annotated
}
function artifactIdentities(role, artifact) { const values = []; if (artifact.ref) values.push({ identity_kind: 'content_address', ref: artifact.ref, bytes_sha256: artifact.bytes_sha256, fingerprint: artifact.fingerprint }); if (role === 'registry') values.push({ identity_kind: 'registry_row', ref: artifact.canonical_row_value.registry_row_ref, bytes_sha256: artifact.canonical_row_bytes_sha256, fingerprint: artifact.recorded_fingerprint }); if (role === 'hold') values.push({ identity_kind: 'hold_row', ref: artifact.canonical_row_value.hold_row_ref, bytes_sha256: artifact.canonical_row_bytes_sha256, fingerprint: artifact.recorded_fingerprint }); return values }
function artifactData(role, artifact) { return ['registry', 'hold'].includes(role) ? [{ path: 'canonical_row_value', value: artifact.canonical_row_value }] : [{ path: 'payload_value', value: artifact.payload_value }, { path: 'stored_row_value', value: artifact.stored_row_value }] }
const canonicalFixtures = persistedFixtures.map(fixture => {
  const available = roleArtifacts(fixture), required = branchRoleSets[fixture.fixture_id], store = {}
  for (const role of required) { if (!available[role] || available[role] === 'UNAVAILABLE' || Object.hasOwn(store, role)) throw new Error(`R44_role_set:${fixture.fixture_id}:${role}`); store[role] = annotateArtifact(role, available[role]) }
  if (Object.keys(store).length !== required.length) throw new Error(`R44_role_count:${fixture.fixture_id}`)
  return { fixture_id: fixture.fixture_id, operation_name: fixture.operation_name, result_branch: fixture.result_branch, request_fingerprint: fixture.request_fingerprint, exact_required_roles: required, artifact_store_by_role: store, artifact_view_role_refs: Object.fromEntries(required.map(role => [role, role])), classifier_row: fixture.classifier_row, expected_reconstructed_selection: fixture.expected_reconstructed_selection }
})
const roleAuthorityRows = canonicalFixtures.map(fixture => ({ fixture_id: fixture.fixture_id, result_branch: fixture.result_branch, exact_required_roles: fixture.exact_required_roles, role_schema_variants: fixture.exact_required_roles.map(role => ({ role, fixture_wrapper_variant: fixture.artifact_store_by_role[role].fixture_wrapper_variant, ...artifactSchemaIdentity(role, fixture.artifact_store_by_role[role]) })) }))
r44.authority_operation_fixture_payload_fingerprint_authority = { schema_version: 'ctrl.g24.authority-operation-fixture-payload-fingerprint-authority.r44.v1', generic_content: { schema_version: 'ctrl.g24.authority-operation-fixture-generic-content-fingerprint.r44.v1', domain_ascii: 'CTRL-G24-R44-PARSED-CONTENT', preimage_order: ['domain_ascii','schema_ref','canonical_bytes_sha256'], canonical_encoding: 'owned_snapshot_unicode_codepoint_sorted_json_utf8' }, native_schema_fingerprint_preimage: 'selected_closed_payload_schema_fingerprint_ref_and_exact_preimage_order', request_fingerprint_preimage: 'selected_operation_request_fingerprint_domain_and_exact_preimage_order', result_fingerprint_preimage: 'selected_operation_result_fingerprint_domain_and_exact_branch_payload_hash', wrapper_fingerprint_preimage: 'selected_store_row_schema_fingerprint_ref_and_exact_preimage_order', comparison_to_self: 'forbidden' }
r44.authority_operation_fixture_persisted_artifact_wrapper_schema = { schema_version: 'ctrl.g24.authority-operation-fixture-persisted-artifact-wrapper-schema.r44.v1', variants: { content_addressed: { exact_keys: Object.keys(canonicalFixtures[0].artifact_store_by_role.target).sort(codePointCompare) }, registry_row: { exact_keys: Object.keys(canonicalFixtures[0].artifact_store_by_role.registry).sort(codePointCompare) }, hold_row: { exact_keys: Object.keys(canonicalFixtures[1].artifact_store_by_role.hold).sort(codePointCompare) } }, exact_keyset_and_variant_required: true, additional_properties: false }
r44.authority_operation_fixture_role_store_authority = { schema_version: 'ctrl.g24.authority-operation-fixture-role-store-authority.r44.v1', store_shape: 'one_closed_role_keyed_object_per_fixture', named_views: 'role_identifiers_only_no_artifact_copies', exact_branch_rows: roleAuthorityRows, missing_duplicate_extra_wrong_role_or_schema: 'hold_without_disclosure_or_write' }

const resolutionRows = [], resolutionKeys = new Set()
function walkArtifactRefs(value, path, visit) { if (!value || typeof value !== 'object') return; for (const [key, item] of Object.entries(value)) { const next = `${path}.${key}`; if (typeof item === 'string' && /_ref(?:_or_unavailable)?$/.test(key) && item !== 'UNAVAILABLE') visit(next, key, item, value); walkArtifactRefs(item, next, visit) } }
for (const fixture of canonicalFixtures) {
  const targets = []
  for (const role of fixture.exact_required_roles) for (const identity of artifactIdentities(role, fixture.artifact_store_by_role[role])) targets.push({ role, ...identity })
  for (const sourceRole of fixture.exact_required_roles) for (const source of artifactData(sourceRole, fixture.artifact_store_by_role[sourceRole])) walkArtifactRefs(source.value, `${sourceRole}.${source.path}`, (sourcePath, field, refValue, parent) => {
    if (source.path === 'stored_row_value' && ['artifact_ref', 'nonce_receipt_ref'].includes(field)) return
    const matches = targets.filter(target => target.ref === refValue)
    if (!matches.length) return
    if (matches.length !== 1) throw new Error(`R44_multiply_resolved:${fixture.fixture_id}:${sourcePath}`)
    const target = matches[0], prefix = field.replace(/_ref(?:_or_unavailable)?$/, ''), byteCandidates = [`${prefix}_bytes_sha256`, `${prefix}_sha256`, `${prefix}_artifact_sha256`], fingerprintCandidates = [`${prefix}_fingerprint`], byteField = byteCandidates.find(key => Object.hasOwn(parent, key)) ?? 'UNAVAILABLE', fingerprintField = fingerprintCandidates.find(key => Object.hasOwn(parent, key)) ?? 'UNAVAILABLE', key = `${fixture.fixture_id}|${sourceRole}|${sourcePath}`
    if (resolutionKeys.has(key)) throw new Error(`R44_duplicate_resolution:${key}`); resolutionKeys.add(key)
    if (byteField !== 'UNAVAILABLE' && parent[byteField] !== target.bytes_sha256) throw new Error(`R44_resolution_bytes:${key}`)
    if (fingerprintField !== 'UNAVAILABLE' && parent[fingerprintField] !== target.fingerprint) throw new Error(`R44_resolution_fingerprint:${key}`)
    resolutionRows.push({ resolution_id: shaBytes(key), fixture_id: fixture.fixture_id, source_role: sourceRole, source_path: sourcePath, reference_field: field, target_role: target.role, target_identity_kind: target.identity_kind, ref_value: refValue, bytes_field_or_UNAVAILABLE: byteField, fingerprint_field_or_UNAVAILABLE: fingerprintField, exact_match_count: 1 })
  })
}
const requiredReferencedRoles = ['target','proof','issuer_proof','evaluator_proof','bundle_truth_projection','bundle','issuer_nonce_receipt','evaluator_nonce_receipt','authority_read_set','raw_bundle','raw_evaluator_proof','request','session_evidence','result','history','hold','registry','replay_payload']
for (const role of requiredReferencedRoles) if (canonicalFixtures.some(f => f.exact_required_roles.includes(role)) && !resolutionRows.some(row => row.target_role === role)) throw new Error(`R44_unresolved_required_role:${role}`)
r44.authority_operation_artifact_resolution_authority = { schema_version: 'ctrl.g24.authority-operation-artifact-resolution-authority.r44.v1', traversal: 'mechanical_all_selected_payload_store_registry_and_hold_reference_fields', exact_one_resolution: 'required_for_every_reference_matching_a_selected_lineage_identity', required_target_roles: requiredReferencedRoles, exact_resolution_count: resolutionRows.length, rows: resolutionRows, unresolved_or_multiply_resolved: 'hold_without_disclosure_or_write' }

const completeRows = []
function correlationValue(fixture, role, path) { if (role === 'fixture_metadata') return getDot(fixture, path); if (role === 'classifier') return getDot(fixture.classifier_row, path); return getDot(fixture.artifact_store_by_role[role], path) }
function addComplete(fixture, category, leftRole, leftPath, rightRole, rightPath, source) { const left = correlationValue(fixture, leftRole, leftPath), right = correlationValue(fixture, rightRole, rightPath); if (left === undefined || right === undefined || canonical(left) !== canonical(right)) throw new Error(`R44_complete_correlation:${fixture.fixture_id}:${category}`); completeRows.push({ correlation_id: shaBytes(`${fixture.fixture_id}|${category}|${leftRole}|${leftPath}|${rightRole}|${rightPath}`), fixture_id: fixture.fixture_id, category, left_role: leftRole, left_path: leftPath, right_role: rightRole, right_path: rightPath, derivation_source: source }) }
for (const fixture of canonicalFixtures) {
  const held = fixture.exact_required_roles.includes('hold'), registry = fixture.artifact_store_by_role.registry, registryRow = registry.canonical_row_value
  for (const role of fixture.exact_required_roles) { const artifact = fixture.artifact_store_by_role[role]; if (!['registry','hold'].includes(role)) { const nonce = artifact.payload_schema_ref === 'proof_nonce_receipt_payload_schema', refField = nonce ? 'nonce_receipt_ref' : 'artifact_ref'; addComplete(fixture, 'content_address', role, 'ref', role, `stored_row_value.${refField}`, 'selected_store_content_address_rule'); const hashField = artifact.payload_schema_ref === 'opaque_bounded_bytes' ? 'opaque_bytes_sha256' : nonce ? 'nonce_receipt_ref' : 'canonical_bytes_sha256'; addComplete(fixture, 'content_hash', role, 'bytes_sha256', role, `stored_row_value.${hashField}`, 'selected_store_content_hash_rule') } }
  const operationRoles = fixture.exact_required_roles.filter(role => getDot(fixture.artifact_store_by_role[role], ['registry','hold'].includes(role) ? 'canonical_row_value.operation_name' : 'payload_value.operation_name') !== undefined)
  for (const role of operationRoles.filter(role => role !== 'registry')) addComplete(fixture, 'operation_name', 'registry', 'canonical_row_value.operation_name', role, ['hold'].includes(role) ? 'canonical_row_value.operation_name' : 'payload_value.operation_name', 'closed_schema_equal_field')
  const operationIdRoles = fixture.exact_required_roles.filter(role => getDot(fixture.artifact_store_by_role[role], ['registry','hold'].includes(role) ? 'canonical_row_value.operation_id' : 'payload_value.operation_id') !== undefined)
  for (const role of operationIdRoles.filter(role => role !== 'registry')) addComplete(fixture, 'operation_id', 'registry', 'canonical_row_value.operation_id', role, role === 'hold' ? 'canonical_row_value.operation_id' : 'payload_value.operation_id', 'closed_schema_equal_field')
  if (held) {
    for (const [category,left,right] of [['hold_branch','canonical_row_value.hold_branch','canonical_row_value.hold_branch'],['hold_row_ref','canonical_row_value.hold_row_ref','canonical_row_value.hold_row_ref'],['hold_fingerprint','canonical_row_value.hold_fingerprint','recorded_fingerprint']]) addComplete(fixture, category, 'registry', left, 'hold', right, 'registry_hold_authority_binding')
    addComplete(fixture, 'payload_hold_ref', 'hold', 'canonical_row_value.hold_row_ref', 'replay_payload', 'payload_value.hold_row_ref', 'replay_resolution_binding')
    addComplete(fixture, 'payload_hold_fingerprint', 'hold', 'recorded_fingerprint', 'replay_payload', 'payload_value.hold_row_fingerprint', 'replay_resolution_binding')
  } else { addComplete(fixture, 'payload_registry_ref', 'registry', 'canonical_row_value.registry_row_ref', 'replay_payload', 'payload_value.committed_registry_row_ref', 'replay_resolution_binding'); addComplete(fixture, 'payload_registry_fingerprint', 'registry', 'recorded_fingerprint', 'replay_payload', 'payload_value.committed_registry_row_fingerprint', 'replay_resolution_binding') }
  const branchPaths = [['result','payload_value.branch'],['history','payload_value.result_branch'],['replay_payload','payload_value.historical_result_branch'],['replay_envelope','payload_value.historical_result_branch']]
  for (const [role,path] of branchPaths.slice(1)) addComplete(fixture, 'result_branch', 'result', 'payload_value.branch', role, path, 'result_history_replay_branch_binding')
  addComplete(fixture, 'fixture_result_branch', 'fixture_metadata', 'result_branch', 'result', 'payload_value.branch', 'fixture_discriminator_binding')
  addComplete(fixture, 'fixture_operation_name', 'fixture_metadata', 'operation_name', 'registry', 'canonical_row_value.operation_name', 'fixture_discriminator_binding')
  addComplete(fixture, 'classifier_result_branch', 'classifier', 'result_branch', 'result', 'payload_value.branch', 'durable_classifier_binding')
  addComplete(fixture, 'classifier_operation_name', 'classifier', 'operation_name', 'registry', 'canonical_row_value.operation_name', 'durable_classifier_binding')
  for (const [registryField, classifierField] of [['fresh_selection_row_id','expected_selection_row_id'],['proof_family','expected_proof_family'],['branch_class','expected_branch_class'],['evidence_kind','expected_evidence_kind'],['fresh_selection_schema_version','expected_selector_version']]) addComplete(fixture, `classifier_${registryField}`, 'registry', `canonical_row_value.${registryField}`, 'classifier', classifierField, 'durable_classifier_binding')
  for (const field of ['fresh_selection_row_id','proof_family','branch_class','evidence_kind','fresh_selection_schema_version']) if (held) addComplete(fixture, `selection_${field}`, 'registry', `canonical_row_value.${field}`, 'hold', `canonical_row_value.${field}`, 'durable_selection_binding')
  if (fixture.exact_required_roles.includes('session_evidence')) { addComplete(fixture, 'session_evidence_branch', 'session_evidence', 'payload_value.hold_branch', 'result', 'payload_value.branch', 'session_evidence_result_binding'); addComplete(fixture, 'session_evidence_operation_id', 'session_evidence', 'payload_value.operation_id', 'registry', 'canonical_row_value.operation_id', 'session_evidence_registry_binding') }
  if (held) { addComplete(fixture, 'payload_held_registry_ref', 'registry', 'canonical_row_value.registry_row_ref', 'replay_payload', 'payload_value.held_registry_row_ref', 'replay_registry_authority_binding'); addComplete(fixture, 'payload_held_registry_fingerprint', 'registry', 'recorded_fingerprint', 'replay_payload', 'payload_value.held_registry_row_fingerprint', 'replay_registry_authority_binding') }
  addComplete(fixture, 'request_fingerprint', 'request', 'fingerprint', 'registry', 'canonical_row_value.request_fingerprint', 'request_registry_binding')
  addComplete(fixture, 'request_ref', 'request', 'ref', 'registry', 'canonical_row_value.request_bytes_ref', 'request_registry_binding')
  addComplete(fixture, 'request_bytes', 'request', 'bytes_sha256', 'registry', 'canonical_row_value.request_bytes_sha256', 'request_registry_binding')
  const resultPrefix = held ? 'hold_result' : 'result'
  for (const [field, suffix] of [['ref','ref'],['bytes_sha256','bytes_sha256'],['fingerprint','fingerprint']]) addComplete(fixture, `registry_result_${suffix}`, 'result', field, 'registry', `canonical_row_value.${resultPrefix}_${suffix}`, 'result_registry_binding')
  for (const [field, suffix] of [['ref','ref'],['bytes_sha256','bytes_sha256'],['fingerprint','fingerprint']]) { addComplete(fixture, `history_result_${suffix}`, 'result', field, 'history', `payload_value.result_${suffix}`, 'result_history_binding'); addComplete(fixture, `payload_result_${suffix}`, 'result', field, 'replay_payload', `payload_value.historical_result_${suffix}`, 'result_replay_binding') }
  for (const [field, suffix] of [['ref','ref'],['bytes_sha256','bytes_sha256'],['fingerprint','fingerprint']]) { addComplete(fixture, `registry_history_${suffix}`, 'history', field, 'registry', `canonical_row_value.historical_response_${suffix}`, 'history_registry_binding'); addComplete(fixture, `payload_history_${suffix}`, 'history', field, 'replay_payload', `payload_value.stored_historical_response_${suffix}`, 'history_replay_binding'); addComplete(fixture, `envelope_history_${suffix}`, 'history', field, 'replay_envelope', `payload_value.replay_response_${suffix}`, 'history_envelope_binding') }
  for (const [field, suffix] of [['ref','ref'],['bytes_sha256','bytes_sha256'],['fingerprint','fingerprint']]) addComplete(fixture, `envelope_payload_${suffix}`, 'replay_payload', field, 'replay_envelope', `payload_value.payload_${suffix}`, 'payload_envelope_binding')
  if (registryRow.request_bytes_ref !== fixture.artifact_store_by_role.request.ref) throw new Error(`R44_request_resolution:${fixture.fixture_id}`)
}
for (const resolution of resolutionRows) completeRows.push({ correlation_id: shaBytes(`resolution|${resolution.resolution_id}`), fixture_id: resolution.fixture_id, category: `resolved_${resolution.target_role}`, left_role: resolution.source_role, left_path: resolution.source_path.replace(`${resolution.source_role}.`, ''), right_role: resolution.target_role, right_path: resolution.target_identity_kind === 'content_address' ? 'ref' : resolution.target_identity_kind === 'registry_row' ? 'canonical_row_value.registry_row_ref' : 'canonical_row_value.hold_row_ref', derivation_source: 'mechanical_exact_one_artifact_resolution' })
r44.authority_operation_restart_correlation_authority = { schema_version: 'ctrl.g24.authority-operation-restart-correlation-authority.r44.v1', derivation: 'mechanical_union_of_every_selected_store_content_rule_exact_one_artifact_resolution_closed_equal_field_registry_hold_binding_durable_selection_classifier_and_replay_resolution', exact_row_count: completeRows.length, rows: completeRows, verifier: 'one_complete_verifier_for_positive_restart_and_every_mutation', handpicked_group_or_parallel_verifier: 'forbidden', mismatch: 'hold_without_disclosure_or_write' }
r44.authority_operation_replay_restart_fixtures = { schema_version: 'ctrl.g24.authority-operation-replay-restart-fixtures.r44.v1', exact_fixture_ids: canonicalFixtures.map(row => row.fixture_id), fixtures: canonicalFixtures, fixture_role_store_authority_ref: 'authority_operation_fixture_role_store_authority', artifact_resolution_authority_ref: 'authority_operation_artifact_resolution_authority', correlation_authority_ref: 'authority_operation_restart_correlation_authority', total_role_keyed_artifact_count: canonicalFixtures.reduce((sum, fixture) => sum + fixture.exact_required_roles.length, 0), exact_restart_count: 4, duplicate_artifact_truth: 'forbidden', restart_failure: 'hold_without_disclosure_or_write' }
r44.fixture_schema_validator = { ...r44.fixture_schema_validator, schema_version: 'ctrl.g24.fixture-schema-validator.r44.v1', validation_scope: 'one_role_keyed_store_exact_branch_role_sets_every_payload_and_wrapper_schema_both_fingerprint_preimages_exact_one_resolution_complete_mechanical_correlation_and_four_restarts', exact_role_keyed_artifact_count: r44.authority_operation_replay_restart_fixtures.total_role_keyed_artifact_count }

r44.semantic_reference_field_specification = {
  schema_version: 'ctrl.g24.semantic-reference-field-specification.r44.v1',
  source: 'independently_pinned_authoritative_schema_and_control_vocabulary_not_any_R40_R41_R42_or_R43_reference_map',
  exact_patterns: [
    { pattern_id: 'tokenized_ref_or_refs', grammar: 'field_name_contains_start_or_underscore_then_ref_or_refs_then_end_or_underscore', occurrence_kind: 'reference_bearing_field' },
    { pattern_id: 'schema_property_reference', grammar: 'closed_schema_property_name_matches_tokenized_ref_or_refs', occurrence_kind: 'typed_runtime_reference_field' },
    { pattern_id: 'semantic_authority_literal', grammar: 'string_value_exactly_equals_or_is_a_declared_child_path_of_one_manifested_semantic_authority_or_pinned_alias', occurrence_kind: 'semantic_authority_reference' },
  ],
  exact_non_suffix_semantic_fields: ['authority', 'then', 'sole_writer', 'writer', 'source', 'target', 'derivation', 'binding', 'transaction_boundary', 'selected_branch_table', 'selected_matrix', 'selected_result_artifact_store_family'],
  required_named_categories: ['_ref', '_refs', 'source_refs', 'transaction_boundary_ref', 'selected_branch_table_ref', 'selected_matrix_ref', 'selected_result_artifact_store_family_ref', 'branch_effects_ref', 'field_encoding_ref', 'transition_table_ref', 'total_result_discriminated_compound', 'replay_path_discriminated_pattern', 'semantic_string_without_suffix'],
  unknown_ref_bearing_field_or_path: 'reject_materialization_and_hold_without_disclosure_or_write',
  occurrence_requirement: 'exact_bijection_between_full_machine_traversal_and_registry_rows',
}
const semanticAuthorityPaths = [...new Set([...r43SemanticAuthorityPaths, 'canonical_snapshot_resource_limits', 'normative_row_ref_derivation', 'authority_operation_fixture_payload_fingerprint_authority', 'authority_operation_fixture_persisted_artifact_wrapper_schema', 'authority_operation_fixture_role_store_authority', 'authority_operation_artifact_resolution_authority', 'authority_operation_restart_correlation_authority', 'semantic_reference_field_specification'])].sort(codePointCompare)
r44.authority_runtime_semantic_manifest_envelope_schema = closed('ctrl.g24.runtime-semantic-authority-manifest-envelope.r44.v1', { schema_version: { const: 'ctrl.g24.runtime-semantic-authority-manifest.r44.v1' }, type: { const: 'owned_snapshot_self_sealed_runtime_semantic_manifest' }, snapshot_pipeline_ref: { const: 'owned_immutable_canonical_snapshot_pipeline' }, hash_contract_ref: { const: 'authority_runtime_semantic_manifest_hash_contract' }, dependency_owner_map_ref: { const: 'authority_runtime_semantic_dependency_owner_map' }, reference_field_registry_ref: { const: 'authority_runtime_semantic_reference_field_registry' }, exact_paths: { type: 'unicode_sorted_unique_identifier_array' }, rows: { type: 'ordered_manifest_row_array' }, exact_expected_count: { const: semanticAuthorityPaths.length }, manifest_graph_sha256: fp, manifest_envelope_seal_sha256: fp }, { caller_writable_fields: [], fallback_or_default: 'forbidden' })

const aliasTargets = { selected_session_operation_request_schema: 'request_schema', selected_session_operation_result_schema: 'operation_result_schema_derivation', selected_result_schema: 'operation_result_schema_derivation', 'authority_runtime_semantic_manifest.row_schema': 'authority_runtime_semantic_manifest_envelope_schema' }
function manifestOwnerForPath(path) { let selected = ''; for (const authorityPath of semanticAuthorityPaths) if ((path === authorityPath || path.startsWith(`${authorityPath}.`)) && authorityPath.length > selected.length) selected = authorityPath; return selected }
function exactSemanticTarget(reference) {
  if (aliasTargets[reference]) return { target: aliasTargets[reference], runtimeKind: 'pinned_semantic_alias' }
  const normalized = reference.replace(/^\$\./, '')
  if (normalized.includes('*')) return { target: 'UNAVAILABLE', runtimeKind: 'discriminated_wildcard_reference' }
  if (resolvePath(r44, normalized) !== undefined) return { target: normalized, runtimeKind: 'UNAVAILABLE' }
  if (reference.includes('_or_') || reference.includes('|')) return { target: 'UNAVAILABLE', runtimeKind: 'closed_discriminated_compound_reference' }
  return { target: 'UNAVAILABLE', runtimeKind: 'runtime_identity_or_field_reference' }
}
function targetVersion(target) { if (target === 'UNAVAILABLE') return 'UNAVAILABLE'; let cursor = target; while (cursor) { const value = resolvePath(r44, cursor); if (value?.schema_version) return value.schema_version; const split = cursor.lastIndexOf('.'); if (split < 0) break; cursor = cursor.slice(0, split) } return semanticVersion(target, resolvePath(r44, target)) }
function ownerFor(reference, source) { const exact = exactSemanticTarget(reference); return exact.target === 'UNAVAILABLE' ? source : manifestOwnerForPath(exact.target) || source }
function refTokenField(key) { return /(^|_)(ref|refs)($|_)/.test(key) }
const noSuffixFields = new Set(r44.semantic_reference_field_specification.exact_non_suffix_semantic_fields)
const referenceRows = [], occurrenceKeys = new Set()
function addReference(source, fieldPath, fieldName, literal, reason) { const resolution = exactSemanticTarget(literal), owner = ownerFor(literal, source), kind = literal === 'UNAVAILABLE' ? 'exact_sentinel_reference' : resolution.target !== 'UNAVAILABLE' ? (fieldName.includes('schema') ? 'exact_nested_schema_reference' : 'exact_nested_semantic_reference') : resolution.runtimeKind; const key = `${source}|${fieldPath}|${literal}`; if (occurrenceKeys.has(key)) return; occurrenceKeys.add(key); referenceRows.push({ source_authority_path: source, field_path: fieldPath, field_name: fieldName, reference_kind: kind, reference_literal: literal, owner_authority_path: owner, expected_owner_schema_version: semanticVersion(owner, resolvePath(r44, owner)), exact_target_path_or_UNAVAILABLE: resolution.target, exact_target_schema_version_or_UNAVAILABLE: targetVersion(resolution.target), runtime_identity_kind_or_UNAVAILABLE: resolution.target === 'UNAVAILABLE' ? resolution.runtimeKind : 'UNAVAILABLE', specification_pattern_id: reason }) }
function walkReferences(value, source, fieldPath = source) { if (!value || typeof value !== 'object') return; for (const [key, item] of Object.entries(value)) { const next = `${fieldPath}.${key}`; if (refTokenField(key) || noSuffixFields.has(key)) { if (Array.isArray(item)) { for (let index = 0; index < item.length; index += 1) if (typeof item[index] === 'string') addReference(source, `${next}.${index}`, key, item[index], refTokenField(key) ? 'tokenized_ref_or_refs' : 'explicit_non_suffix') } else if (typeof item === 'string') addReference(source, next, key, item, refTokenField(key) ? 'tokenized_ref_or_refs' : 'explicit_non_suffix') } if (typeof item === 'string' && ownerFor(item, source) !== source) addReference(source, next, key, item, 'semantic_value'); walkReferences(item, source, next) } }
r44.schema_change_manifest = { schema_version: 'ctrl.g24.trusted-ingress-schema-change-manifest.r44.v1', derivation: 'bounded_exact_extension_from_frozen_R43_to_R44_one_role_keyed_persisted_artifact_truth_exact_one_resolution_declared_wrapper_fingerprints_and_complete_correlation_authority', frozen_parent_sha256: shaBytes(inputBytes), changed_semantic_paths: ['$', '$.materialization', '$.authority_operation_fixture_payload_fingerprint_authority', '$.authority_operation_fixture_persisted_artifact_wrapper_schema', '$.authority_operation_fixture_role_store_authority', '$.authority_operation_artifact_resolution_authority', '$.authority_operation_replay_restart_fixtures', '$.authority_operation_restart_correlation_authority', '$.fixture_schema_validator', '$.semantic_reference_field_specification', '$.authority_runtime_semantic_reference_field_registry', '$.authority_runtime_semantic_reference_owner_map', '$.authority_runtime_semantic_dependency_owner_map', '$.authority_runtime_semantic_manifest_hash_contract', '$.authority_runtime_semantic_manifest_envelope_schema', '$.authority_runtime_semantic_manifest', '$.schema_change_manifest'], removed_semantic_paths: ['$.authority_operation_replay_restart_fixtures.fixtures.*.persisted_lineage_artifacts', '$.authority_operation_replay_restart_fixtures.fixtures.*.named_artifact_objects'], recursive_same_version_semantic_change: 'forbidden', every_changed_or_new_semantic_object_has_r44_identifier: true, frozen_parent_core_must_remain_byte_identical: true, caller_writer_or_precedence_extensions: 'forbidden' }
r44.required_negative_fixture_families = [...new Set([...r44.required_negative_fixture_families, 'normative_schema_row_ref_derivation', 'full_restart_correlation_and_resealed_splice', 'captured_primordial_prototype_pollution', 'preallocation_resource_limits', 'independent_reference_universe_bijection'])]

const hashVersion = 'ctrl.g24.runtime-semantic-authority-manifest-hash.r44.v1'
r44.authority_runtime_semantic_manifest_hash_contract = { schema_version: hashVersion, snapshot_pipeline_ref: 'owned_immutable_canonical_snapshot_pipeline', resource_limits_ref: 'canonical_snapshot_resource_limits', canonical_codec_ref: 'canonical_json_utf8_encoding', hash_input: 'bounded_owned_immutable_snapshot_only', content_domain_ascii: 'CTRL-G24-RUNTIME-SEMANTIC-CONTENT-R44', dependency_domain_ascii: 'CTRL-G24-RUNTIME-SEMANTIC-DEPENDENCIES-R44', graph_domain_ascii: 'CTRL-G24-RUNTIME-SEMANTIC-GRAPH-R44', envelope_domain_ascii: 'CTRL-G24-RUNTIME-SEMANTIC-MANIFEST-ENVELOPE-R44', hashing_rule: 'bounded_snapshot_then_sha256_exact_unicode_sorted_canonical_JSON_UTF8_using_captured_primordials' }
function contentHash(path, value) { return hashCanonical({ domain_ascii: r44.authority_runtime_semantic_manifest_hash_contract.content_domain_ascii, manifest_hash_version: hashVersion, authority_path: path, canonical_authority_snapshot: snapshotOwned(value) }) }
function dependencyHash(path, scope, rows) { return hashCanonical({ domain_ascii: r44.authority_runtime_semantic_manifest_hash_contract.dependency_domain_ascii, manifest_hash_version: hashVersion, authority_path: path, dependency_scope: scope, canonical_sorted_dependency_rows: rows }) }
function graphHash(rows) { return hashCanonical({ domain_ascii: r44.authority_runtime_semantic_manifest_hash_contract.graph_domain_ascii, manifest_hash_version: hashVersion, manifest_rows: rows }) }
function envelopeHash(manifest) { return hashCanonical({ domain_ascii: r44.authority_runtime_semantic_manifest_hash_contract.envelope_domain_ascii, manifest_hash_version: hashVersion, manifest_without_envelope_seal: manifest }) }
r44.authority_runtime_semantic_dependency_owner_map = { ...r44.authority_runtime_semantic_dependency_owner_map, schema_version: 'ctrl.g24.runtime-semantic-dependency-owner-map.r44.v1' }
r44.authority_runtime_semantic_reference_field_registry = { ...r44.authority_runtime_semantic_reference_field_registry, schema_version: 'ctrl.g24.runtime-semantic-reference-field-registry.r44.v1' }
r44.authority_runtime_semantic_reference_owner_map = { ...r44.authority_runtime_semantic_reference_owner_map, schema_version: 'ctrl.g24.runtime-semantic-reference-owner-map.r44.v1' }
r44.authority_runtime_semantic_manifest = { ...r44.authority_runtime_semantic_manifest, schema_version: 'ctrl.g24.runtime-semantic-authority-manifest.r44.v1' }
for (const path of semanticAuthorityPaths) if (!['authority_runtime_semantic_manifest', 'authority_runtime_semantic_dependency_owner_map', 'authority_runtime_semantic_reference_owner_map', 'authority_runtime_semantic_reference_field_registry', 'semantic_reference_field_specification'].includes(path)) walkReferences(resolvePath(r44, path), path)
referenceRows.sort((left, right) => codePointCompare(`${left.source_authority_path}|${left.field_path}|${left.reference_literal}`, `${right.source_authority_path}|${right.field_path}|${right.reference_literal}`))
r44.authority_runtime_semantic_reference_field_registry = { schema_version: 'ctrl.g24.runtime-semantic-reference-field-registry.r44.v1', specification_ref: 'semantic_reference_field_specification', row_schema: closed('ctrl.g24.runtime-semantic-reference-field-registry-row.r44.v1', { source_authority_path: id, field_path: id, field_name: id, reference_kind: id, reference_literal: id, owner_authority_path: id, expected_owner_schema_version: id, exact_target_path_or_UNAVAILABLE: id, exact_target_schema_version_or_UNAVAILABLE: id, runtime_identity_kind_or_UNAVAILABLE: id, specification_pattern_id: id }), exact_occurrence_rows: referenceRows, exact_expected_occurrence_count: referenceRows.length, occurrence_bijection: 'every_actual_reference_occurrence_selected_by_the_independent_R44_specification_exactly_once', precise_target_rule: 'each_semantic_reference_resolves_the_exact_nested_object_and_its_own_nearest_schema_version_never_a_top_level_fallback', runtime_discriminator_rule: 'runtime_identifiers_sentinels_wildcards_and_closed_compounds_are_explicitly_discriminated_and_never_claim_semantic_target_resolution', unknown_reference_semantics: 'reject_materialization_and_hold_without_disclosure_or_write', inference_from_suffix_only: 'forbidden' }
r44.authority_runtime_semantic_reference_owner_map = { schema_version: 'ctrl.g24.runtime-semantic-reference-owner-map.r44.v1', specification_ref: 'semantic_reference_field_specification', reference_field_registry_ref: 'authority_runtime_semantic_reference_field_registry', row_count: referenceRows.length, ownership_rule: 'semantic_authority_refs_resolve_one_manifested_owner_runtime_identity_and_field_refs_are_owned_by_their_closed_source_authority' }
const priorOwners = Object.fromEntries(materializedR43.authority_runtime_semantic_dependency_owner_map.rows.map(row => [row.authority_path, row.typed_owner_paths]))
const occurrenceDeps = {}; for (const row of referenceRows) if (row.owner_authority_path !== row.source_authority_path) (occurrenceDeps[row.source_authority_path] ??= new Set()).add(row.owner_authority_path)
const dependencyOverrides = { authority_operation_replay_restart_fixtures: ['authority_operation_registry', 'authority_operation_hold_store', 'authority_operation_historical_response_schema', 'authority_operation_replay_payload_schema', 'authority_operation_replay_envelope_schema', 'session_hold_evidence_schema', 'authority_operation_restart_correlation_authority', 'normative_row_ref_derivation', 'fixture_schema_validator', 'owned_immutable_canonical_snapshot_pipeline', 'canonical_snapshot_resource_limits'], authority_operation_restart_correlation_authority: ['authority_operation_registry', 'authority_operation_hold_store', 'authority_operation_replay_payload_schema', 'authority_operation_replay_envelope_schema', 'session_hold_evidence_schema'], normative_row_ref_derivation: ['authority_operation_registry', 'authority_operation_hold_store', 'canonical_json_utf8_encoding'], owned_immutable_canonical_snapshot_pipeline: ['canonical_snapshot_resource_limits'], canonical_snapshot_resource_limits: [], semantic_reference_field_specification: [], fixture_schema_validator: [], materialization: [], schema_change_manifest: [] }
const ownerRows = semanticAuthorityPaths.map(path => ({ authority_path: path, typed_owner_paths: [...new Set([...(dependencyOverrides[path] ?? priorOwners[path] ?? []), ...[...(occurrenceDeps[path] ?? [])]])].filter(owner => semanticAuthorityPaths.includes(owner) && owner !== path).sort(codePointCompare), unqualified_legacy_alias_owner_path: path }))
r44.authority_runtime_semantic_dependency_owner_map = { schema_version: 'ctrl.g24.runtime-semantic-dependency-owner-map.r44.v1', exact_paths: semanticAuthorityPaths, rows: ownerRows, reference_field_specification_ref: 'semantic_reference_field_specification', reference_field_registry_ref: 'authority_runtime_semantic_reference_field_registry', explicit_reference_edges_required: true, unresolved_multiply_owned_or_caller_ref: 'reject_materialization_and_hold_without_disclosure_or_write' }
delete r44.authority_runtime_semantic_manifest
const contentHashes = Object.fromEntries(semanticAuthorityPaths.map(path => [path, contentHash(path, resolvePath(r44, path))]))
const graph = Object.fromEntries(ownerRows.map(row => [row.authority_path, row.typed_owner_paths]))
function transitive(path) { const seen = new Set(), visit = current => { for (const dependency of graph[current] ?? []) if (!seen.has(dependency)) { seen.add(dependency); visit(dependency) } }; visit(path); seen.delete(path); return [...seen].sort(codePointCompare) }
const manifestRows = semanticAuthorityPaths.map(path => { const value = resolvePath(r44, path), directPaths = graph[path], directRows = directPaths.map(owner => ({ authority_path: owner, authority_content_sha256: contentHashes[owner] })), transitiveRows = transitive(path).map(owner => ({ authority_path: owner, authority_content_sha256: contentHashes[owner] })); return { authority_path: path, semantic_kind: kindOf(value), exact_keyset: keysetOf(value), authority_schema_ref: path, authority_schema_version: semanticVersion(path, value), direct_dependency_paths: directPaths, direct_dependency_content_hashes: directRows, direct_dependency_set_sha256: dependencyHash(path, 'direct', directRows), transitive_dependency_paths: transitiveRows.map(row => row.authority_path), transitive_dependency_content_hashes: transitiveRows, transitive_dependency_set_sha256: dependencyHash(path, 'transitive', transitiveRows), authority_content_sha256: contentHashes[path] } })
const manifestWithoutSeal = { schema_version: 'ctrl.g24.runtime-semantic-authority-manifest.r44.v1', type: 'owned_bounded_snapshot_self_sealed_runtime_semantic_manifest', snapshot_pipeline_ref: 'owned_immutable_canonical_snapshot_pipeline', resource_limits_ref: 'canonical_snapshot_resource_limits', hash_contract_ref: 'authority_runtime_semantic_manifest_hash_contract', dependency_owner_map_ref: 'authority_runtime_semantic_dependency_owner_map', reference_field_specification_ref: 'semantic_reference_field_specification', reference_field_registry_ref: 'authority_runtime_semantic_reference_field_registry', exact_paths: semanticAuthorityPaths, rows: manifestRows, exact_expected_count: semanticAuthorityPaths.length, manifest_graph_sha256: graphHash(manifestRows) }
r44.authority_runtime_semantic_manifest = { ...manifestWithoutSeal, manifest_envelope_seal_sha256: envelopeHash(manifestWithoutSeal) }
r44.visible_surface_changes = []
r44.external_actions_authorized = []

const finalSnapshot = snapshotOwned(r44)
export const materializedR44 = r44
export const materializedR44Snapshot = finalSnapshot
export const materializedR44Output = `${apply(PrimordialJSONstringify, JSON, [finalSnapshot, null, 2])}\n`
export const ownedSnapshotR44 = snapshotOwned
export const canonicalR44 = canonical
export const r44SemanticAuthorityPaths = semanticAuthorityPaths
export const r44ResourceLimits = LIMITS
export const deriveNormativeRowRefR44 = deriveNormativeRowRef
const invoked = process.argv[1] && resolve(process.argv[1]) === fileURLToPath(import.meta.url)
if (invoked) { const mode = process.argv[2] ?? '--check'; if (mode === '--write') { writeFileSync(join(root, outputPath), materializedR44Output); console.log(`wrote ${outputPath}`) } else if (mode === '--check') { if (readFileSync(join(root, outputPath), 'utf8') !== materializedR44Output) { console.error(`${outputPath} differs from materializer`); process.exit(1) } console.log(`ok: ${outputPath} is the exact fully materialized R44 effective contract`) } else throw new Error(`unsupported mode:${mode}`) }
